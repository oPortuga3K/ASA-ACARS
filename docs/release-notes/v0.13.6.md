## 🇩🇪 Deutsch

**v0.13.6 — vereint alle Stable-Fixes aus v0.12.5-12.12 mit dem v0.13.x-Integrity-System. Wichtigster Effekt: Auto-File-PIREP läuft jetzt im Backend (= völlig Fenster- und Tab-unabhängig) UND der Integrity-Validator ist im Permissive-Mode der nichts mehr unnötig blockiert.**

### Was Du davon hast

**Wenn Du gerade von v0.13.5 kommst:** Du bekommst rückwirkend die 8 Mai-Releases die in Deiner v0.13.x-Linie gefehlt haben:

- **Backend-Auto-File** (war v0.12.6 vom 22.05.) — PIREP wird im Rust-Backend automatisch eingereicht beim Phase-Wechsel auf "Arrived". **Fenster im Hintergrund? Anderer Tab offen? Egal — geht trotzdem raus.** Genau Torbens Befund von heute, war auf der main-Linie schon im Mai gefixt.
- **Divert/Manual-PIREP-Reparatur** (v0.12.5)
- **V/S-Profil-Redesign mit Flare-Aufschlüsselung** (v0.12.7) + **Landungs-Grafiken 1:1 wie VPS** (v0.12.8)
- **Wind-Visualisierung + PDF-Export der Landungs-Analyse** (v0.12.9)
- **BlackSquare-Caravan ATC-Modell-Bereinigung** (v0.12.10)
- **Auto-Start flutet das Aktivitätsprotokoll nicht mehr** (v0.12.11)
- **Nachrichten-Tab + Wetter-Briefing-Knopf** (v0.12.12)
- **Doppelter-Haken-Notice-Fix** (im latest main)

**Wenn Du gerade von v0.12.12 kommst:** Du bekommst zusätzlich das komplette v0.13.x-Integrity-System:

- Mid-Session Integrity Validator (Server-Side) — erkennt Sim-Crashes, false PIREPs etc.
- Stream F Sim-Crash-Resume mit Pilot-getriggertem Position-Re-Check
- Live-Δ-Banner (gespeichert vs Sim jetzt vs Differenz) damit Du beim Reposition sofort siehst ob Du in der Toleranz bist

### Wichtig: Validator-Update von heute Vormittag (v0.13.4 Server)

Der Server-Side-Validator wurde radikal entschärft. Praktisch jeder Flug ist jetzt **trusted** und geht automatisch durch. Nur zwei spezifische Trigger landen noch in der Review-Queue beim VA-Owner:

1. PIREP ohne Touchdown (orphan/incomplete)
2. Sim-Crash-Signatur am Touchdown (parking_brake + engines_off + Telemetrie-Korruption zusammen — = WZZ2271-Pattern)

Banner während des Flugs zeigen weiterhin Anomalien an, **beeinflussen aber Dein Verdict nicht mehr automatisch.** Bei normalem Fliegen merkst Du das ganze Integrity-System nicht.

### Lessons-learned (transparent)

- v0.13.0 war auf einer alten v0.12.4-Basis gebaut. Heute v0.13.5 Auto-File-React-Fix gebaut für Torben — **nicht gewusst** dass main bereits Backend-Auto-File (v0.12.6) hat. Mein React-Watcher war redundant.
- v0.13.6 vereint beide Linien. React-Watcher wurde gelöscht (Backend macht's bereits korrekt).
- Bei künftigen Feature-Branches: erst `git fetch && git log origin/main..HEAD` checken bevor neue Lösungen gebaut werden.

### Was Du tun musst

Nichts. Auto-Updater rollt v0.13.6 aus, beim nächsten App-Start ist alles aktiv.

---

## 🇬🇧 English

**v0.13.6 — merges all stable fixes from v0.12.5-12.12 with the v0.13.x integrity system. Main upshot: PIREP auto-file now runs in the backend (= fully window- and tab-independent) AND the integrity validator runs in permissive mode which no longer unnecessarily blocks.**

### What you get

**Coming from v0.13.5:** You retroactively get the 8 May releases that were missing from your v0.13.x line:

- **Backend auto-file** (was v0.12.6 from 22.05.) — PIREP gets auto-submitted in the Rust backend on phase transition to "Arrived". **Window in background? Other tab open? Doesn't matter — still goes out.** Exactly Torben's finding from today, was already fixed on the main line in May.
- **Divert/manual-PIREP repair** (v0.12.5)
- **V/S profile redesign with flare breakdown** (v0.12.7) + **landing graphs 1:1 like VPS** (v0.12.8)
- **Wind visualization + PDF export of landing analysis** (v0.12.9)
- **BlackSquare Caravan ATC model cleanup** (v0.12.10)
- **Auto-start no longer floods the activity log** (v0.12.11)
- **Messages tab + weather briefing button** (v0.12.12)

**Coming from v0.12.12:** You additionally get the complete v0.13.x integrity system: mid-session integrity validator, Stream F sim-crash resume with pilot-triggered position re-check, live Δ-banner.

### Important: validator update from this morning (v0.13.4 server)

The server-side validator was significantly relaxed. Practically every flight is now **trusted** and auto-accepted. Only two specific triggers still land in the VA owner's review queue (PIREP with no touchdown / sim-crash signature). If you fly normally, you won't notice the integrity system at all.

### What you need to do

Nothing. Auto-updater rolls out v0.13.6, everything active on next app start.

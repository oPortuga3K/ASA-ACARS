## 🇩🇪 Deutsch

### iFly 737 MAX — Profil-Audit

- **Parkbremse:** Die Anzeige liest jetzt den echten Bremshebel des iFly statt des Standard-SimVars, der beim Einschalten der Elektrik minutenlang fälschlich "gelöst" meldete.
- **Flaps:** Boeing-Flugzeuge zeigen jetzt echte Boeing-Stufen (UP, 1, 2, 5, 10, 15, 25, 30, 40) statt Airbus-Bezeichnungen wie "1+F".
- **Flap-Protokoll entprellt:** Kurze Zwischenwerte beim Durchfahren der Rasten (z. B. ein 3-Sekunden-"FULL") landen nicht mehr im Aktivitätsprotokoll.
- **Autobrake:** Der iFly-Wahlschalter wird jetzt korrekt angezeigt (RTO, OFF, 1, 2, 3, MAX).
- **A/THR:** Zeigt beim iFly "ARMED" statt fälschlich "ENGAGED".

### HUD & Widget

- **Restflugzeit (ETE) plausibilisiert:** Die ETE erscheint erst, wenn sie unter dem 1,3-fachen der geplanten Flugzeit liegt — keine Fantasiewerte mehr direkt nach dem Start.
- **Zeitanzeige rotiert automatisch:** ETE, Ankunftszeit (ETA) und geflogene Zeit (FLT) wechseln alle 8 Sekunden von selbst.
- **Browser-HUD:** Die /hud-Seite ist jetzt transparent und kompakt — der Streifen endet hinter dem Text, ideal als Overlay.
- **Flow-Widget v3.9** steht als Download bereit (Crewcenter → ACARS-Tools).

## 🇬🇧 English

### iFly 737 MAX — profile audit

- **Parking brake:** The display now reads the iFly's actual brake lever instead of the standard SimVar, which falsely reported "released" for minutes after powering up the aircraft.
- **Flaps:** Boeing aircraft now show real Boeing detents (UP, 1, 2, 5, 10, 15, 25, 30, 40) instead of Airbus labels like "1+F".
- **Flap log debounced:** Brief intermediate values while moving through the detents (e.g. a 3-second "FULL") no longer end up in the activity log.
- **Autobrake:** The iFly selector is now decoded correctly (RTO, OFF, 1, 2, 3, MAX).
- **A/THR:** Shows "ARMED" on the iFly instead of incorrectly "ENGAGED".

### HUD & widget

- **Time-to-arrival (ETE) sanity check:** The ETE only appears once it drops below 1.3× the planned flight time — no more fantasy values right after departure.
- **Time cell auto-rotates:** ETE, arrival time (ETA) and elapsed time (FLT) now cycle automatically every 8 seconds.
- **Browser HUD:** The /hud page is now transparent and compact — the strip ends right after the text, ideal as an overlay.
- **Flow widget v3.9** is available for download (crew center → ACARS tools).

---

*Keine Änderungen am Wire-Format — kein asa-acars-live-Deploy nötig. / No wire-format changes — no asa-acars-live deploy required.*

## 🇩🇪 Deutsch

**v0.13.13 — FSReborn Phenom 300E Aircraft-Profile. Wer mit der FSR Phenom 300E im MSFS fliegt: der Auto-Start funktioniert jetzt sauber aus Cold & Dark.**

### Was Du davon hast

**Real-World-Befund (Pilot Michael, NJE 265 LEBL → EGJJ, 26.05.2026):** Der ASA-ACARS Auto-Start lehnte den Flugstart ab mit der Meldung "Triebwerke sind an — Auto-Start greift nur bei kalt/stehendem Flugzeug." Tatsächlich standen die Triebwerke aber **aus**. Der FSR Phenom 300E liefert beim Standard-SimVar `GENERAL ENG COMBUSTION:N` in der Cold-&-Dark-Phase unzuverlässige Werte → ASA-ACARS dachte, die Engines liefen.

Ab v0.13.13 erkennt ASA-ACARS den FSR Phenom 300E am Aircraft-Title und liest stattdessen die **FSR-eigenen Engine-Knob-LVars** direkt aus:
- `L:FSR_300E_ENGINE1_KNOB_POS` (0 = STOP, 1 = RUN, 2 = START)
- `L:FSR_300E_ENGINE2_KNOB_POS` (gleiches Schema)

Diese reflektieren den **direkten Pilot-Befehl** und sind in jedem Aircraft-State zuverlässig — insbesondere im Cold & Dark vor dem ersten Engine-Start.

### Was das praktisch heißt

| Szenario | Vor v0.13.13 | Ab v0.13.13 |
|---|---|---|
| Cold & Dark am Gate, beide Engines AUS | Auto-Start scheitert, "Triebwerke sind an" | Auto-Start triggert sauber ✓ |
| Engine 1 im START-Sequenz | engines_running zeigt falsch 0 | engines_running = 1 ✓ |
| Beide Engines RUN im Cruise | funktionierte schon, kein Verhalten geändert | identisch ✓ |
| Andere Phenom-Addons (Asobo Default, Carenado etc.) | wie bisher | unverändert — Detection greift nur bei `fsreborn + phenom + 300` im Title |

### Aircraft Profile Label

Im Activity-Log siehst Du nach Aircraft-Detection jetzt: **"Aircraft-Profile: FSReborn Phenom 300E"** (analog zu Fenix/PMDG/FlyByWire-Profilen).

### Bonus: HubHop Reference-Doc

Für künftige Aircraft-Profile-Bauten haben wir die MobiFlight-HubHop-Datenbank ausgewertet:
- 31.435 Presets / 61 Vendoren / 200+ Aircraft
- API-URL + Workflow dokumentiert in `docs/dev/lvar-discovery-hubhop.md`

Wenn ein neuer Addon mit kaputten Standard-SimVars auftaucht, finden wir die nötigen LVars jetzt in unter 5 Minuten statt mehreren Stunden Suche.

### Was Du tun musst

Nichts. Auto-Updater rollt v0.13.13 aus. Wer mit FSR Phenom 300E fliegt, kann ab nächstem App-Start aus Cold & Dark Auto-Start nutzen.

### QS

- 7 neue Unit-Tests für die FSR-Phenom-300E-Detection (mit Michaels echtem Aircraft-Title, ICAO-Fallback E55P, Edge-Cases gegen Asobo-Phenom + andere FSR-Modelle)
- 414 Workspace-Tests grün
- 73/73 Frontend-Tests grün
- cargo build + Frontend Production Build clean

---

## 🇬🇧 English

**v0.13.13 — FSReborn Phenom 300E aircraft profile. If you fly the FSR Phenom 300E in MSFS: auto-start now works cleanly from cold & dark.**

### What you get

**Real-world finding (pilot Michael, NJE 265 LEBL → EGJJ, 2026-05-26):** ASA-ACARS auto-start refused to start the flight with the message "Triebwerke sind an — Auto-Start greift nur bei kalt/stehendem Flugzeug" ("Engines on — auto-start only works for cold/stationary aircraft"). The engines were actually **off**. The FSR Phenom 300E provides unreliable values for the standard `GENERAL ENG COMBUSTION:N` SimVar in the cold & dark phase → ASA-ACARS thought engines were running.

From v0.13.13 ASA-ACARS detects the FSR Phenom 300E via the aircraft title and reads the **FSR-specific engine knob LVars** directly:
- `L:FSR_300E_ENGINE1_KNOB_POS` (0 = STOP, 1 = RUN, 2 = START)
- `L:FSR_300E_ENGINE2_KNOB_POS` (same scheme)

These reflect the **direct pilot command** and are reliable in any aircraft state — particularly cold & dark before the first engine start.

### What this means in practice

| Scenario | Before v0.13.13 | From v0.13.13 |
|---|---|---|
| Cold & dark at gate, both engines OFF | Auto-start fails, "Engines on" | Auto-start triggers cleanly ✓ |
| Engine 1 in START sequence | engines_running falsely shows 0 | engines_running = 1 ✓ |
| Both engines RUN in cruise | already worked, behavior unchanged | identical ✓ |
| Other Phenom addons (Asobo default, Carenado etc.) | as before | unchanged — detection only triggers on `fsreborn + phenom + 300` in title |

### Aircraft profile label

In the activity log after aircraft detection you'll now see: **"Aircraft Profile: FSReborn Phenom 300E"** (analogous to Fenix/PMDG/FlyByWire profiles).

### Bonus: HubHop reference doc

For future aircraft profile builds we evaluated the MobiFlight HubHop database:
- 31,435 presets / 61 vendors / 200+ aircraft
- API URL + workflow documented in `docs/dev/lvar-discovery-hubhop.md`

When a new addon with broken standard SimVars shows up, we now find the needed LVars in under 5 minutes instead of hours of searching.

### What you need to do

Nothing. Auto-updater rolls out v0.13.13. Anyone flying FSR Phenom 300E can use auto-start from cold & dark on next app start.

### QA

- 7 new unit tests for FSR Phenom 300E detection (with Michael's real aircraft title, ICAO fallback E55P, edge cases against Asobo Phenom + other FSR models)
- 414 workspace tests green
- 73/73 frontend tests green
- cargo build + frontend production build clean

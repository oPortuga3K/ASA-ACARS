## 🇩🇪 Deutsch

**Codebase-Audit + Security-Cleanup — kein hardcoded Token mehr, ~700 LOC tot raus, strict-CSP.**

### Was ist passiert

Komplettes QS-Audit über den Pilot-Client mit 3 parallelen Auditor-Agenten. Dieses Release setzt alle Pilot-Client-Findings um (VPS-/SSH-Changes folgen in einem späteren Release).

### Wichtig für VA-Owner / Pilot-Aktion nötig

**🔴 Discord-Webhook ist nicht mehr hardcoded.** Nach v0.7.13-Update kommen **KEINE** Discord-Posts mehr bis du die URL einmal in Settings einträgst:

1. Im Discord-Server: alten ASA-Webhook löschen → neuen erstellen (Channel-Settings → Integrations → Webhooks)
2. URL kopieren
3. Im ASA-ACARS: **Einstellungen** → Discord-Integration → Webhook-URL einfügen

Grund: vorher war der Token im public GitHub-Repo lesbar — jeder hätte spammen können. Jetzt liegt die URL lokal bei jedem Piloten (oder leer = keine Posts).

### Cleanup (Pilot bemerkt nichts, Code ist gesund)

- Discord-Rich-Presence-Block (170 LOC, seit v0.4.0 tot mit Versprechen "Wiring kommt in v0.4.5") → raus
- 4 verwaiste React-Components, 6 orphan Tauri-Commands, 8 Fenix-FCU-Felder, diverse cargo-warned-Funktionen → raus
- `keyring` Rust-Dep (v0.5.15-Migration, 30+ Releases her) + `schemars` Workspace-Dep + `discord-rich-presence` Dep → raus
- 4 tote i18n-Keys + ganzer Dashboard-Locale-Block (DE/EN/IT) → raus
- 8 implementierte/released Specs nach `docs/spec/historical/` archiviert

### Security-Härtung

- Tauri-Updater-Private-Key aus dem Repo-Working-Tree raus
- Tauri-Webview Content-Security-Policy von `null` auf strict (default-src 'self', whitelisted connect-src für phpVMS / MQTT-WS / SimBrief). XSS-Defense-in-Depth

### Was kommt als Nächstes (v0.7.14 oder später)

VPS-Seiten-Patches: NOPASSWD:ALL → sudoers-Whitelist, Rate-Limit auf Auth-Endpoints, `@fastify/static` Major-Bump (CVE-Fix), Caddy-Security-Headers. Brauchen ein SSH-Test-Window.

Außerdem: 4 Vulnerabilities in `rustls-webpki` via `rumqttc` (transitive Dep) — Behebung verlangt `rumqttc` Major-Bump auf `rustls 0.23+`.

---

## 🇬🇧 English

**Codebase audit + security cleanup — no more hardcoded token, ~700 LOC dead code removed, strict CSP.**

### What happened

Full QS audit of the pilot client with 3 parallel auditor agents. This release implements all pilot-client findings (VPS/SSH changes follow in a later release).

### Important for VA owners / pilot action needed

**🔴 Discord webhook is no longer hardcoded.** After updating to v0.7.13, **NO** Discord posts go out until you enter the URL once in Settings:

1. In your Discord server: delete the old ASA webhook → create a new one (Channel settings → Integrations → Webhooks)
2. Copy the URL
3. In ASA-ACARS: **Settings** → Discord Integration → paste the webhook URL

Reason: the token used to sit readable in the public GitHub repo — anyone could have spammed. Now the URL lives locally per pilot (or empty = no posts).

### Cleanup (pilots don't notice, code is healthy)

- Discord Rich Presence block (170 LOC, dead since v0.4.0 with a "wiring lands in v0.4.5" promise) → gone
- 4 orphan React components, 6 orphan Tauri commands, 8 Fenix FCU fields, several cargo-warned functions → gone
- `keyring` Rust dep (v0.5.15 migration, 30+ releases ago) + `schemars` workspace dep + `discord-rich-presence` dep → gone
- 4 dead i18n keys + entire dashboard locale block (DE/EN/IT) → gone
- 8 implemented/released specs archived to `docs/spec/historical/`

### Security hardening

- Tauri updater private key moved out of the repo working tree
- Tauri webview Content Security Policy from `null` to strict (default-src 'self', whitelisted connect-src for phpVMS / MQTT WS / SimBrief). XSS defense-in-depth

### What's next (v0.7.14 or later)

VPS-side patches: NOPASSWD:ALL → sudoers whitelist, rate limits on auth endpoints, `@fastify/static` major bump (CVE fix), Caddy security headers. Need an SSH test window.

Also: 4 vulnerabilities in `rustls-webpki` via `rumqttc` (transitive dep) — fix requires `rumqttc` major bump to `rustls 0.23+`.

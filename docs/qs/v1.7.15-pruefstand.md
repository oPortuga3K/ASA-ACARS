# v1.7.15 — Prüfstand für eine externe QS

**Stand:** 2026-09-02 · **Umfang:** `v1.7.14..HEAD`

Diese Fassung hat **einen** Anlass: den ersten echten MSFS-Flug unter v1.7.14.
Er ist der erste, bei dem die Abfrage funktioniert hat — und er zeigt genau
einen verbliebenen Fehler.

---

## Der Flug

| | |
|---|---|
| Platz | YSBK (Bankstown, Sydney), Bahn 11C |
| Aufsetzen | 2026-09-01 21:19:44 UTC |
| Fassung | 1.7.14 |
| Simulator | MSFS (`SunRise 12.2`) |
| Meldung | `auskunft_ohne_vergleich(platz=YSBK, bahnen=6, diagnose=geliefert(YSBK, bahnen=6, rollwege=444))` |

**Was zum ersten Mal funktioniert hat:** Der Simulator wurde nach dem
**richtigen** Platz gefragt und hat geantwortet — YSBK selbst, sechs Bahnen,
444 Rollwege.

Zum Vergleich dieselbe Nacht unter 1.7.13: `ohne_bahnen(rollwege=0)` bei YSCB,
LDZA, YMML, LGAV, EDDW; `auskunft_ohne_vergleich(bahnen=2)` bei EDDB und LSZH —
also entweder nichts oder die Szenerie eines fremden Platzes.

**Was trotzdem fehlte:** der Vergleich. Die Diagnose sagt `geliefert`, ein
Übernahmebericht existiert nicht.

---

## Der Befund

Die Zuordnung (`correlate_touchdown_runway`) läuft an **zwei** Stellen — dem
Sampler-Pfad und dem FSM-Pfad —, und beide liegen im **Aufsetz-Tick**. Es gibt
keine Stelle, die sie später nachholt.

Traf die Antwort nach dem Aufsetzen ein, wurde sie nie mehr angeschaut. Bei 444
Rollwegen kommt die Lieferung stückweise; ein kurzer Anflug auf einen kleinen
Platz reicht dafür nicht.

**Nachgemessen, nicht vermutet:**

* Navdaten waren da — `navdata_source = navigraph`, `navdata_cycle = 2608`
* die Bahn war sauber zugeordnet — 11C, Achsversatz 2,53 m, 322 m hinter der
  Schwelle
* die Auskunft passte zum Platz und hatte Bahnen (der ICAO-Filter greift also
  nicht)
* beim Bau des Datensatzes sagt die Diagnose `geliefert`, beim Aufsetzen gab es
  keinen Bericht → **die Antwort kam dazwischen**

---

## Was gebaut wurde

### 1. Die Zuordnung ist vom Aufsetz-Augenblick gelöst

Sie nimmt die Position jetzt aus den **gespeicherten Aufsetzwerten**
(`landing_lat` / `landing_lon` / `landing_heading_true_deg`) statt aus dem
gerade aktuellen Snapshot.

⚠ Das ist **belegt, nicht angenommen**: `stamp_touchdown_metadata` läuft an
beiden Aufrufstellen unmittelbar davor und setzt diese Felder mit **derselben
Formel**, die die Zuordnung heute benutzt:

    stats.landing_lat = td_buf_sample.map(|s| s.lat).unwrap_or(snap.lat)
    // Zuordnung: td_buf_sample.map(|s| (s.lat, ..)).unwrap_or((snap.lat, ..))

Im Aufsetzmoment ändert sich also nichts. Danach wird die Funktion erst
wiederholbar.

### 2. Nachgeholt wird im Tick, direkt nach der Ernte

Das ist die einzige Stelle, an der eine neue Auskunft am Flug ankommt. Ein
eigener Auslöser wäre eine zweite Stelle, die man vergessen kann.

Die Entscheidung ist eine reine Funktion (`bahnaufloesung_nachholen`), außerhalb
von `cfg(windows)`:

| Bedingung | warum |
|---|---|
| Aufsetzpunkt **vollständig** | lat, lon **und** Kurs. Ein Teil-Dreier hieße, mit einem geratenen Wert zu rechnen. Ein Durchstarten setzt lat/lon zurück, dort greift es also richtig **nicht** |
| Auskunft vorhanden | sonst ändert ein zweiter Lauf nichts |
| Lieferung **neuer** als die zuletzt zugeordnete | nicht „es gibt noch keinen Bericht". Die Ernte nimmt ausdrücklich eine neuere Lieferung desselben Platzes an — mit einem Boolean blieben Auskunft B und Bericht A nebeneinander liegen. Damit läuft es genau einmal je **Lieferung** statt einmal je Flug |

### 3. Prüfung und Ausführung unter EINEM Lock

`bahn_am_aufsetzpunkt_nachholen(stats, flight, simulator)` prüft und handelt in
derselben Sperre — und bekommt `snap` **gar nicht erst**. Sie *kann* nicht auf
die aktuelle Lage zurückfallen.

⚠ Der Simulator darf aus dem Schnappschuss kommen (er wechselt im Flug nicht),
die **Position** nicht.

### 4. Der Nachtrag an den Recorder wird wiederholt

Das Ereignis `touchdown_rollout_finalized` trägt die Bahnwerte in ihrem finalen
Stand nach — es adressiert bereits über `pirep_id` + `touchdown_at`. Es lief
aber **einmal**: Die Sperre war `last_published_rollout_ts`.

Sie ist jetzt `(touchdown_at, korrelierter_szenerie_stand)`. Ändert sich der
Stand — also holt das Nachholen eine Bahn nach —, geht der Nachtrag erneut
hinaus.

⚠ **Auf der Recorder-Seite ist nichts zu tun, nachgemessen:**
`updateTouchdownRolloutFinalized` patcht über `pirep_id` + `touchdown_at` und
hat **keine** Einmal-Bedingung. Ein zweiter Nachtrag überschreibt die Bahnwerte
sauber. Der irreführende Kommentar dort („der Client wiederholt das Ereignis
nicht") ist berichtigt.

### 5. Warum das reicht

`build_landing_record` rechnet die Achsen **beim Bau** aus `stats.runway_match`
und `stats.runway_nav_geometry` — es gibt keine vorher eingefrorene
Achsenbewertung. Eine nachgeholte Bahn wirkt deshalb auf Achsen, Ausfahrten
**und** Meldung gleichermaßen.

`correlate_touchdown_runway` hat keine Seiteneffekte außer Zuweisungen und
Logzeilen (nachgemessen: keine `spawn_`, keine Zähler) — mehrfacher Aufruf ist
unbedenklich.

---

## Was geprüft ist

| Prüfung | Ergebnis |
|---|---|
| `cargo test --workspace --no-fail-fast` | **1772 grün**, 0 Fehlschläge (v1.7.14: 1769) |
| Frontend | 731 grün, 1 übersprungen |
| Typen | sauber |

**Der Test, der wirklich bindet** —
`die_spaete_szenerie_erzeugt_beim_nachholen_einen_bericht`. Er fährt die
Funktion, die auch die Produktion ruft:

1. EDDP-Navdaten in den Cache, Aufsetzen an 26R, stempeln, zuordnen
2. **ohne Szenerie** → Bahn da, aber *kein* Übernahmebericht, Stand 0
3. Szenerie trifft ein (Stand 5) — so wie bei YSBK, nach dem Aufsetzen
4. `bahn_am_aufsetzpunkt_nachholen(...)` → **der Bericht entsteht**, Stand
   nachgezogen, Bahn unverändert 26R
5. zweiter Aufruf → nichts zu tun
6. neuere Lieferung (Stand 9) → **wieder** zugeordnet

⚠ Der erste Entwurf dieses Tests rief nur zweimal `correlate_touchdown_runway`
auf: keine Szenerieauskunft, kein Aufruf der Nachhol-Funktion, kein geprüfter
Bericht. Der ganze Produktionsblock hätte entfernt werden können, ohne dass er
rot wird.

Ein zweiter Test prüft die andere Richtung: **ohne** Aufsetzpunkt (Durchstarten)
passiert gar nichts — keine Bahn, kein Bericht, kein Stand.

Und weil der Tick selbst nicht testbar ist, bewacht
`der_tick_holt_die_bahnaufloesung_nach` die **Aufrufstelle**: dass sie existiert
und dass Prüfung und Aufruf nicht auf zwei Sperren verteilt sind.

### Gegenproben

| Rückbau | wird rot |
|---|---|
| wieder die aktuelle Position | `die_bahnaufloesung_ist_nachholbar…` / Kettentest |
| ohne Aufsetzpunkt nachholen | `ohne_aufsetzpunkt_holt_das_nachholen_nichts_nach` |
| Stand statt Boolean zurückbauen | `nachgeholt_wird_bei_einer_neueren_lieferung` |
| Stand nicht mitschreiben | `die_spaete_szenerie_erzeugt_beim_nachholen_einen_bericht` |

Dazu eine **Blindprobe** (nur ein Kommentar geändert), die grün bleibt — ohne
sie sagt eine Reihe roter Gegenproben nichts darüber aus, ob der Aufbau zwischen
wirksam und unwirksam unterscheidet.

---

## Die dünnen Stellen — hier zuerst hinsehen

**1. Der Windows-Block wird auf dem Mac nicht übersetzt.** Unverändert die
größte Falle: In v1.7.14 haben **vier** Änderungen an diesem Block beim ersten
Versuch nicht übersetzt, jede einzelne erst von der CI gefunden. Der Aufruf des
Nachholens liegt im Tick — dieser Teil ist **nicht** cfg-gegated, die Ernte
darunter schon.

**2. Das Nachholen läuft bis zum Flugende, nicht nur im Nachlauf — es gibt kein
Phasengate.** Bewusst so:
Bei 444 Rollwegen kann die Lieferung Minuten dauern. Die Kosten sind drei
`is_some()` je Durchlauf, und sobald ein Bericht existiert, läuft es nie wieder.
Ungeprüft ist, wie lange eine große Lieferung tatsächlich braucht — dafür fehlt
der Messwert aus einem echten Flug.

**3. Das Nachholen hängt am Stand, nicht am Inhalt.** Eine neuere Lieferung
löst eine Neuzuordnung aus, auch wenn sie dieselben Bahnen enthält. Das ist
billig (ein Vergleich), aber es ist keine Prüfung auf echte Änderung.

**4. `landing_heading_true_deg` wird beim Durchstarten nicht zurückgesetzt.**
`landing_lat`/`landing_lon` schon. Für das Nachholen ist das ohne Wirkung (die
Bedingung prüft lat/lon), aber es ist ein bestehender Riss in der
Rücksetzung — gefunden beim Messen, nicht behoben.

**5. Die Flughafenidentität existiert weiterhin dreifach** — Anfrage-ICAO,
`auskunft.icao` und Ernteziel. Offener P2 aus der zwölften Runde von v1.7.14.

---

~~**6. Der wiederholte Nachtrag ist nie über MQTT gelaufen.**~~ ✅ Erledigt in
der zweiten Runde: `bahnFinalisierung.test.ts` fährt zwei verschieden alte
Nachträge durch den echten Handler. Dabei kam heraus, dass der Nachtrag von
achtundzwanzig Feldern nur vier trug — siehe unten.

---

## Was NICHT belegt ist

* **Der PAVEMENT-Weg ist weiterhin nie gelaufen.** Bis heute wurde keine
  einzige MSFS-Bahn übernommen — auch der YSBK-Flug kam nicht so weit. Ob die
  Übernahme selbst funktioniert, ist nach wie vor offen.
* Ob das Nachholen **rechtzeitig** greift, zeigt erst der nächste MSFS-Flug. Der
  Zeitpunkt, zu dem die Lieferung eintrifft, steht in keinem Datensatz.

---

## Zweite Runde: der Korrekturvertrag (02.09.2026)

Die zweite externe QS hat vier Befunde gebracht. Drei davon hingen an derselben
Stelle: Das Nachholen korrigierte den **Client**, aber nicht die **Zeile beim
Recorder**.

### Der Kern des Befundes

`touchdown_rollout_finalized` trug von der Bahn nur die Rollspur und die
Ausrollstrecke — vier Felder. Die übrigen vierundzwanzig standen nur am
`touchdown_complete`, das rund neun Sekunden nach dem Aufsetzen rausgeht.

Kam die Szenerie danach, korrigierte der Client seinen Flugzustand, und der
Nachtrag brachte davon vier Felder mit. Bahnlänge, versetzte Schwelle,
Herkunft, die Korrekturbeträge und die ganze Aufsetzpunkt-Einordnung blieben
auf dem Stand vor der Szenerie stehen. Die Zeile sah korrigiert aus und war es
zur Hälfte nicht.

Vom späteren PIREP übernimmt der Recorder nur Punktzahl und Noten
(`updateTouchdownScores`) — die Rohwerte nicht. Diese beiden Ereignisse sind
also der **einzige** Weg, auf dem eine Korrektur die Zeile erreicht.

### Was gebaut wurde

**Eine Gruppe, eine Ableitung.** `BahnHerkunftWire` fasst alle
achtundzwanzig Felder, die eine späte Szenerie ändern kann. `bahn_herkunft(stats)`
ist die einzige Stelle, an der sie entstehen; beide Ereignisse rufen sie.
`#[serde(flatten)]` legt sie auf der Leitung flach ab — der Server sieht
keinen Unterschied zur bisherigen Form.

> Die Fehlerklasse dahinter ist alt: Zwei Stellen, die dieselben Felder je
> einzeln aufzählen, laufen auseinander. Der Compiler fängt das nicht — beide
> bauen gültige Strukturen, sie bauen nur verschiedene Werte hinein.
> Der Wächter `die_bahn_herkunft_entsteht_nur_einmal` zählt die Bau-Stellen.

**Ein Riegel gegen Überholen.** Neu auf der Leitung ist `bahn_szenerie_stand`
— die Nummer der Lieferung, aus der die Werte stammen. Eine Zeile nimmt nur
einen Nachtrag mit **größerem oder gleichem** Stand an. Gleich, weil der erste
Nachtrag denselben Stand trägt wie der Touchdown und trotzdem die
Ausrollstrecke bringen muss.

Der Riegel steht an **zwei** Stellen, weil es zwei Wege gibt: in der Datenbank
(`updateTouchdownRolloutFinalized`) und in der Warteschlange des Recorders. Die
Warteschlange nimmt Nachträge auf, deren Touchdown-Zeile noch nicht da ist —
dort hätte der ältere den neueren verdrängt, und die Datenbank hätte nur den
Überlebenden gesehen.

`updated === 0` hat seither zwei Bedeutungen: Zeile fehlt (aufheben) oder
Riegel hat abgewiesen (verwerfen). `touchdownZeileVorhanden()` trennt sie. Ein
aufgehobener überholter Nachtrag hätte später genau den Rückschritt angerichtet,
den der Riegel gerade verhindert hat.

**Der Simulator kommt aus dem Aufsetz-Moment.** Das Nachholen bekam bisher
`snap.simulator` durchgereicht, mit der Begründung, der Simulator wechsele
während eines Fluges nicht. Das stimmt für den Simulator, nicht für die
**Verbindung**. `stats.aufsetz_simulator` hält fest, womit die Zuordnung
tatsächlich gerechnet hat; die Funktion nimmt keinen Simulator mehr entgegen.

### Der End-to-End-Test

`recorder/src/__tests__/bahnFinalisierung.test.ts` fährt zwei verschieden alte
Nachträge durch den **echten** MQTT-Handler auf einer echten SQLite:

| Fall | Erwartung |
|---|---|
| Später Nachtrag mit Szenerie | alle sieben geprüften Felder korrigiert |
| Alter Nachtrag hinterher | wird verworfen, gute Zuordnung bleibt |
| Gleicher Stand zweimal | geht durch (sonst friert die Ausrollstrecke ein) |
| Client ohne Stand (< 1.7.15) | gilt wie bisher |
| Zwei Nachträge vor der Zeile | der neuere gewinnt in der Warteschlange |

Jeder Riegel hat seine eigene Gegenprobe: Ohne `HERKUNFT_FELDER` werden drei
Prüfungen rot, ohne den Datenbank-Riegel genau eine, ohne den
Warteschlangen-Riegel genau die andere. Kein Riegel deckt einen anderen zu.

### Was das für den Server heißt

Die Release-Notes sagten „Keine Serverarbeit nötig". Das war falsch. Der
Recorder muss **vor** dem Client ausgerollt werden; die Notes sind
richtiggestellt.

---

## Dritte Runde: was den Neustart nicht überlebt (02.09.2026)

Die dritte externe QS hat fünf Befunde gebracht — die maschinell grüne Kette
der zweiten Runde hatte zwei Löcher, die kein Test sehen konnte, weil kein Test
einen Neustart fährt.

### P0 — die Zahl auf der Leitung war prozesslokal

`bahn_szenerie_stand` trug die SimConnect-Anfragenummer aus dem
`Auftragsbuch`. `Auftragsbuch::default()` setzt sie auf 0 zurück. Nach einem
App-Neustart kam eine *tatsächlich neuere* Lieferung damit als Stand 1 an — und
der Riegel, der gerade eingebaut worden war, wies sie als veraltet ab. Die
Korrektur wäre für immer verloren gewesen, und **nirgends wäre etwas rot
geworden**: Der Riegel hätte genau das getan, was in ihm steht.

Es heißt jetzt `bahn_revision` und ist eine mit dem Flug **persistierte**
Revision: `korreliere_bahn` zählt sie hoch, `PersistedFlightStats` trägt sie
über den Neustart. Sie wird zwischen zwei Aufsetzern nicht zurückgesetzt — der
Riegel im Recorder hängt ohnehin an der einzelnen Zeile.

> Der Test dazu prüft ausdrücklich, dass die Leitungszahl **nicht** gleich
> `korrelierter_szenerie_stand` ist. Ohne das wäre er auch mit der alten
> Anfragenummer grün gewesen.

### P1 — das Nachholen fiel nach einem Neustart komplett aus

`aufsetz_simulator` stand nur im Speicher. Nach dem Resume war es `None`, und
`bahn_am_aufsetzpunkt_nachholen` kehrte in der ersten Zeile zurück. Die ganze
Mechanik dieser Version wäre bei jedem Flug ausgefallen, der einen Neustart
gesehen hat — ohne Meldung. Jetzt persistiert, wie `landing_simulator` daneben.

### P1 — eine Korrektur konnte nichts zurücknehmen

Vierundzwanzig Felder der Gruppe trugen `skip_serializing_if`. Der Recorder
aktualisiert nur Schlüssel, die im Ereignis vorkommen. Wurde ein Wert durch die
spätere Zuordnung ungültig — ein Korrekturbetrag, weil jetzt nichts mehr
übernommen wurde —, fehlte der Schlüssel und der alte Wert blieb stehen. Die
Zeile hätte eine Korrektur angezeigt, die es nicht mehr gibt.

Die Gruppe trägt ihre Felder jetzt **immer**, auch als `null`. Der Test zählt
die Schlüssel, statt Stichproben zu nehmen: Ein einzelnes wieder eingefügtes
`skip_serializing_if` macht genau ein Feld unkorrigierbar, und zwar lautlos.

### P1 — der Altclient umging beide Sperren

`undefined` hieß „kein Riegel". Ein verspäteter Nachtrag eines Clients vor
1.7.15 konnte damit eine Revision 7 überschreiben. Fehlend zählt jetzt als 0:
Legacy auf Legacy bleibt möglich, Legacy auf Neu nicht.

> Die genaue Gegenprobe hat hier etwas gezeigt, was die grobe verdeckt hätte:
> Der Handler normalisiert schon auf 0, der Riegel in der DB-Methode wird auf
> diesem Weg **nie** mit `undefined` gerufen. Er ist trotzdem nötig — die
> Methode ist öffentlich. Also prüft ihn ein eigener Test direkt.

### P2 — zwei Zahlen für dasselbe Drittel

`landing_touchdown_zone` wurde roh aus Bahnlänge und Aufsetzdistanz gerechnet,
`td_third` in `assess_touchdown` mit Abzug der versetzten Schwelle. Zwei
Webapp-Karten lesen die ältere. Die neue Ableitung korrigierte nur `td_third`.

Jetzt gibt es eine Rechnung: `drittel_nachfuehren` steht am **Ende** von
`korreliere_bahn` und setzt das alte Feld aus dem neuen. Ohne Bahntreffer wird
es geleert statt stehengelassen.

Die Vorrichtung des Tests ist so gewählt, dass die beiden Rechnungen
auseinandergehen — roh 1519 m auf 3600 m Bahn → Drittel **2**, korrigiert 300 m
auf 2381 m nutzbarer Länge → Drittel **1**. Bei gleicher Zahl hätte der Test
nichts gezeigt.

> ⚠ Der erste Entwurf dieses Tests wollte die Bahn**länge** über die Szenerie
> ändern. Die Messung zeigte: Sie wird gar nicht übernommen — der Test wäre
> grün gewesen und hätte nichts geprüft. Die entscheidende Achse ist die
> versetzte Schwelle.

---

## Vierte Runde: ein Claude-Prüfer über beide Diffs (02.09.2026)

Codex war am Kontingent gescheitert. Stattdessen ein Prüf-Agent mit
frischem Kontext, beide Repos, die Fehlerklassen-Liste als Auftrag — und die
Ansage, meine *Prüfungen* mitzuprüfen. 21 Minuten, 69 Werkzeugaufrufe,
**zehn Befunde**, drei davon P1. Dazu ein belastbarer „in Ordnung"-Teil:
Der Riegel weist keine echte Korrektur ab (`naechste_id` prozessweit monoton,
am Code belegt), keine Sperre über ein `await`, Feldnamen dreifach identisch.

### P1-A — mein Drittel-Umbau löschte die Aufsetzzone auf kurzen Plätzen

`classify_tdz` meldet unter 1200 m `None` — es gibt dort keine
Aufsetzzonen-*Markierung*. Ich hatte `landing_touchdown_zone` daran gehängt.
Buschplätze verloren die Anzeige lautlos; beide Drittel-Tests hielten die
Bahnlänge konstant (Klasse 2, bei mir). Jetzt: `runway_assessment::drittel`
als eigene Rechnung ohne die Grenze, `classify_tdz` nutzt sie mit,
`bahnmasse()` als die eine Stelle für Aufsetzdistanz und nutzbare Länge.

### P1-B — die Hauptstelle hielt die eigene Regel nicht

„Wer die Bahn ändert, setzt die Fahne" — `bahn_upgrade_anwenden` tat es,
`korreliere_bahn` nicht. Ohne Bahntreffer beim Aufsetzen bleibt die Spur leer,
der Streamer sendet nie, das Nachholen findet die Bahn, und niemand meldet
sie. Eine Zeile.

### P1-C — die Rollspur mischte zwei Bezugssysteme

Die Spur speichert Projektionen gegen die Bahnachse, nicht Koordinaten. Kam
die Szenerie nach dem Aufsetzen mit versetzter Schwelle, trugen die Punkte
davor den alten Nullpunkt, die danach den neuen — auf der Leitung ein einziger
Versatz. Das kam mit dem Nachholen selbst (HEAD); meine Änderung weitete es
von „einmal je Flug" auf „einmal je Lieferung" aus.

Neu projizieren geht nicht, der Puffer hält fünf Sekunden. `spur_auf_neue_achse`
unterscheidet: dieselbe Achse (beide neuen Endpunkte ≤ 5 m neben der alten,
gleiche Richtung) → exakte Längsverschiebung aller Punkte und Marken, Überrollen
gegen die neue Länge neu gerechnet; andere Achse → `spur_verwerfen`, weg und
sichtbar statt weg und falsch.

### Sieben P2 — vier davon an meinen Prüfungen

| | Befund | Abhilfe |
|---|---|---|
| D | Bau-Stellen-Wächter zählte Test-Aufrufe mit (4 von 6 Treffern in Tests) | `produktionsteil()` entfernt alle 51 `#[cfg(test)]`-Blöcke über die Klammerbilanz |
| E | kein Wächter gegen die Rückkehr der rohen Rechnung in `step_flight_at` | Wächter auf die **Zuweisung**: genau eine, in `drittel_nachfuehren` |
| F | Null-Test zählte keine neuen Felder | `assert_eq!(obj.len(), NAMEN.len())` **vor** der Namensprobe |
| G | `save_active_flight` ignorierte den Fehlschlag | gibt `bool` zurück; `error!` statt `warn!`; Doku sagt „soweit die Platte sich beschreiben lässt" statt „IMMER" |
| H | Sperre und Fahne fielen auch ohne MQTT-Versand | `let Some(handle) = … else { continue }` — beides erst nach dem Senden; Wächter auf die Reihenfolge |
| I | Einreich-Nachtrag meldete `rollout_final: true` ungeprüft | `wire(rollout_finalized && Spur fertig)` — dieselbe Bedingung wie im Streamer |
| J | `is_some()` statt „übernommen" | `bahn_geometrie_quelle == "szenerie"` |

### Was die Gegenproben diesmal gezeigt haben

**Die verschärfte Zusicherung (J) war sofort rot** — und der Grund war nicht
die Übernahme, sondern die Test-Szenerie: `gegenende = Schwelle + 0,05° Ost`.
Die 26R läuft nach Westen. `passende_szenerie_bahn` prüft die Bahn**mitte**
gegen die Nav-Achse — 3,5 km daneben, Bahn verworfen, Bericht leer,
`is_some()` trotzdem grün. Beide Szenerie-Vorrichtungen hatten das. Jetzt
`eddp_szenerie()` mit dem 08L-Ende aus der Nav-Vorrichtung.

**Eine Gegenprobe war ein Compile-Fehler.** Meine erste Mutation für H war
syntaktisch kaputt — „irgendetwas ist rot" beweist nichts (Klasse 4).
Wiederholt mit einer Mutation, die compiliert: dann rot.

**Eine Gegenprobe blieb grün, zu Recht.** Für J habe ich die Szenerie auf
Werte gesetzt, die den Navdaten gleichen — und `bahn_geometrie_quelle` sagte
weiter „szenerie". `uebernommen` wird bei jeder geometrisch passenden Bahn
geschrieben; die Szenerie ist dann die Quelle, auch wenn die Zahlen gleich
sind. Die Mutation war die falsche, nicht der Test: Sein Rot war schon die
Ost-Vorrichtung.

**Die Zeilenzahl im Reihenfolge-Wächter war ein Mittel.** Der Wächter „Sicherung
vor Versand" verlangte < 12 Zeilen Abstand — und brach, als der MQTT-Riegel
dazwischenkam. Jetzt: Sicherung liegt *nach* der Sperre, die den Zweig
öffnet. Das ist die Zusicherung.

---

## Fünfte Runde: der Prüfer prüft die Abhilfen (02./03.09.2026)

Zweite Runde des Claude-Prüfers, mit den zehn Vorbefunden und den
behaupteten Abhilfen als Auftrag. Ergebnis: **sieben geschlossen, drei
teilweise, zwölf neue** — darunter ein P0, den meine eigene Abhilfe für P2-H
eingebaut hatte.

### N1 (P0) — die Abhilfe für P2-H würgte den Tick ab

`let Some(handle) = … else { continue }` landete in der Hauptschleife des
Streamers. Ohne MQTT-Handle — und der fehlt bei jedem Keyring-, Provision-
oder Start-Fehler für die ganze Sitzung — fielen damit **Heartbeat,
Positionen und Flugdatei** für den Rest des Flugs aus. Der Wächter, den ich
für P2-H geschrieben hatte, schrieb das `continue` sogar fest.

Jetzt ein `match` ohne `continue`: Der `None`-Arm warnt einmal, der
`Some`-Arm sichert, sendet, und lässt erst dann Sperre und Fahne fallen. Die
Sicherung steht **hinter** dem Riegel — vorher schrieb sie ohne Verbindung
bei jedem Tick die Flugdatei. Der Wächter prüft Struktur: Versand, Sperre und
Fahne im selben Arm, kein `continue` im Zweig.

### N2 (P1) — das Einreich-Upgrade tauschte die Bahn ohne die Spur

`bahn_upgrade_anwenden` ersetzte `runway_match` und ließ die Spur gegen die
alte Achse stehen — genau die Mischung, gegen die P1-C gebaut wurde, jetzt
aktiv nachgeschickt. `alte_achse_von()` als gemeinsame Stelle, Rebase in
beiden Schreibern.

### N3 (P1) — das Drittel hatte keine Leitung

`landing_touchdown_zone` ist beim Recorder eine **Spalte**, die nur der
INSERT schreibt. Der Nachtrag korrigierte `td_third` im JSON; die Spalte
blieb. Jetzt: `zone_fuer_leitung()` als die eine, vertrauens-geriegelte
Ableitung an beiden Ereignissen; der Recorder schreibt die Spalte beim
Nachtrag mit (`null` leert sie, fehlend lässt sie stehen).

### Die übrigen neun

| | Befund | Abhilfe |
|---|---|---|
| N4 | `finalize_filed_pirep` löschte die Fahne vor der Handle-Prüfung — dieselbe Klasse wie P2-H, an der Stelle, die die Abhilfe neu angelegt hatte | `match (offen, handle)`; ohne Handle Warnung; Wächter auf die Arm-Struktur |
| N5 | `drittel()` ohne Längenriegel → Bahn mit `length_ft == 0` bekam Drittel 3 | `nutzbare_laenge_m > 0.0`, sonst `None` |
| N6 | Offline-Einreichen: Flug verworfen, bevor jemand senden konnte; Doku behauptete das Gegenteil | Nachtrag beim Einreihen gebaut, in `QueuedPirep` persistiert, vom Worker geschickt; Fahne persistiert |
| N7 | Nach Neustart baute der Einreich-Nachtrag `rollout_final: false` aus leerer Spur | `rollout_finalized`, `bahn_spur`, `bahn_spur_laeuft`, `bahn_spur_bis` persistiert |
| N8 | „`serde_json` scheitert an NaN" | **Widerlegt, nachgemessen:** `to_value(NaN)` liefert `null`, kein Fehler. Vergleich trotzdem auf `Debug` umgestellt |
| N9 | Recorder-Warteschlange ersetzte statt zu verschmelzen | je Feld gewinnt der neuere, nichts geht verloren; Test |
| N10 | `nur_code` schnitt an `//` in Strings; Klammerbilanz zählte Strings mit | echter Tokenizer: Strings geleert, Roh-Strings, Zeilen-/Blockkommentare, Zeichenliterale |
| N11 | vier Wächter mit Mittel statt Zusicherung (`stats.`-Nadel, `>= 2`, Zeichenabstände, toter Parameter) | Nadel ohne Bindung, `== 2`, Klammerbilanz, Parameter entfernt; Parallelbahn-Test für `ACHSE_TOLERANZ_M` |
| N12 | Doppelzeile, falscher Kommentar, Feldlisten ohne Abgleich | Zeile und Kommentar richtiggestellt; Feldlisten-Abgleich bleibt offen (drei Repos, kein gemeinsamer Test) |

### Was die Gegenproben diesmal gelehrt haben

**Ein Filter kann ein Ergebnis verschlucken.** Drei Wächter-Proben meldeten
„kein Ergebnis" bei null Compile-Fehlern. Die rohe Ausgabe zeigte: Sie
*waren* rot. Ein Skript, das Ausgabe filtert, ist selbst eine Prüfung, die
eine Gegenprobe braucht.

**Ein NaN auf einer Seite trifft die Lücke nicht.** Mein erster Träger für N8
war `tch_actual_ft` — das Upgrade rechnet es neu, das NaN stünde nur
*vorher*. Der zweite Träger (Szenerie-Bericht, vom Upgrade unberührt) stand
auf beiden Seiten — und bewies dann, dass es die Lücke gar nicht gibt. Der
Test ohne mögliches Rot ist entfernt.

**Mein Tokenizer hat meinen Wächter geleert.** Der N4-Wächter suchte den
String „keine MQTT-Verbindung" — den `nur_code` seit N10 auf `""` leert.
Rot am echten Code. Jetzt prüft er die Arm-Struktur und `tracing::warn!`,
keine String-Inhalte.

---

## Sechste Runde: die N7-Abhilfe hatte ein P1 geöffnet (03.09.2026)

Dritte Runde des Claude-Prüfers: **elf von zwölf geschlossen**, N8-Widerlegung
bestätigt — und die Abhilfe für N7 hatte ein neues P1 aufgemacht. Zum zweiten
Mal dieselbe Klasse: Eine Abhilfe öffnet einen Sendeweg, für den der Zustand
nicht vollständig ist.

### P1 — nach dem Neustart löschte der erste Nachtrag gute Werte

N7 persistierte Spur und Ausrollen. Nach dem Neustart war die Streamer-Sperre
`None`, `rollout_finalized && spur_fertig` galt — der Streamer sendete beim
ersten Tick erneut. Mit Spur, aber **ohne** die Marken (Räumpunkt, Kante,
Messfenster, Überrollen, Querversatz) und ohne `landing_touchdown_zone`: alle
`null`, und der Recorder setzt `null` ausdrücklich. Der Einreich-Weg konnte es
nicht heilen: `drittel_nachfuehren` stellte das Drittel wieder her, aber die
Änderungserkennung verglich nur die Herkunftsgruppe, in der es nicht steht.
**Spalte für immer leer, `td_third` richtig** — das N3-Symptom, von der
N3-Abhilfe selbst erzeugt.

Abhilfe, beides: der **vollständige** Bahndisziplin-Zustand persistiert (14
weitere Felder), und `letzter_nachtrag` (Aufsetzzeit, Revision) persistiert
und als Seed der Streamer-Sperre — kein blindes Senden mehr nach dem Neustart.
Die Änderungserkennung vergleicht Herkunft **plus** Drittel **plus**
Aufsetzdistanz.

### Die sieben P2

| | Befund | Abhilfe |
|---|---|---|
| Feldselektiv | `landing_float_distance_m`, `runway_geometry_trusted/_reason`, `runway`-Spalte blieben nach später Zuordnung stehen; der Riegel lief mit alter Aufsetzdistanz | `drittel_nachfuehren` führt die Aufsetzdistanz mit; `bahnvertrauen()` als eine Ableitung an Touchdown und Nachtrag; Riegel-Felder am Nachtrag. **`runway`-Spalte bewusst nicht:** sie kommt zuerst aus `approach_runway` (FMS/ATIS), der Treffer ist nur Rückfall — ein Nachtrag würde eine gewollte Quelle überschreiben |
| Feldlisten ungetestet | `HERKUNFT_FELDER` nur per `console.warn` auf Live geprüft | Listen auf Modulebene exportiert; `bahnfelderMeldung.test.ts` prüft eine vollständige v1.7.15-Nutzlast gegen alle drei Listen, Anzahl 29 gegen 29 |
| Live-Ereignis ohne Drittel | `bus.emit` trug die Spalte nicht — offene Ansicht behielt das alte Drittel | Feld am Ereignistyp und an beiden `emit`-Stellen |
| Round-Trip ungetestet | einziger neuer Deserialisierungspfad, zwei `flatten` nebeneinander | `der_nachtrag_ueberlebt_die_warteschlange`: `to_value → from_value → to_value` bytegleich |
| Bedingte Zusicherung | `if dt_q <= …` im Rebase-Test — Vorrichtung entschied, was geprüft wird (Klasse 8) | Vorrichtung festgenagelt: `assert!(dt_q <= TOLERANZ)`, dann die Überroll-Zusicherung unbedingt |
| E/A unter der MQTT-Sperre | Flugdatei unter `mqtt.lock().await` geschrieben | Präsenz prüfen (Sperre sofort frei) → sichern → sperren → senden; Wächter auf die neue Struktur |
| Worker verwirft Nachtrag | Altbestand ohne PIREP-JSON verlor den Bahn-Nachtrag mit | eigener Zweig sendet ihn trotzdem |

### Was die Gegenproben diesmal gelehrt haben

**Die Shell ist zsh.** Zweimal meldeten die Wächter-Proben „kein Ergebnis"
bei null Compile-Fehlern. Ursache: `$2` mit Leerzeichen (`--test
spur_verdrahtung`) wird in zsh nicht in Wörter zerlegt — `cargo` bekam ein
Argument. Die `--lib`-Proben hatten kein Leerzeichen und liefen. Dieselbe
Klasse wie der Ausgabefilter in Runde 5: Das Prüfskript selbst war falsch, und
es sah aus wie ein fehlendes Ergebnis statt wie ein Fehler.

**Die Wiederherstellung ist keine Rechnung.** Der Wächter „Drittel wird nur an
einer Stelle gesetzt" wurde rot, als `apply_to` das persistierte Drittel
zurücktrug. Er unterscheidet jetzt Rechnung (`drittel_nachfuehren`) und
Wiederherstellung (`= self.landing_touchdown_zone`) und verlangt genau eine von
jeder.

---

## Siebte Runde: das Prinzip statt der Stelle (03.09.2026)

Vierte Runde des Claude-Prüfers: elf Abhilfen halten — und **zum dritten Mal
in Folge hatte eine Abhilfe ein Loch geöffnet.** N13 (P1): Mein Spur-Rebase
im Einreich-Upgrade (N2) **verwarf** eine fertige Spur bei Achswechsel; dort
gibt es kein Nachfüllen wie beim Nachholen, und der Nachtrag meldete danach
`rollout_final: false` mit lauter `null` an eine längst endgültige Zeile. N14
(P1): sieben weitere Eingaben der Wire-Funktionen waren nicht persistiert;
nach Neustart kippte `bahn_geometrie_quelle` auf „navdaten".

Nach drei Wiederholungen derselben Klasse habe ich das Prinzip geändert, nicht
die Stelle:

1. **Eine fertige Spur wird nie verworfen.** Verwerfen nur, solange der Puffer
   nachfüllen kann. Sie misst, wo das Flugzeug gegen die damals gültige Bahn
   gerollt ist — eine genauere Vermessung derselben Bahn macht sie nicht
   falsch.
2. **Der Nachtrag lässt einen unvollständigen Spur-Block weg**, statt ihn als
   `null` zu senden (`bahn: Option<BahnWire>`). Der Recorder rührt dann nichts
   an. Damit kann kein Sendeweg mehr aus unvollständigem Zustand löschen —
   egal, welche Abhilfe ihn als Nächstes anlegt.
3. **Eingaben, die zu groß zum Persistieren sind** (`szenerie_auskunft`,
   `arr_ground_geojson`), führen zu **ausgelassenen** Ableitungen
   (`runway_exits` mit `skip_serializing_if`), nicht zu genullten. Die kleinen
   Eingaben (Szenerie-Bericht, `sim_kennung`, TCH, Spurweite) sind persistiert.

### Die übrigen Befunde

| | Befund | Abhilfe |
|---|---|---|
| N15 | Live-Ereignis: Recorder schickt das Drittel, Webapp nahm es nicht an | `types.ts`, `liveSocket.ts`, `store.ts` — Top-Level-Feld der Zeile, `undefined` lässt stehen |
| N16 | `landing_float_distance_m` roh neben korrigiertem Drittel | **Bewusst roh gelassen.** Erster Versuch (korrigiert) kippte den Vertrauens-Riegel bei 4000 ft Versatz ins Negative — zwei Divert-Score-Tests rot. Der Riegel liest das Feld an sechs Stellen und ist auf roh geeicht. Die Karte muss auf `td_distance_from_threshold_m` wechseln: Webapp-Folgepunkt |
| N17 | Änderungserkennung sah Breite/Belag nicht | ganzer `runway_match` im Vergleich |
| N18 | Querversatz ≤ 5 m wanderte unkorrigiert in scorewirksame Felder | affiner Querausgleich zwischen Schwelle und Ende, `max_querversatz` neu bestimmt; die Toleranz selbst bleibt ohne Korpus |
| N19 | Worker-Zweig ohne PIREP-JSON schwieg | beide Fehlerfälle gemeldet |
| N20 | Drittel-Test prüfte gegen dieselbe Ableitung; Merge-Test modellierte einen Client, den es nicht gibt | konkrete Erwartung (`Some(true)`, `Some(1)`); Merge-Test modelliert den fehlenden Block; neuer DB-Test „ohne Block bleibt alles stehen" |
| N21 | 28 statt 29; Nachfüllen gilt nur beim Nachholen; `spur_verwerfen`-Doku | richtiggestellt |

### Was die Gegenproben diesmal gelehrt haben

**Die Vorrichtung extrapoliert.** Der Querausgleich läuft affin zwischen
Schwelle und Ende; Punkte *vor* der neuen Schwelle bekommen den Fixture-Rest
mit Faktor 1 + dt_l/Spanne. Die Toleranz muss das Doppelte des gemessenen
Rests zulassen — sonst prüft der Test die Interpolation der Vorrichtung
statt den Code.

**Ein Fix kann eine Eichung brechen, die nirgends steht.** Die
N16-Korrektur der Aufsetzdistanz war fachlich richtig und trotzdem falsch:
Der Vertrauens-Riegel war stillschweigend auf den rohen Wert geeicht. Zwei
Tests, die nichts mit der Distanz zu tun haben, haben es gezeigt. Ohne den
Volllauf nach jeder Änderung wäre das durchgegangen.

---

## Achte Runde: das Prinzip hatte zwei Löcher (03.09.2026)

Fünfte Runde des Claude-Prüfers, Auftrag: erst das Prinzip prüfen, dann, ob
es selbst ein Loch geöffnet hat. Zwei hat es — und eines davon ist der
schwerste Befund der ganzen Serie.

### N22 (P0) — der Querausgleich holte die Ausfahrt in den Score

Meine Neubestimmung von `bahn_max_querversatz_m` „aus der verschobenen Spur"
(Runde 7, gegen N18) nahm die **ganze** Spur. Die läuft absichtlich durch die
Ausfahrt bis 80 m neben die Kante; das Live-Maximum wächst nur bei offenem
Messfenster. Ergebnis: Jede sauber geräumte Landung wurde nach einer späten
Zuordnung zu „Rad neben der befestigten Fläche" — im Score und in
`min_edge_clearance_m`. Genau der Fall, für den das Release gebaut ist.

Abhilfe: Der **gespeicherte** Höchstwert rückt um den Querversatz an seiner
Stelle (Fensterpunkt mit größtem Betrag); er wird nie neu bestimmt.

> **Gegenprobe, ehrlich:** Den Fensterfilter zu entfernen ließ den Test
> **grün**. Was ihn trägt, ist „nie neu bestimmen" — die Gegenprobe dafür
> (wieder aus der ganzen Spur rechnen) ist rot. Der Filter ist eine
> Verfeinerung, die diese Vorrichtung nicht sichtbar macht; ich behaupte
> nicht, ihn geprüft zu haben.

### N23 (P1) — `flatten` auf `Option` liest einen fehlenden Block als `Some(default)`

`#[serde(flatten)] Option<BahnWire>` liefert bei fehlenden Schlüsseln
`Some(BahnWire::default())` — `rollout_final: false`, lauter `None`. Auf dem
Warteschlangen-Weg holte das N13 zurück; meine Zusicherung dazu war
tautologisch (`is_none() || !rollout_final`, bei `Some(default)` immer wahr).
Abhilfe: `TouchdownRolloutFinalizedPayload::aus_json` stellt `None` her
(Erkennungsmerkmal: `rollout_final` fehlt), beide Lesestellen nehmen sie, ein
Wächter zählt; der Test prüft jetzt erst, dass `from_value` das
serde-Verhalten zeigt, dann, dass `aus_json` es korrigiert.

### N24 (P2) — die behaltene Spur stand im alten Bezug neben neuen Marken

P1-C durch die Hintertür. Abhilfe: `bahn_spur_bezug_veraltet` (persistiert)
bei Achswechsel nach dem Ausrollen; `spur_block()` als **eine** Entscheidung
für Streamer und Einreich-Nachtrag — fertig UND gültig, sonst kein Block.
Ein Wächter zählt `.wire(true)` in der Produktion auf genau eins.

### Die übrigen

| | Befund | Abhilfe |
|---|---|---|
| N25 | Karte zeigt rohe Distanz neben korrigiertem Drittel | `_LandingQualityCard`/`_ApproachStabilityCard` nehmen `td_distance_from_threshold_m` aus dem Payload, wenn vorhanden |
| N26 | `bahn_szenerie_status` kippt nach Neustart auf eine vagere Aussage | `szenerie_status_fest` bei jeder Zuordnung festgehalten und persistiert; ohne Auskunft gilt er |
| N27 | `wingspan_m` fällt nach Neustart aufs gebuchte Muster | `aufgeloestes_muster` persistiert |
| N28 | dritte `bahn_herkunft`-Aufrufstelle ungedeckt | im Rumpf von `bahn_nachtrag_bauen` geprüft — eine Dateizählung wäre an den zwei Vergleichsaufrufen gescheitert |

---

## Neunte Runde: die Spiegelung des P0 (03.09.2026)

Sechste Runde des Claude-Prüfers: N23, N27, N28 geschlossen — und die
N22-Abhilfe hatte ein **neues P1**, die Spiegelung des P0. Nach einer
seitlichen Achskorrektur kann der Argmax kippen: Ein Punkt mit umgekehrtem
Vorzeichen wird der größte. Mein „verschobener Altwert" meldete dann 0,01 m
neben einer Spur bei −4,99 m — bewiesen an **meiner eigenen Vorrichtung**,
deren Erwartung ich falsch festgeschrieben hatte. Wo N22 eine saubere Landung
als „Rad neben der Bahn" meldete, verschwieg das hier ein echtes.

Abhilfe (vom Prüfer vorgeschlagen, so umgesetzt): Höchstwert über die
**Fensterpunkte im neuen Bezug** neu bestimmen, den verschobenen Altwert als
Untergrenze behalten (die Spur ist auf 10 m ausgedünnt und hat nicht jeden
Punkt gesehen). Damit ist der Fensterfilter, den Runde 8 als „nicht prüfbar"
buchen musste, jetzt tragend: Ohne ihn liefert die Ausfahrts-Vorrichtung −60.
Beide Gegenproben rot.

### Die übrigen

| | Befund | Abhilfe |
|---|---|---|
| 2 (P2) | die einzige Schreibstelle von `szenerie_status_fest` war ungetestet | `die_spaete_szenerie…` prüft sie nach dem Nachholen; Gegenprobe (Stelle gelöscht) rot |
| 3 (P2) | Recorder hält Spur der alten Bahn neben Herkunft der neuen — der bewusste Rest von N24 | `bahn_spur_veraltet` in der Herkunftsgruppe (30 Felder), damit die Anzeige es sehen kann; die Karten werten es noch nicht aus — **benannt, nicht als geschlossen gebucht** |
| 4 (P2) | negative Aufsetzdistanz grün und „past threshold" — älter als dieses Release, durch N25 fester an die Schwellensemantik gebunden | Karten: `< 0` → rot, „vor der Landeschwelle"; Tipp „Undershoot" |
| 5 (P2) | `szenerie_status_fest` klebte nach Divert am neuen Platz | fällt mit der Auskunft, die er beschreibt — aber nur, wenn eine da war (sonst wäre N26 wieder auf); Test für beide Richtungen |
| 6 (P3) | Verbots-Wächter griff nur beim Turbofish | beide Schreibweisen über den leerraumfreien Text |

### Was die Gegenproben diesmal gelehrt haben

**Eine Gegenprobe, deren Anker nicht greift, sieht aus wie „ok".** Die
Befund-5-Probe meldete grün — weil das Mutations-Skript vor dem Schreiben
abgebrochen war und der Test auf dem unveränderten Code lief. Erst ein
umbruchtolerantes Muster machte sie rot. Die Klasse 7 hat damit eine dritte
Ausprägung: nicht nur der Filter und die Shell, auch der Anker.

**Ein Test kann eine falsche Erwartung festschreiben.** Die Quer-Vorrichtung
war richtig gebaut; ihre Zusicherung (`3,0 − dq`) war meine Annahme, nicht
die Rechnung. Der Prüfer hat sie nachgerechnet. Vorrichtung und Erwartung
getrennt prüfen.

---

## Zehnte Runde: „Bereit" — und was das heißt (03.09.2026)

Siebte Runde des Claude-Prüfers, erstmals mit dem Urteil: **bereit für
Windows-CI und den realen MSFS-Flug.** Kein Befund ist mehr ein Fehler im
ausgelieferten Verhalten. Befund 1–6 der sechsten Runde: geschlossen, mit
Belegen; Feldzahl 30 maschinell auf allen drei Seiten gleich.

Was blieb, sind Prüflücken — und die habe ich geschlossen, ohne
Produktionscode anzufassen:

| | Lücke | Abhilfe | Gegenprobe |
|---|---|---|---|
| N1 (P2) | Der Neustart-Zweig des Status-Tests lief nie durch den Generationswechsel (`generation: 0` → kein Zweig) — der rettende Riegel war ungedeckt | zweiter Aufruf mit `generation: 1`, Zusicherung, dass der Zweig lief | Riegel entfernt → rot |
| N2 (P2) | Der Interpolationsterm von `quer_bei` war von keinem Fixture berührt — in allen acht galt `dt_q ≈ de_q` (Klasse 2) | gedrehte Achse: Schwelle +4 m, Ende −4 m; Erwartung aus den **gemessenen** Achsen hergeleitet, plus die Zusicherung, dass es NICHT die Konstante ist | `quer_bei` durch `dt_q` ersetzt → rot |
| N3 (P3) | Verbots-Wächter nur für zwei Schreibweisen | acht Muster (Turbofish mit/ohne Pfad, `Option<…>`, `from_str`) | — (Textmuster) |
| N4 (P3) | Feldlisten dreifach gezählt, nie gegeneinander | Kommentar 29→30 richtiggestellt; **bleibt offen:** drei Repos, kein gemeinsamer Test möglich — ein 31. Feld fällt in Rust auf (Anzahl-Test), im Recorder nur als `console.warn` auf Live | — |

**Keine achte Prüfrunde** für diese Änderung: Sie enthält nur Tests, einen
Kommentar und diese Seite. Die Gegenproben sind gefahren; Volllauf unten.

### Was jetzt noch offen ist — nicht aus dem Diff

* **Windows-CI** hat diesen Code nie gesehen. Der MSFS-Adapter compiliert nur
  dort; v1.7.14 hatte vier Fehler, die auf dem Mac unsichtbar waren.
* **Recorder-CI** ebenso.
* **Der reale MSFS-Flug.** Der PAVEMENT-Weg ist bis heute nie gelaufen. Ob die
  Szenerie tatsächlich nach dem Ausrollen eintrifft (der YSBK-Fall) und ob das
  Nachholen dann greift, weiß niemand aus einer Messung.
* **Bewusst nicht geschlossen:** die `runway`-Spalte (Quelle ist
  `approach_runway`); `ACHSE_TOLERANZ_M = 5` ist ohne Korpus gewählt; der Restfehler in
  `alt_verschoben` bei gedrehter Achse ist durch `|de_q − dt_q| ≤ 10 m`
  begrenzt und wird von der dichten Spur meist gedämpft.

### Bilanz der zehn Runden

Drei externe Runden (Thomas, v1.7.15 Runde 1–3), sieben mit dem Prüfagenten.
**In fünf Runden hintereinander hat eine Abhilfe ein neues Loch geöffnet** —
dreimal dieselbe Klasse (Sendeweg aus unvollständigem Zustand), dann das
Prinzip, dann zwei Löcher im Prinzip, dann die Spiegelung eines P0. Was
geholfen hat, war nicht mehr Sorgfalt an der Stelle, sondern: das Prinzip
ändern, wenn die Klasse sich wiederholt; jede Abhilfe in der nächsten Runde
wie einen Ursprungsbefund prüfen lassen; und jede Gegenprobe darauf prüfen,
ob sie die Zusicherung trifft oder nur eine Verfeinerung.

---

## Elfte Runde: Codex über beide Repos (03.09.2026)

Nach dem „Bereit" des Prüfagenten lief `/codex:review` (OpenAI Codex, Plugin
1.0.6) über den ungecommitteten Baum beider Repos — ein anderer Prüfer, ein
anderer Blick. Ergebnis: **1 P1 + 5 P2**, alle sechs bestätigt. Keiner
davon hätte den Prüfagenten treffen müssen, denn keiner betraf das Prinzip
der Runden 5–9; alle sechs sind Sendeweg- und Anzeige-Ränder.

### P1 — Einreichen ohne MQTT verlor den Nachtrag doch

Runde 5 hatte den Einreich-Trichter so gebaut, dass die Fahne erst mit
Handle fällt und ohne Handle gemeldet wird. Gemeldet — und dann? Der Flug
ist nach dem Einreichen gelöscht, die PIREP-Warteschlange entfernt ihre
Datei vor dem Senden. Es gab keinen Ort mehr, an dem der Nachtrag hätte
liegen können. Der Wächter von Runde 5 prüfte „meldet", nicht „bewahrt":
wieder ein Wächter, der ein Mittel festschreibt statt der Zusicherung.

**Abhilfe:** eine eigene Ablage `nachtrag_queue` (`bahn_nachtraege/`,
Datei je `pirep_id` + Aufsetzzeit, gelesen über `aus_json`). Drei Stellen
schreiben hinein — der `(true, false)`-Arm des Trichters, der gequeute
Nachtrag ohne Handle, der Worker-Altbestand ohne Handle —, und der
Warteschlangen-Worker leert sie bei jedem Tick, sobald ein Handle da ist,
**vor** dem Client-Riegel (ein Nachtrag braucht nur MQTT, keinen
phpVMS-Login). Die Fahne fällt im Ablage-Arm erst im Ok-Zweig des
Einreihens. Idempotent über `bahn_revision` am Recorder.

### Die fünf P2

* **Nachtrag und Aufsetz-Ereignis adressierten verschiedene Zeiten.**
  `touchdown_complete` nimmt `sampler_touchdown_at.or(landing_at)`, der
  Nachtrag nur `landing_at`. Bei Busch-/VFR-Landungen (Abtaster sofort,
  `Arrived` Minuten später oder nie) traf der Nachtrag keine Zeile oder
  unterblieb. Jetzt eine Funktion `aufsetzzeit(stats)` für alle drei
  Stellen (Touchdown, Streamer-Nachtrag, Einreich-Nachtrag).
* **Im Fehlerzweig von `flight_end` fiel die Fahne beim Bauen.** Schlug
  `pirep_queue::enqueue` fehl, stellte `restore_flight_for_retry` den Flug
  ohne offene Korrektur wieder her. Jetzt fällt sie erst nach dem
  erfolgreichen Einreihen.
* **Die Karten werteten `bahn_spur_veraltet` nicht aus.** War oben unter
  „bewusst nicht geschlossen" — Codex hat es als P2 eingestuft, zu Recht:
  Eine Querachse zur falschen Bahn sieht wie ein Messwert aus. Der Mapper
  lässt bei `true` die acht Spurfelder leer (Proben, Freigabepunkt,
  Messgrenze, Messende, Freigabetempo, Seite, größter Versatz, kleinster
  Randabstand); Geometrie (Breite, Spurweite, Spannweite) bleibt.
* **`runway_geometry_trusted`/`_reason` erreichten die Karten nie.** Die
  Typen führten beide seit v0.7.6 als Top-Level-Felder, gesetzt hat sie der
  DTO nie — `trusted ?? true` galt immer. Jetzt setzen beide DTO-Bauer sie
  aus der Payload (`recorder/src/vertrauen.ts`), und der Store zieht sie
  beim Nachtrag aus dem Block hoch (fehlender Schlüssel lässt stehen,
  explizites `null` löscht — wie am Recorder).
* **`td_distance_from_threshold_m: null` fiel auf die rohe Distanz
  zurück.** Ein explizites `null` ist eine Aussage (Bahnbezug gefallen),
  kein Loch; der Rückfall zeigte eine Zahl mit anderem Nullpunkt unter
  derselben Beschriftung. Jetzt eine gemeinsame Funktion
  `distanzHinterSchwelle.ts` für beide Karten (das Doppel war der zweite
  Teil des Befunds): fehlender Schlüssel → roh, `null` → `null`.

### Gegenproben

Neun Mutationen, jede kompiliert, jede macht genau den einen gemeinten Test
rot — mit einer Ausnahme, die gewollt ist: Die Ablage über `from_value`
statt `aus_json` macht **zwei** rot, den Verhaltenstest (Block kommt als
`Some(default)` zurück) und den Wächter (Zählstelle 3 statt 2). Beide
sagen dasselbe auf zwei Ebenen; das ist die Blindprobe, nicht Rauschen.

| Mutation | roter Test |
|---|---|
| Nachtrag nimmt wieder nur `landing_at` | `der_nachtrag_nimmt_die_abtaster_aufsetzzeit` |
| Ablage liest über `from_value` | `die_ablage_liest_zurueck_was_sie_schrieb` + Wächter `aus_json` |
| Fahne im Ablage-Arm vor `enqueue` | `ohne_handle_geht_der_nachtrag_in_die_ablage` |
| `flight_end`: Fahne wieder beim Bauen | `im_fehlerzweig_faellt_die_fahne_erst_nach_dem_einreihen` |
| Worker: Ablage nach dem Client-Riegel | `der_worker_leert_die_ablage_vor_dem_client_riegel` |
| Mapper: `lateral_samples` ungeriegelt | `runwayDiagramMapper` „veraltete Spur: keine Proben" |
| Distanz: `null` fällt wieder zurück | `vertrauenHochziehen` „explizites null" |
| Store: Hochziehen entfernt | `vertrauenHochziehen` „zieht das Vertrauen nicht hoch" |
| DTO: eine Stelle vergessen | `vertrauenDto` „beide Touchdown-DTOs" |

Volllauf danach: Workspace 50 Ziele, 1823 Tests; Recorder Exit 0;
Webapp Exit 0; `tsc` beide; `cargo fmt --check`; `git diff --check` beide.

### Was diese Runde gelehrt hat

* **Ein zweiter Prüfer mit anderem Werkzeug findet die andere Klasse.**
  Sieben Runden Prüfagent hatten das Prinzip (Sendeweg aus unvollständigem
  Zustand) sauber gemacht; Codex fand die Ränder — Verlustpfade nach dem
  Senden, Zeitstempel-Kaskaden, tote Typfelder. Das „Bereit" eines
  Prüfers ist der Stand seines Blickwinkels.
* **„Meldet" ist keine Zusicherung.** Der Runde-5-Wächter forderte
  `tracing::warn!` im Arm ohne Handle. Eine Warnung bewahrt nichts. Der
  neue Wächter fordert die Ablage und die Reihenfolge Ablage → Fahne.
* **Ein Typfeld, das nie gesetzt wird, ist eine Behauptung.** Der DTO-Typ
  versprach `runway_geometry_trusted` seit Monaten; die Karten haben sich
  darauf verlassen. Wer einen Typ erweitert, muss die Setzstelle
  mitliefern — oder das Feld lassen.

---

## Zwölfte Runde: Codex adversarial — die Abhilfe der elften hatte zwei Löcher (03.09.2026)

Nach dem Commit der Runde 11 lief `/codex:adversarial-review` über genau
diese Fix-Commits (`--base HEAD~1`), mit dem Auftrag, die Abhilfen selbst
anzugreifen. Ergebnis: **2 High im Client, 1 High + 2 Medium auf der
Live-Seite.** Beide Client-Befunde trafen die Ablage aus Runde 11 — die
Klasse „Abhilfe öffnet ein neues Loch", zum sechsten Mal in diesem Prüfstand.

### Client, High 1 — ein Handle ist kein Zustellnachweis

Die Ablage aus Runde 11 griff nur, wenn KEIN Handle da war. Bei liegender
Leitung bleibt der Handle aber für den Reconnect bestehen; der Sendeweg war
fire-and-forget (Einreihen 30 s, Publish 20 s, beides konnte scheitern, und
niemand hörte davon), und der Aufrufer löschte Fahne und Datei sofort. Der
Drain der Ablage löschte die Datei ebenfalls direkt nach dem Aufruf. Der als
behoben dokumentierte Verlust bestand fort — nur eine Netzausfallklasse
weiter.

**Abhilfe: Ablage zuerst, Datei fällt erst mit der Zustellmeldung.**

* `Handle::touchdown_rollout_finalized` gibt es nicht mehr. An seine Stelle
  tritt `NachtragSender::senden`, klonbar (kein Senden unter dem
  `state.mqtt`-Schloss), mit Zustellmeldung: `true` nur, wenn die Leitung
  stand (`link_rx`) UND der Publish angenommen wurde. Auftragskanal voll,
  Kanal geschlossen, Leitung liegt, Frist abgelaufen — alles `false`.
* `nachtrag_queue::senden` ist DER Sendeweg für alle vier Aufrufer
  (Streamer, Einreichen, gequeuter Nachtrag, Worker-Altbestand): Datei
  schreiben → senden → auf die Meldung warten (75 s) → bei `true` die Datei
  entfernen, und zwar nur, wenn sie noch dieselbe Revision trägt (der
  Streamer kann inzwischen eine neuere in dieselbe Datei geschrieben haben).
  `entfernen_wenn_revision` ist die einzige Löschstelle.
* Der Worker leert die Ablage bei jedem Tick, eins nach dem anderen, und
  bricht beim ersten `false` ab (liegt die Leitung, liegt sie für alle).
  Der Recorder riegelt über `bahn_revision`; ein Doppel schadet nicht.

**Was das nicht ist:** ein Broker-PUBACK. rumqttc meldet keine
Paketkennung an den Aufrufer; „angenommen" heißt: im Vorrat, bei stehender
Leitung gesendet, QoS 1 mit Wiederholung nach Reconnect durch rumqttc. Die
verbleibende Lücke (Leitung fällt zwischen Annahme und Sendung, Client
stirbt vor dem Reconnect) schließt die Ablage: Was nicht als `true` gehört
wurde, liegt noch da. Das steht so auch in den Notes.

### Client, High 2 — die Ablage kannte keinen Mandanten

Ein Ordner für alle Konten; der Worker schickte alles über den gerade
angemeldeten Handle. Zwei Piloten auf einem Rechner: der fremde Nachtrag
ging auf das fremde Topic und war danach gelöscht.

**Abhilfe:** `bahn_nachtraege/<va>/<pilot>/`, die Identität steht zusätzlich
IN der Datei, `list_in` liefert nur, was zu Pfad UND Inhalt passt (eine
Datei im Ordner von 42, deren Inhalt 43 gehört, bleibt liegen). Mit Handle
kommt die Identität aus dem Handle; ohne Handle aus dem Schlüsselbund
(dieselbe, mit der die nächste Verbindung aufgebaut wird) — fehlt sie, ist
das ein Fehler, nicht „irgendwo ablegen".

### Live, High — der WebSocket schickte die rohe Zeile

`bus.emit({ kind: "touchdown", payload: row })` — `row` war die
`TouchdownRow` aus `insertTouchdown`: `payload_json` als Text, kein
geparstes `payload`, keine Top-Level-Felder. Der Store gleicht Nachträge
über `payload.pirep_id` ab; für eine in der offenen Sitzung frisch
eingetroffene Landung war das leer. Mein Test aus Runde 11 begann mit einem
fertigen REST-DTO und sah den Weg nie.

**Abhilfe:** `recorder/src/touchdownDto.ts` — EIN Mapper
(`touchdownRowMitFlugzeugToDto`) für die Live-Liste und den WebSocket;
`safeJson`/`frozenVerdict` ziehen dorthin um. Der Handler baut das Ereignis
über `touchdownEventPayload` (mit Flugzeug-Join, sonst gleiche Form ohne
Muster/Kennung). Der Test geht jetzt durch den echten Weg: `handle()` →
`bus` → Ereignisform. (`server.ts` behält daneben ein schlankes
`touchdownRowToDto` für Statistik- und Pilotenendpunkte.)

### Live, Medium — eine Text-Revision vergiftete den Riegel

`bahn_revision: "7"` ging mit Vergleichswert 0 durch, landete als Text im
JSON, und SQLite liest Text nie als `<=` einer Zahl: alle späteren gültigen
Nachträge still abgewiesen. **Abhilfe:** `bahnRevisionGueltig` (endliche,
nichtnegative Ganzzahl), sonst zählt das Feld als fehlend UND wird aus dem
Patch entfernt; der SQL-Riegel castet zusätzlich — eine bereits vergiftete
Zeile heilt damit von selbst (Test 4).

### Live, Medium — die Anflugkarte beschuldigte auf verworfener Geometrie

Undershoot-Warnung, Zone-3-Tipp, Duck-under-Muster: alle aus Feldern, die
bei `runway_geometry_trusted: false` nichts wert sind. Die
Landing-Quality-Karte blendete sie aus, die Anflugkarte nicht.
**Abhilfe:** derselbe Riegel (`?? true` für Zeilen vor v0.7.6) vor
Aufsetzdistanz, Drittel, TCH- und Aim-Klassen. Render-Test mit
`trusted: true / undefined / false`.

### Gegenproben

Zehn Mutationen. Zwei davon machen mehr als einen Test rot, beide gewollt:

| Mutation | roter Test |
|---|---|
| `senden` ohne Ablage | `jeder_nachtrag_geht_ueber_die_ablage` |
| Streamer umgeht die Ablage (`nachtrag_sender().senden` direkt) | derselbe + die zwei Streamer-Wächter aus Runde 3/5 (sie suchen die letzte Sendestelle) |
| Publisher ohne Leitungsprüfung | `der_publisher_meldet_nur_bei_stehender_leitung` |
| `lesen` ohne Identitätsprüfung | `die_ablage_trennt_die_mandanten` + `die_ablage_liest_zurueck` (Pilot 43 liest 42) |
| `entfernen_wenn_revision` ohne Vergleich | `die_ablage_bleibt_bei_neuerer_revision_liegen` |
| Sender meldet `true` bei totem Kanal | `ohne_publisher_kommt_false` |
| Handler schickt die rohe Zeile | `wsTouchdownDto` „der Handler schickt die rohe Zeile" |
| Revision: jede Zahl gilt | `wsTouchdownDto` „bahnRevisionGueltig(-1)" |
| Riegel ohne CAST | `wsTouchdownDto` „der CAST fehlt" |
| Anflugkarte ohne Riegel | `approachVertrauen` „ohne Vertrauen beschuldigt sie nicht" |

Die erste Fassung der WebSocket-Probe blieb bei der Mutation grün: Sie rief
den Helfer `touchdownEventPayload` direkt auf, nicht die Emit-Stelle. Das ist
Fehlerklasse 4 aus v1.7.14 („Probe trifft die Verfeinerung, nicht die
Zusicherung") — die Probe geht jetzt über `handle()` und den Bus.

Volllauf danach: Workspace 50 Ziele, 1829 Tests; Recorder Exit 0; Webapp
Exit 0; `tsc` beide; `cargo fmt --check`; `git diff --check` beide.

### Was diese Runde gelehrt hat

* **„Handle vorhanden" ist ein Zustandsflag, keine Zustellung.** Jede
  Stelle, die daraus „gesendet" ableitete, war falsch — auch die aus Runde
  11. Die Regel: Was dauerhaft sein muss, liegt VOR dem Senden auf der
  Platte und fällt erst mit einer Meldung, die der Transport selbst gibt.
* **Eine Ablage ohne Eigentümer ist ein Leck.** Alles, was auf der Platte
  auf eine spätere Verbindung wartet, braucht die Identität, für die es
  bestimmt ist — im Pfad UND im Inhalt.
* **Der adversariale Lauf nach dem „Bereit" lohnt.** Runde 11 hatte
  denselben Prüfer (Codex) im normalen Modus; erst der Auftrag, die
  Abhilfen anzugreifen, fand die Löcher darin.

---

## Dreizehnte Runde: Codex adversarial über Runde 12 — Zustellung heißt PUBACK (03.09.2026)

Derselbe Lauf, diesmal über die Runde-12-Commits. Ergebnis: **3 High + 1
Medium im Client, 1 High + 1 Medium auf der Live-Seite.** Alle Client-Highs
trafen die Runde-12-Ablage — zum siebten Mal die Klasse „Abhilfe öffnet ein
neues Loch". Thomas' Entscheidung: High 1 mit echter PUBACK-Zuordnung, nicht
als benannte Restlücke.

### Client, High 1 — „im Kanal angenommen" war keine Zustellung

Runde 12 hatte es offen benannt; Codex ließ es nicht gelten: `publish()`
meldet nur die Übernahme in rumqttcs Auftragskanal, und ein `true` daraus
ließ die Ablage-Datei fallen, bevor der Broker das Paket je gesehen hatte.

**Abhilfe: das Zustellbuch.** rumqttc 0.25 gibt dem Aufrufer keine
Paketkennung, aber der Eventloop macht sichtbar, was zählt:
`Outgoing::Publish(pkid)` in genau der Reihenfolge, in der Aufträge in den
Kanal kamen, und `Incoming::PubAck(pkid)`.

* JEDER Publish — Ereignisse, Positionen (QoS 0), Status, Nachträge — geht
  durch `publish_registriert`: unter einem `std::sync::Mutex` `try_publish`
  und, nur bei Annahme, ein Eintrag hinten ins Buch (`Some(meldung)` für
  einen Nachtrag, `None` sonst). Kein `.await` im Schloss, darum
  `try_publish` mit eigener Warteschleife bis zur alten 20-s-Frist statt
  `publish().await`. Zwei Aufgaben können sich so nicht überholen.
* Drive-Loop: `Outgoing::Publish(pkid)` nimmt den vordersten Eintrag —
  pkid 0 (QoS 0) verbraucht ihn, sonst wartet er unter der pkid;
  `PubAck(pkid)` meldet `true`.
* Leitungsabriss: Wir fahren `clean_session`; rumqttc leert beim nächsten
  CONNACK alles Anstehende, auch die Aufträge, die beim Abriss im Kanal
  lagen. `zustellbuch_leitung_weg` meldet allem Unterwegs `false` und nimmt
  genau so viele frische Einträge vom Buchanfang, wie `eventloop.pending`
  Publishes mit pkid 0 trägt — sonst liefe das Buch ab dem nächsten Publish
  um eins versetzt. Danach `pending.clear()`, damit ein zweiter Abriss vor
  dem CONNACK dieselben Aufträge nicht nochmal zählt. Beide Abriss-Stellen
  (Watchdog, Poll-Fehler) rufen es.
* Kollision (`Outgoing::AwaitAck`): rumqttc hält neue Aufträge an, bis das
  alte Paket bestätigt ist; das kollidierte geht danach als nächstes raus.
  Kommt sein `Outgoing::Publish` vor dem `PubAck` des alten durch die
  Ereignisschlange, stehen unter der pkid zwei Einträge, bestätigt in
  Reihenfolge.

Was rumqttc dafür hergibt, ist an der Quelle nachgelesen (0.25.1:
`state.rs` `outgoing_publish`/`clean`, `eventloop.rs` `poll`/`select`,
`pending` ist ein öffentliches Feld). Vier reine Tests am `Zustellbuch`
(Reihenfolge mit QoS 0, Abriss mit Verschluckten, Kollision, Outgoing ohne
Eintrag) und ein Wächter über die vier Stellen.

### Client, High 2 — ohne Ablage wurde trotzdem gesendet

`senden` setzte `pfad = None` und schickte weiter; der Streamer setzte
Sperre und Fahne. Jetzt: `senden` gibt `Err` zurück, BEVOR gesendet wird;
der Streamer wertet das aus und lässt Sperre und Fahne stehen (nächster
Tick). Beim Einreichen und im Worker-Altbestand ist der Flug bereits weg —
dort wird der Fehler laut gemeldet; ein zweites Fahrzeug gibt es nicht.

### Client, High 3 — „unlesbar" galt als löschbar

`revision_in_datei` gab bei jedem Lesefehler `None`, der Catch-all löschte.
Gleichzeitig schrieb `enqueue_in` mit `fs::write` direkt in die Zieldatei.
Jetzt: gelöscht wird nur bei `Some(rev) == zugestellt`; `None` bleibt
liegen und wird gemeldet; geschrieben wird über Temp-Datei + `rename`.

### Client, Medium — Migration der Runde-11-Dateien: gegenstandslos

Der Runde-11-Stand wurde nie gebaut oder ausgeliefert (kein Tag zwischen
a8d6bb2 und dfc4766; Releases sind tag-getrieben). Auf keinem
Pilotenrechner liegt eine Datei im alten Format. Festgehalten, nicht
migriert.

### Live, High — Runde 12 hatte den falschen der beiden Wege gewählt

Codex hatte „ablehnen oder Feld ignorieren" angeboten; ich nahm
„ignorieren". Damit standen die Daten der logisch siebten Revision ohne
Ordnungsmarker in der Zeile, und eine verspätete gültige 1 überschrieb sie.
Jetzt fail-closed: Feld vorhanden, aber keine Ganzzahl → der GANZE Nachtrag
wird abgelehnt und gemeldet. Nur ein wirklich fehlendes Feld gilt als
Altclient-Revision 0. Der Runde-12-Test hatte das Gegenteil festgeschrieben;
er ist umgedreht.

### Live, Medium — `CAST AS INTEGER` stufte 7.9 zu 7

Im Bestand gibt es das nicht (der Client schreibt `u32`, kein
v1.7.15-Client hat den Live-Recorder je erreicht), die Abhilfe ist trotzdem
billig: `CAST AS REAL`. Tests mit 7.9, `"1e3"`, `"7"` und `"abc"`.

### Gegenproben

| Mutation | roter Test |
|---|---|
| `senden` schickt auch ohne Ablage | `ohne_ablage_wird_nicht_gesendet_und_die_sperre_bleibt` |
| Streamer sperrt vor dem Ergebnis | derselbe (Sperre steht zweimal) |
| unlesbar wird gelöscht | `die_ablage_bleibt_bei_neuerer_revision_liegen` + derselbe Wächter |
| Drive-Loop trägt `Outgoing` nicht ein | `die_zustellung_ist_das_puback` |
| abgelehnter Publish steht im Buch | derselbe |
| Abriss nimmt keine verschluckten Aufträge | `leitung_weg_meldet_false_und_haelt_das_buch_in_reihe` |
| Revision: nur Feld verwerfen statt ablehnen | `wsTouchdownDto` „muss abgelehnt werden" |
| Riegel mit `INTEGER`-Cast | `wsTouchdownDto` „der CAST stuft herab" |

### Was diese Runde gelehrt hat

* **„Ehrlich benannt" ist keine Abhilfe.** Runde 12 hatte die
  PUBACK-Lücke sauber dokumentiert; ein Prüfer, der die Abhilfe angreifen
  soll, liest das als „bekannt und offen". Eine Restlücke bleibt nur, wenn
  der Auftraggeber sie so entscheidet — hier hat er das Gegenteil
  entschieden.
* **Was die Bibliothek nicht hergibt, gibt oft der Eventloop her.** Keine
  Paketkennung am Aufrufer — aber eine geordnete Ereignisfolge. Die
  Zuordnung braucht dann eine einzige Regel (alle Publishes durch eine
  Stelle) und einen Wächter darauf.
* **Bei zwei angebotenen Wegen den strengeren nehmen.** „Feld ignorieren"
  klang harmlos; es ließ Daten ohne Ordnungsmarker zu. Fail-closed hätte in
  Runde 12 schon gestanden.

---

## Vierzehnte Runde: die drei Ränder der PUBACK-Abhilfe (03.09.2026)

Codex adversarial über die Runde-13-Commits: **3 High im Client, 1 Medium
Live.** Alle drei Client-Befunde sitzen an den Rändern der Zustellbuch-
Abhilfe, keiner in ihrer Mitte.

### High 1 — der Einreichpfad hat keinen nächsten Tick

Runde 13 gab `senden` ein `Err` und ließ den Streamer die Sperre halten.
Beim Einreichen gibt es aber keinen nächsten Tick: Der Flug ist weg, und
im `(true, true)`-Arm fiel die Fahne weiterhin beim Bauen. Scheiterte die
Ablage danach, war die Korrektur verloren — genau auf dem Fehlerpfad, den
Runde 13 ausdrücklich behandelt hatte.

**Abhilfe, zweistufig.** Im Trichter legt der `(true, true)`-Arm den
Nachtrag ab, BEVOR die Fahne fällt (`senden` schreibt dieselbe Datei
danach nochmal, atomar, gleicher Inhalt). Zusätzlich legt `flight_end` den
offenen Nachtrag ab, BEVOR es den Flugbericht einreicht: Stirbt der Client
zwischen erfolgreichem Einreichen und Trichter, liegt die Korrektur schon
da und der Worker schickt sie beim nächsten Start. Schlägt diese Ablage
fehl, wird trotzdem eingereicht — der Flugbericht ist wichtiger —, aber
laut.

### High 2 — Lesen und Löschen waren nicht atomar

`entfernen_wenn_revision` las Revision N und löschte danach denselben
Pfad. Dazwischen konnte der Streamer N+1 auf genau diesen Pfad umbenennen;
das PUBACK für N löschte dann N+1. Mein Test war sequentiell und sah das
Fenster nicht.

**Abhilfe: die Revision gehört in den Dateinamen.**
`<pirep>-<td>-r000003.json`. Ein PUBACK löscht die Datei, die es meint;
eine neuere Revision ist eine andere Datei. `entfernen` liest nicht mehr,
es löscht — das Fenster existiert nicht mehr, statt bewacht zu werden. Die
Nullfüllung sortiert den Ordner nach Revision, `list_in` sortiert danach,
der Worker schickt die älteste zuerst, und der Recorder weist Überholtes
über seinen Riegel ab. Eine unlesbare Datei liefert `list_in` gar nicht
erst aus, sie wird also nie gesendet und nie entfernt.

### High 3 — ein Schreibfehler ließ einen Geist im Zustellbuch

rumqttc legt `Outgoing::Publish(pkid)` in `state.events`, BEVOR es
schreibt (0.25.1, `eventloop.rs` `select`: `handle_outgoing_packet` puffert
das Ereignis, dann `network.write`). Scheitert Schreiben oder Flush, läuft
`clean()`, aber das Ereignis bleibt gepuffert und käme nach dem Reconnect
als Erstes zurück. Meine Bereinigung zählte nur `pending` mit pkid 0 —
der bereits zugeteilte QoS-1-Publish blieb als frischer Eintrag stehen,
das zurückkehrende `Outgoing` machte ihn zum Geist unter seiner pkid, und
ein späteres PUBACK derselben Kennung hätte ihn statt des richtigen
Eintrags bedient.

**Abhilfe:** `zustellbuch_leitung_weg` leert zuerst `state.events` und
trägt jedes gepufferte `Outgoing::Publish` ein (ein gepuffertes `PubAck`
zählt als echte Zustellung), dann erst wird alles Unterwegs abgeschrieben.
Beide Felder sind in rumqttc öffentlich.

### Live, Medium — `1e100` ist eine Ganzzahl

`Number.isInteger(1e100)` ist `true`. Gespeichert sperrte das über den
REAL-Vergleich jede spätere Korrektur. **Abhilfe:** `Number.isSafeInteger`
plus Obergrenze `BAHN_REVISION_MAX = 1_000_000` (der Client zählt je
Landung ab 1 in Einerschritten). Im SQL-Riegel zählt jeder gespeicherte
Wert außerhalb von [0, 1e6] als 0 — eine bereits vergiftete Zeile heilt
mit dem nächsten gültigen Nachtrag von selbst.

### Gegenproben

| Mutation | roter Test |
|---|---|
| Fahne fällt vor der Ablage | `beim_einreichen_liegt_die_ablage_vor_fahne_und_einreichen` |
| keine Ablage vor dem Einreichen | derselbe |
| Revision nicht im Dateinamen | `jede_revision_ist_ihre_eigene_datei` + Wächter (nach dem Schärfen) |
| Ereignisschlange nicht geleert | `gepufferte_ereignisse_landen_vor_der_bereinigung_im_buch` + `die_bereinigung_leert_die_ereignisschlange` |
| Ereignisse erst nach dem Abschreiben | dieselben beiden |
| `Number.isInteger` statt `isSafeInteger` | `wsTouchdownDto` „bahnRevisionGueltig(1000001)" |
| Riegel ohne Bereichsprüfung | `wsTouchdownDto` „1e100 sperrt eine echte 5" |

**Ein Wächter musste geschärft werden.** Die Mutation „Revision nicht im
Dateinamen" ließ `let _ = revision;` stehen — der Wächter suchte das Wort
„revision" und blieb grün. Die Nadel ist jetzt das Format (`-r{`), gelesen
aus der Rohquelle, weil `nur_code` Zeichenketten leert. Wieder
Fehlerklasse 1 aus v1.7.14: ein Wächter, der ein Mittel festschreibt
statt der Zusicherung.

### Was diese Runde gelehrt hat

* **Ein Fenster schließt man besser, als man es bewacht.** Runde 13 prüfte
  die Revision vor dem Löschen; das war eine Prüfung mit Lücke. Die
  Revision im Namen macht die Lücke unmöglich, statt sie zu verkleinern.
* **„Nächster Tick" ist eine Annahme, keine Eigenschaft.** Sie gilt im
  Streamer und gilt nicht beim Einreichen. Jede Wiederholungslogik muss
  sagen, WER wiederholt.
* **Wer den Zustand einer Bibliothek mitführt, muss ihren ganzen Zustand
  kennen.** `pending` war die halbe Wahrheit; `state.events` war die
  andere. Beides ist öffentlich, beides gehört bereinigt.

---

## Fünfzehnte Runde: der zweite Einreichweg und ein Nebeneffekt (03.09.2026)

Codex adversarial über die Runde-14-Commits. **Live: `approve`, keine
Befunde** — der erste grüne Repo-Verdict in dieser Serie. Client: **2 High
+ 1 Medium**, davon einer eine echte Regression der Runde-14-Abhilfe.

### High 1 — die Vorablage kannte nur einen der beiden Einreichwege

Runde 14 baute sie in `flight_end` ein. Der manuelle Weg (`file_manual_pirep`,
auch der manuelle Divert) ruft `client.file_pirep` direkt und löscht danach
den gespeicherten Flug; der Trichter kommt erst später. Stirbt der Client
dazwischen, sind Flug und Nachtrag weg — genau das Fenster, das Runde 14
schließen sollte, nur eine Funktion weiter.

**Abhilfe:** ein Helfer `nachtrag_vor_dem_einreichen_sichern(app, flight)`,
und beide Wege rufen ihn unmittelbar vor ihrem `/file`. Der Wächter prüft
das an den zwei Aufrufstellen namentlich und verlangt genau drei
Vorkommen im Produktionsteil: Definition plus zwei Aufrufer. Ein dritter
Einreichweg, der ihn vergisst, ändert diese Zahl nicht — aber er ändert die
Nadel, auf die der Wächter zeigt, und fällt beim nächsten Umbau auf.

### High 2 — meine Bereinigung verwarf empfangene Nachrichten

Die Runde-14-Abhilfe leerte `state.events` vollständig, wertete aber nur
`Outgoing::Publish` und `Incoming::PubAck` aus. Alles andere fiel in den
Leerarm — auch ein bereits empfangenes `Incoming::Publish`, also eine
Integritäts- oder Chat-Nachricht. rumqttc puffert eingehende Pakete vor dem
Flush; scheitert der, landet die Nachricht genau dort und ist lautlos weg.
Bei QoS 0 oder schon gesendetem ACK kommt sie nie wieder.

**Abhilfe:** Nur die zwei buchrelevanten Sorten werden entnommen, alles
andere wandert zurück in die Schlange und wird vom Drive-Loop normal
verarbeitet. Der Test legt jetzt eine Chat-Nachricht neben das
Geist-Ereignis und prüft, dass genau sie überlebt.

Das ist die Klasse „Abhilfe öffnet ein neues Loch" zum achten Mal, aber in
einer neuen Ausprägung: nicht eine Lücke in der Abhilfe, sondern ein
**Nebeneffekt auf einen fremden Weg**. Wer fremden Zustand anfasst, muss
wissen, wer sonst noch daraus liest.

### Medium — Ablagefehler hält das Einreichen nicht auf

Codex will das Einreichen blockieren, wenn die Ablage scheitert. **Thomas'
Entscheidung (03.09.2026): nein.** Der Flugbericht des Piloten ist wichtiger
als die Bahnkorrektur; ein Pilot mit voller Platte soll nicht auf seinem
Flug sitzenbleiben. Der Verlust wird laut gemeldet, die Zusicherung im
Helfer sagt das ausdrücklich. Bewusst offen, nicht übersehen.

### Gegenproben

| Mutation | roter Test |
|---|---|
| manueller Weg sichert nicht | `beim_einreichen_liegt_die_ablage_vor_fahne_und_einreichen` |
| automatischer Weg sichert nicht | derselbe |
| Sicherung erst nach dem Einreichen | derselbe |
| Bereinigung wirft empfangene Nachrichten weg | `gepufferte_ereignisse_landen_vor_der_bereinigung_im_buch` + `die_bereinigung_leert_die_ereignisschlange` |

### Was diese Runde gelehrt hat

* **Eine Zusicherung gilt nicht für eine Funktion, sondern für einen
  Vorgang.** „Vor dem Einreichen" heißt: vor JEDEM Einreichen. Ein Helfer
  und ein Wächter über alle Aufrufstellen, nicht eine Zeile an der Stelle,
  die man gerade offen hat.
* **Wer fremden Zustand leert, muss wissen, wer sonst daraus liest.**
  `state.events` gehörte nicht mir. Ich habe zwei Sorten gebraucht und alle
  genommen.
* **Ein `approve` ist erreichbar.** Die Live-Seite hat es nach drei Runden
  bekommen — dieselbe Methode, nur ein kleinerer Änderungsraum.

---

## Sechzehnte Runde: gerettet ist nicht zugestellt (03.09.2026)

Codex adversarial über den Runde-15-Commit. **Ein Befund, High** — und er
trifft genau die Abhilfe der Vorrunde.

### Der Befund

Runde 15 legte ein gepuffertes `Incoming::Publish` zurück in
`state.events`, statt es wegzuwerfen. Damit war es gerettet, aber nicht
zugestellt: `EventLoop::poll()` versucht bei fehlender Verbindung zuerst
den Reconnect und liefert die Ereignisschlange erst danach aus. Bleibt der
Broker gestört, wartet die Nachricht unbegrenzt; beendet der Pilot die App,
ist sie endgültig weg. Mein Test prüfte nur, dass sie noch in der Schlange
liegt — er prüfte den Verbleib, nicht die Zustellung.

### Abhilfe

* Die Zustellung liegt jetzt in einer eigenen Funktion,
  `eingehendes_publish_zustellen`, und der normale `poll()`-Arm ruft sie
  ebenso wie die beiden Abriss-Stellen.
* `zustellbuch_leitung_weg` reicht die geretteten Publishes heraus
  (`#[must_use]`), statt sie zurückzulegen; beide Aufrufer stellen sie
  sofort zu — vor jedem Reconnect und jedem Backoff.
* Der neue Test baut die Lage nach, um die es geht: `network == None`, ein
  gepuffertes Chat-Publish, kein Verbindungsaufbau — und verlangt, dass die
  Nachricht beim Empfänger ankommt. Er prüft auch, dass der Test selbst
  keine Verbindung öffnet, denn genau das wäre die stille Ausrede.

### Gegenproben

| Mutation | roter Test |
|---|---|
| Nachricht zurückgelegt statt herausgereicht | `die_gerettete_nachricht_erreicht_den_empfaenger_ohne_verbindung` + `gepufferte_ereignisse_landen_vor_der_bereinigung_im_buch` |
| Watchdog stellt nicht zu | `die_bereinigung_leert_die_ereignisschlange` |
| Poll-Fehler stellt nicht zu | derselbe |

Die Fixture des neuen Tests war beim ersten Lauf falsch (Chat-Felder aus
dem Kopf statt aus dem Struct) und machte ihn rot — ein nützlicher Unfall:
Er zeigte, dass der Test wirklich durch die Deserialisierung geht und nicht
an ihr vorbei.

### Was diese Runde gelehrt hat

* **„Gerettet" ist eine schwächere Aussage als „zugestellt".** Meine
  Runde-15-Abhilfe bewahrte die Nachricht vor dem Mülleimer und legte sie
  in eine Warteschlange, deren Bedingung ich nicht geprüft hatte. Wer etwas
  aufhebt, muss sagen, wer es wann wieder herausholt.
* **Ein Test auf den Verbleib ist kein Test auf die Wirkung.** „Liegt noch
  in der Schlange" war grün, während die Nachricht nie ankam. Die Probe
  muss den Empfänger fragen, nicht das Lager.
* **Drei Runden, drei Ausprägungen derselben Klasse.** Runde 14 machte ein
  Loch, Runde 15 einen Nebeneffekt, Runde 16 eine halbe Rettung. Der Weg
  hinaus war jedes Mal derselbe: die Zusicherung schärfer formulieren und
  den Test auf die Wirkung richten.

---

## Siebzehnte Runde: beide Repos `approve` (03.09.2026)

Der adversariale Lauf über den Runde-16-Commit gibt **`approve`** —
„Ship: Die behobene Nachrichtenverlust-Lücke ist im tatsächlichen
HEAD~1-Diff geschlossen; keine substantielle Regression oder ungeschützte
Fehlerstrecke nachweisbar. No material findings."

Damit steht es für beide Repos: **asa-acars-live seit Runde 15,
asa-acars-src seit Runde 16.** Die Befunde sind ausgegangen — das ist die
Abbruchbedingung dieses Verfahrens, nicht eine Zahl von Runden.

### Bilanz der siebzehn Runden

Drei Prüfer im Wechsel: Thomas selbst (Runden 1–3), ein Claude-Prüfagent
(4–10), Codex im normalen und im adversarialen Modus (11–17).

| Phase | Runden | Was gefunden wurde |
|---|---|---|
| Thomas | 1–3 | Zuschnitt, Sperre auf der falschen Größe |
| Prüfagent | 4–10 | das Prinzip: Sendeweg aus unvollständigem Zustand |
| Codex normal | 11 | die Ränder: Verlustpfade, Zeitkaskade, totes Typfeld |
| Codex adversarial | 12–16 | die Abhilfen selbst, fünf Runden lang |

**In neun Runden hintereinander hat eine Abhilfe ein neues Loch geöffnet.**
Die Ausprägungen wurden dabei immer feiner: erst dieselbe Klasse dreimal
(unvollständiger Sendeweg), dann Löcher im Prinzip, dann Ränder der
Abhilfe, dann ein Nebeneffekt auf einen fremden Weg, zuletzt eine halbe
Rettung. Der Änderungsraum schrumpfte von hunderten Zeilen auf eine
Funktion.

**Was jedes Mal geholfen hat**, war nicht mehr Sorgfalt an der Stelle:

1. Das Prinzip ändern, wenn sich die Klasse wiederholt — nicht die Stelle
   reparieren.
2. Jede Abhilfe in der nächsten Runde wie einen Ursprungsbefund prüfen
   lassen.
3. Jede Gegenprobe daraufhin prüfen, ob sie die Zusicherung trifft oder
   nur eine Verfeinerung.
4. Ein Fenster schließen, statt es zu bewachen.
5. Die Probe auf die Wirkung richten, nicht auf den Verbleib.

### Was `approve` nicht heißt

* **Der reale MSFS-Flug ist nie gelaufen.** Der PAVEMENT-Weg, die späte
  Szenerie nach dem Ausrollen (der YSBK-Fall), das Nachholen — nichts
  davon ist je in einem echten Flug gemessen worden. Das bleibt die
  Freigabebedingung.
* **Kein Broker-PUBACK-Ersatz für alles.** Das Zustellbuch ordnet
  Bahn-Nachträge zu; andere Ereignisse laufen weiter fire-and-forget, was
  für sie richtig ist.
* **Bewusst offen:** Ein Ablagefehler hält das Einreichen nicht auf (der
  Flugbericht des Piloten geht vor, Thomas' Entscheidung); die
  `runway`-Spalte kommt weiter aus `approach_runway`;
  `ACHSE_TOLERANZ_M = 5` ist ohne Korpus gewählt.

---

## Eine externe QS anstoßen

    /code-review ultra

Nutzergestartet und kostenpflichtig. Der Prüfstand der Vorfassung
(`v1.7.14-pruefstand.md`) trägt zwölf Runden — und die sechs Fehlerklassen, die
sich dort durchzogen. Sie sind der beste Einstieg, weil fast alle späten Befunde
**die Prüfungen** trafen und nicht die Produktion:

1. Ein Wächter, der ein **Mittel** festschreibt statt einer **Zusicherung**
2. Ein Test, der die entscheidende Achse **konstant hält**
3. Ein Wächter, der **sich selbst findet** — auch bei zusammengebauten Nadeln,
   wenn ein Kommentar die verbotene Form erwähnt
4. Eine Gegenprobe, die den **falschen** Test rot macht
5. Ein Test, der einen Zustand löscht, ohne ihn vorher zu setzen
6. Die **Blindprobe** als Gegenprobe zur Gegenprobe

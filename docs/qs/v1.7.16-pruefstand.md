# Prüfstand v1.7.16 — Parkpositionen aus der Szenerie

Auslöser (03.09.2026, Live-Auswertung der ersten v1.7.15-Flüge): Vier von
sieben echten Flügen hatten mindestens ein fehlendes Gate. Der Fall EDDS→LEPA
(Palma, 328 kartierte Stände, trotzdem keine Ankunfts-Standposition) ließ
sich nicht mit „OSM kennt den Platz nicht" erklären — die Karte war da.

Thomas' Frage traf den eigentlichen Punkt: *„Eigentlich liefert der
Simulator uns doch die Position. Wenn ich in einem MSFS starte, wähle ich
doch auch schon die Parkposition aus."* Die Antwort war ja — beide
Simulatoren führen ihre eigenen Standdaten, und die Bahn-aus-der-Szenerie-
Architektur (v1.7.8) hatte das Muster für genau diesen Fall schon einmal
gelöst: die installierte Szenerie ist die erste Instanz, eine Drittquelle
(OpenStreetMap, live.kant.ovh via AeroDataBox — ungeeignet, weil es ein
Echtzeit-Flugverfolgungsdienst ist, keine statische Flughafen-Layout-
Datenbank) nur das Netz.

## Was gebaut wurde

**Ein gemeinsamer Typ** (`sim_core::szenerie::SzenerieStand`), analog zu
`SzenerieBahn`/`SzenerieRollweg` — beide Simulatoren produzieren ihn, der
Auswerter kennt keinen Unterschied.

**X-Plane**: `apt.dat`-Zeilencode `1300` (Rampenstart/Gate), dieselbe Datei,
die schon für Bahnen gelesen wird — kein neuer Dateizugriff, nur ein
weiterer Zeilencode im selben Durchlauf. Format nach der offiziellen
Spezifikation (developer.x-plane.com, APT1100/1130) verifiziert, nicht aus
dem Gedächtnis übernommen.

**MSFS**: SimConnect-Facility-Gruppe `TAXI_PARKING`, parallel zu `RUNWAY`
und den drei Rollweg-Gruppen. Der Enum-Wert `SIMCONNECT_FACILITY_DATA_TAXI_PARKING`
steht im vendorten SDK-Header (`ffi/include/SimConnect.h`) und wird von
bindgen erzeugt — nicht geraten. Die Feldnamen (`TYPE`, `NAME`, `SUFFIX`,
`NUMBER`, `HEADING`, `RADIUS`, `BIAS_X`, `BIAS_Z`) stammen aus der
offiziellen SDK-Dokumentation (`docs.flightsimulator.com`), derselben
Quelle, die auch `BAHN_DEFINITION` und die Rollweg-Feldlisten belegt hat.

## MSFS-Buchstabe: `SUFFIX` und (seit Runde 2) `NAME` — `TYPE` bleibt außen vor

`TYPE` bleibt außen vor — seine Ordnungszahlen sind eine ANDERE Aufzählung
(`SIMCONNECT_TAXIWAY_PARKING_TYPE`) als die von `NAME`/`SUFFIX`, und dafür
liegt keine verifizierte Werteliste vor.

**Korrektur nach externer Gegenprüfung (Codex, adversarial, 04.09.2026,
Runde 1):** Die ERSTE Fassung dieses Abschnitts nahm für `SUFFIX` „NONE
oder ein Buchstabe" an (aus der knapperen Editor-Dokumentation) und
kodierte `1..=26 → A..Z` — mit Grenzprüfung, aber auf falscher Grundlage.
Codex fand den Fehler, bevor der erste Flug lief: gegen die offizielle
SDK-Referenz (`docs.flightsimulator.com`,
`SimConnect_AddToFacilityDefinition`, TAXI_PARKING) verifiziert, teilen
sich `SUFFIX` **und** `NAME` wortwörtlich dieselbe Aufzählung:

```text
0: NONE          6: S_PARKING     11: DOCK
1: PARKING       7: SW_PARKING    12..37: GATE_A .. GATE_Z
2: N_PARKING     8: W_PARKING
3: NE_PARKING    9: NW_PARKING
4: E_PARKING     10: GATE
5: SE_PARKING
```

Nur `12..=37` ist ein Buchstabe (`12 → A`). Die alte `1..=26`-Annahme hätte
aus Suffix 1 (PARKING) den erfundenen Buchstaben „A" gemacht — ein
Anzeigefehler derselben Klasse wie `belag_code` (v1.7.12): eine plausible,
aber ungeprüfte Kodierung einer fremden Simulator-API angewandt.

**Runde 2:** Die Runde-1-Fassung deckte NUR `SUFFIX` ab und ließ `NAME`
absichtlich außen vor — mit der (jetzt widerlegten) Begründung, seine
Ordnungszahlen seien unbekannt. Da beide Felder dieselbe, jetzt verifizierte
Tabelle tragen, ist diese Begründung hinfällig: Codex zeigte den konkreten
Fall `NAME = GATE_A(12)`, `SUFFIX = NONE(0)`, bei dem die Runde-1-Fassung
stumm auf „23" statt eines Buchstabens gefallen wäre. `stand_name` decodiert
seither beide Felder.

**Runde 3:** Die Runde-2-Fassung setzte den `NAME`-Buchstaben an dieselbe
Stelle wie `SUFFIX` — immer NACH der Nummer (z. B. „23A" für
`NAME = GATE_A`). Das widerspricht der Scenery-Editor-Dokumentation
(`docs.flightsimulator.com`, Developer_Mode/Scenery_Editor/Objects/
TaxiwayParking_Objects.htm): `NUMBER` „goes with the given name" (hängt
sich an `NAME` an, also DAVOR), `SUFFIX` wird „to be added to the taxiway
parking name and number" (hängt sich ans Ende von `NAME` + `NUMBER` an,
also DANACH). `NAME = GATE_A`, `NUMBER = 23`, `SUFFIX = NONE` ist demnach
„A23", nicht „23A" — die Runde-2-Fassung hätte das falsche Ergebnis sowohl
in der Anzeige als auch in den PIREP-Custom-Fields ausgeliefert. `stand_name`
setzt seither `NAME` VOR und `SUFFIX` NACH der Nummer; beide können jetzt
gleichzeitig beitragen (`NAME = GATE_Z`, `SUFFIX = GATE_A` → „Z23A"), womit
auch die in Runde 2 offene Frage nach einer Vorrangregel entfällt — es gibt
keinen Widerspruch mehr, weil beide Positionen unterschiedlich sind.

**Diese Zeile bleibt offen, bis ein echter MSFS-Flug sie bestätigt oder
widerlegt** — wie beim Bahn-Feature selbst kann kein Adapter-Code hier ohne
laufenden Simulator verifiziert werden. Verifiziert ist jetzt die
VOLLSTÄNDIGE dokumentierte Struktur (Feldnamen, Byte-Größen, Enum-Existenz,
die numerische Kodierung UND die Zusammensetzungsposition, alle gegen
Primärquellen), nicht mehr nur ihre Existenz — aber ob MSFS im Feld
tatsächlich Werte im dokumentierten Bereich liefert, bleibt ungeprüft bis
zum ersten echten Flug.

## Wie Szenerie und OSM zusammenspielen

Eine Stelle, `standliste_fuer(stats, icao, osm)` — Szenerie zuerst (mit
ICAO-Prüfung gegen `stats.szenerie_auskunft`, damit keine veraltete Auskunft
eines anderen Flughafens einspringt), OSM als Netz nur wenn die Szenerie
nichts hat. Vier Verbrauchsstellen rufen sie auf: der Boarding-Nachzieh-Tick,
der BlocksOn/Arrived-Nachzieh-Tick, das Stand-Gate selbst
(RYR-1142-Erkennung) und der Arrived-Fallback (`settle_at_arrival_stand`).

Bewusst NICHT verändert: die unmittelbare Erfassung direkt beim OSM-Fetch-
Abschluss. Bricht die OSM-Antwort leer ab (MGGT/MNMG-Fall), fängt der
nächste Tick der drei ANKUNFTS-Stellen (siehe unten) den Szenerie-Stand
trotzdem ein — die Verzögerung ist ein Tick, kein fehlender Fall.

### Ehrliche Einschränkung, gefunden von der externen Gegenprüfung (Runde 1): nur die ANKUNFT ist heute wirklich szenerie-gestützt

Die erste Fassung dieses Dokuments zählte „vier Verbrauchsstellen" auf, als
wären alle vier gleich gut versorgt. Das stimmt nicht: `stats.szenerie_auskunft`
wird — bei BEIDEN Simulatoren — ausschließlich für das ZIEL geerntet, nie
für den Abflugplatz:

* **X-Plane** liest die Szenerie erst bei der Bahn-Korrelation am Touchdown
  (`korreliere_bahn_bei_touchdown`, gegen `actual_icao` — den TATSÄCHLICHEN
  Landeplatz). Vor dem Aufsetzen ist `szenerie_auskunft` für diesen Flug
  leer.
* **MSFS** fordert die Facility-Daten über `szenerie_ziele`/`szenerie_ernteziel`
  an — beide ausschließlich aus `runway_correlation_icao`,
  `divert_prefetch_icao` und `flight.arr_airport`, alles Ziel-Kandidaten.
  Kein Pfad fragt je den Abflugplatz an.

Der Boarding-Nachzieh-Tick (`standliste_fuer(&stats, &flight.dpt_airport, …)`)
ruft die Funktion damit zwar korrekt auf, bekommt aber strukturell so gut
wie nie eine Szenerie-Antwort — die ICAO-Prüfung in `standliste_fuer` lehnt
die (Ziel-)Auskunft für den (Abflug-)Platz zu Recht ab. Er fällt im
Regelfall auf OSM zurück, genau wie vor diesem Feature. Das ist kein Bug —
`standliste_fuer` selbst verhält sich exakt wie dokumentiert und getestet —
sondern eine Lücke in der ERNTE, die dieses Release NICHT schließt: eine
eigene Abflug-Ernte (für MSFS ein zweiter SimConnect-Request beim
Flugstart, für X-Plane ein `apt.dat`-Lesen VOR dem Rollen statt erst beim
Touchdown) wäre ein eigenständiges Stück Arbeit, keine Erweiterung dieses
Merges. **Damit liefert dieses Release Szenerie-Stände verlässlich nur für
die ANKUNFT** — genau der Fall, der den Auslöser (EDDS→LEPA) motiviert
hat. Die Abflugseite bleibt auf dem alten OSM-Stand, bis eine eigene
Abflug-Ernte gebaut wird (Thomas' Entscheidung, siehe „Was noch aussteht").

## Externe Gegenprüfung (Codex, adversarial)

`node codex-companion.mjs adversarial-review --base HEAD~1 --scope branch`,
jeweils gegen den letzten Commit — dieselbe Schleife wie bei v1.7.15
(fixen → testen → gegenproben → committen → nächste Runde, bis keine neuen
Befunde mehr kommen).

### Runde 1 (gegen den ersten Commit) — Verdikt `needs-attention`, drei Befunde

| Schwere | Befund | Reaktion |
|---|---|---|
| High | `SUFFIX` als `1..=26 → A..Z` decodiert — gegen die SDK-Referenz falsch, echte `A..Z` liegen bei `12..=37` | **Gefixt.** `stand_name` korrigiert, Tests korrigiert + ergänzt (`stand_name_kategorie_suffix_ist_kein_buchstabe`), Gegenprobe R1a unten |
| Medium | BlocksOn/Arrived-Nachzieh-Tick hing an `arr_stands_icao` (nur bei ERFOLGREICHEM OSM-Abruf gesetzt) — scheitert OSM komplett, griff der Szenerie-Rückfall nie | **Gefixt.** Neue Funktion `arrival_retry_ziel` trennt ICAO-Eigentümerschaft (`arr_stands_requested_icao`) vom OSM-Erfolg, mit eigenem Divert-Schutz gegen eine veraltete OSM-Liste. Vier neue Tests, Gegenprobe R1b unten |
| Medium | Boarding-Nachzieh-Tick hat strukturell keine Abflug-Szenerie zum Konsumieren (weder X-Plane noch MSFS ernten den Abflugplatz) | **Nicht gefixt, ehrlich dokumentiert** (siehe „Ehrliche Einschränkung" oben) — eine Abflug-Ernte ist ein eigenständiges Stück Arbeit, keine Erweiterung dieses Merges. Scope-Entscheidung liegt bei Thomas |

Der dritte Befund ist bewusst keine Zeile Code geworden: `standliste_fuer`
und die Boarding-Verbrauchsstelle verhalten sich beide exakt wie
spezifiziert (Szenerie zuerst, sonst OSM) — das Problem liegt VOR dieser
Stelle, in der fehlenden Ernte. Ihn dennoch als „behoben" zu verbuchen wäre
genau die Fehlerklasse, die dieses Dokument selbst kritisiert (falsche
Abdeckung behauptet).

### Runde 2 (gegen den Runde-1-Fix-Commit) — Verdikt `needs-attention`, zwei Befunde

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | `stand_name` verwirft `NAME` weiterhin vollständig — Beispiel `NAME=GATE_A(12)`, `SUFFIX=NONE(0)` würde stumm auf „23" statt „23A" fallen. Die Runde-1-Begründung (Ordnungszahlen von NAME unbekannt) ist seit Runde 1 selbst widerlegt: NAME trägt dieselbe verifizierte Tabelle wie SUFFIX | **Gefixt.** `stand_name` decodiert jetzt beide Felder, SUFFIX gewinnt bei einem Widerspruch. Drei neue Tests, Gegenprobe R2a unten |
| Medium | `arrival_retry_ziel` (R1) nutzt `arr_stands_requested_icao`, das seinerseits an `divert_hint` hängt. `divert_hint` wird laut eigenem Code-Kommentar „erst beim Arrived-Fallback gemintet" — ein Divert, der normal über TaxiIn→BlocksOn ankommt (kein Fallback nötig), mintet u. U. NIE einen `divert_hint`, und der Nachzieh-Tick bliebe dauerhaft beim GEPLANTEN statt dem echten Ziel | **Gefixt.** Neues Feld `arr_actual_icao`, von `settle_at_arrival_stand` bei JEDEM Aufruf aus der frisch berechneten Position gesetzt (unabhängig von `divert_hint`) — `arrival_retry_ziel` bevorzugt es jetzt vor `arr_stands_requested_icao`. Zwei neue Tests, Gegenprobe R2b unten |

Beide Runde-2-Befunde bestätigten sich bei genauer Prüfung als real, aber
enger als Codex' erste Formulierung: `settle_at_arrival_stand` berechnete
sein `at_icao` schon vorher bei JEDEM Aufruf frisch aus der Position (nie
aus `divert_hint`) — das Problem betraf ausschließlich den NACHZIEH-Tick,
der (noch) keinen Zugriff auf diesen bereits korrekten Wert hatte. Die
Lösung persistiert genau diesen längst richtigen Wert, statt eine neue
Berechnung einzuführen.

### Runde 3 (gegen den Runde-2-Fix-Commit) — Verdikt `needs-attention`, drei Befunde

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | `NAME`-Buchstabe stand an der falschen Stelle — immer NACH der Nummer wie `SUFFIX`, statt DAVOR (siehe MSFS-Buchstabe-Abschnitt oben). `NAME = GATE_A, NUMBER = 23, SUFFIX = NONE` wurde „23A" statt „A23" | **Gefixt.** `stand_name` setzt `NAME` jetzt vor, `SUFFIX` nach der Nummer; beide können gleichzeitig beitragen. Tests korrigiert + ein neuer Test für den gleichzeitigen Fall, Gegenprobe R3a unten |
| Medium | `maybe_spawn_stand_fetch` (der eigentliche OSM-Abruf) wählte sein Ziel-ICAO weiterhin NUR aus `divert_hint` — `arrival_retry_ziel` (R1/R2) bekam zwar das richtige `arr_actual_icao` zum PRÜFEN, aber der OSM-ABRUF selbst fragte bei einem divert_hint-losen Divert für immer den GEPLANTEN Platz an. Kennt die Szenerie den echten Platz nicht, gäbe es dann auch keinen OSM-Rückfall | **Gefixt.** Logik in neue Funktion `arrival_fetch_ziel_icao` extrahiert (dieselbe Priorität wie `arrival_retry_ziel`: `arr_actual_icao` → `divert_hint` → geplant), von `maybe_spawn_stand_fetch` aufgerufen. Drei neue Tests, Gegenprobe R3b unten |
| Medium | `arr_actual_icao` (R2) fehlte in `PersistedFlightStats` — ein App-Neustart in BlocksOn/Arrived verlor den Wert, weil `settle_at_arrival_stand` (die einzige Setzstelle) nach dem Neustart nicht erneut läuft | **Gefixt.** Feld zu `PersistedFlightStats` ergänzt (Save- und Restore-Seite), `#[serde(default)]` für alte Resume-Dateien. Ein neuer Rundlauf-Test, Gegenprobe R3c unten |

Alle drei Runde-3-Befunde waren echte, nicht überzeichnete Lücken — anders
als in Runde 2 gab es hier keine Stelle, die das Problem bereits woanders
abfing.

### Runde 4 (gegen den Runde-3-Fix-Commit) — Verdikt `needs-attention`, zwei Befunde

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | Zaehler-Reset in `maybe_spawn_stand_fetch` hing zusätzlich an `stats.arr_stands_requested == true`. Ein Fehlversuch setzt dieses Flag aber VORHER schon auf `false` (damit der nächste Tick es erneut versucht) — trifft ein Zielwechsel GENAU in so einem Tick, greift der Reset nicht, und der neue (tatsächliche) Platz erbt das fast erschöpfte Zähler-Budget des alten (geplanten) | **Gefixt.** Flag-Bedingung entfernt — der Reset hängt jetzt ausschließlich am ICAO-Vergleich, über die neue gemeinsame Funktion `passt_zum_angefragten_ankunftsziel` |
| Medium | `arr_ground_geojson` (die Bodenkarte für Rollweg-Ausfahrten) wurde OHNE Stale-Spawn-Riegel geschrieben — anders als `arr_stands`/`arr_stands_icao` direkt daneben. Eine verspätete Antwort für den ALTEN (geplanten) Platz konnte die bereits geladene Karte des ECHTEN Platzes überschreiben, ohne dass ein Fehler sichtbar würde | **Gefixt.** Derselbe Riegel wie bei `arr_stands` angewandt, über dieselbe gemeinsame Funktion |

Beide Befunde betrafen dieselbe Grundursache: drei Stellen prüften bisher
„gehört diese Antwort/dieser Zähler noch zum aktuell angefragten Ziel"
unabhängig voneinander — zwei davon fehlerhaft oder fehlend. `maybe_spawn_stand_fetch`
und der Antwort-Handler nutzen jetzt EINE Funktion
(`passt_zum_angefragten_ankunftsziel`) an allen drei Stellen, mit drei
neuen Tests und einer Gegenprobe (R4a) dafür. Für die beiden konkreten
Aufrufstellen selbst (Zähler-Reset, Bodenkarten-Übernahme) gibt es KEINE
eigene rot werdende Gegenprobe — `maybe_spawn_stand_fetch` ist eine
async-Funktion mit Netzwerk-Spawn, die dieses Projekt (wie schon an
anderer Stelle dokumentiert, siehe „Was noch aussteht") ohne Windows-
Laufzeit/Integrationsrahmen nicht isoliert prüfen kann; verifiziert ist
hier nur, dass beide Stellen dieselbe, getestete Funktion aufrufen, nicht
mehr eigene, unabhängige (und potenziell wieder auseinanderlaufende)
Vergleiche.

### Runde 5 (gegen den Runde-4-Fix-Commit) — Verdikt `needs-attention`, ein Befund

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | Der Runde-4-Schreib-Riegel (`passt_zum_angefragten_ankunftsziel`) beweist nur den MOMENT der Ablage von `arr_ground_geojson`. `arr_ground_geojson` selbst trug (anders als `arr_stands`/`arr_stands_icao`) KEINE eigene ICAO-Markierung — ändert sich das Ziel danach ERNEUT (z. B. ein zweiter Divert), blieb die Karte des alten Ziels unmarkiert liegen, und `bahn_felder` las sie beim Lesezugriff ohne jede ICAO-Prüfung | **Gefixt.** Neues Feld `arr_ground_geojson_icao`, atomar mit der Karte gesetzt; `bahn_felder` prüft es beim Lesen gegen `runway_correlation_icao` (den tatsächlichen Landeplatz). Zwei Bestandstests um die neuen Felder ergänzt (sonst hätten sie ihre eigentliche Prüfung — Inhalt statt Zugehörigkeit — nicht mehr erreicht), ein neuer Test, Gegenprobe R5a unten |

Diese Runde deckte eine SCHREIB/LESE-Asymmetrie auf: `arr_stands` hatte von
Anfang an ein eigenes ICAO-Feld, `arr_ground_geojson` nicht — obwohl beide
aus derselben Antwort stammen und denselben Lebenszyklus haben. Die Lücke
lag nicht im Runde-4-Fix selbst, sondern in einer schon vorher bestehenden
Asymmetrie, die erst durch die Runde-4-Änderung (ein zweiter,
möglicherweise überholender Zielwechsel wird jetzt strukturell erwartet)
praktisch relevant wurde.

### Runde 6 (gegen den Runde-5-Fix-Commit) — Verdikt `needs-attention`, ein Befund

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | Der Runde-5-Fix prüfte nur `arr_ground_geojson` (die OSM-Serverkarte) gegen `runway_correlation_icao` — `szenerie_karte` (die BEVORZUGTE Quelle, direkt aus `stats.szenerie_auskunft`) bekam GAR KEINE ICAO-Prüfung und hatte zusätzlich Vorrang vor der jetzt geprüften Serverkarte. Eine veraltete `szenerie_auskunft` (z. B. ein früherer Divert-Kandidat, dessen Ernte noch nicht durch die des tatsächlichen Landeplatzes ersetzt wurde) hätte damit nicht nur selbst falsche Daten geliefert, sondern auch eine korrekt geprüfte Serverkarte verdrängt | **Gefixt.** `szenerie_karte` bekommt dieselbe ICAO-Prüfung gegen `runway_correlation_icao` wie `arr_ground_geojson` — mit `None` (vor dem Touchdown, bei keiner Produktions-Aufrufstelle von `bahn_felder` der Fall) als unverändertem Rückfall, um die bestehende Live-Vorschau nicht zu blockieren. Ein neuer Test, Gegenprobe R6a unten |

Die ICAO-Zugehörigkeitsprüfung war damit an allen drei relevanten Stellen
vorhanden (Szenerie, Serverkarte, OSM-Standliste über `standliste_fuer`) —
**„die letzte Lücke" war zu früh behauptet**, siehe Runde 7: es fehlte noch
die Frage, GEGEN WELCHES ICAO genau geprüft wird.

### Runde 7 (gegen den Runde-6-Fix-Commit) — Verdikt `needs-attention`, ein Befund

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | Runde 6 prüfte Szenerie UND Serverkarte gegen `runway_correlation_icao` — aber `bahn_felder` projiziert die Ausfahrten auf die Geometrie von `runway_match` (der tatsächlich gematchten Bahn), und beide ICAOs sind NICHT garantiert identisch: `correlate_airport_icao` bevorzugt bei mehrdeutigen Nachbarflughäfen bewusst den GEPLANTEN Platz, während `lookup_runway_with_fallback` unabhängig davon eine Bahn eines tatsächlich näheren NACHBARN zurückgeben kann. Eine gegen `runway_correlation_icao` geprüfte Karte wäre dann auf die Bahn des Nachbarn projiziert worden — plausibel aussehende, aber falsche Ausfahrten. Zusätzlich: `None => true` ließ eine aus einer alten Fassung wiederhergestellte `runway_correlation_icao` (`#[serde(default)]`) grundlos passieren | **Gefixt.** Beide Prüfungen laufen jetzt gegen `runway_match.airport_ident` — den Platz, zu dem die tatsächlich verwendete Geometrie gehört —, mit `None => false` (fail closed) statt `None => true`. Kostet nichts: ohne `runway_match` berechnet `bahn_felder` ohnehin keine Ausfahrten. Ein neuer Test, Gegenprobe R7a unten |

Ein zweiter, ursprünglich geplanter Test (Legacy-Resume: `runway_match`
vorhanden, `runway_correlation_icao` fehlt) erwies sich beim Schreiben als
nicht beweisfähig: `landeplatz_der_bahn` ist `None` GENAU DANN, wenn `rm`
selbst `None` ist — und ohne `rm` berechnet `bahn_felder` so oder so keine
Ausfahrten. Der Fail-closed-Zweig ist damit strukturell nicht von außen
beobachtbar; er ist eine Verteidigung für den Fall, dass sich diese
Kopplung künftig einmal ändert, keine heute nachweisbare Verhaltensänderung
— ehrlich als solche belassen, statt einen Test zu schreiben, der nichts
beweist.

### Runde 8 (gegen den Runde-7-Fix-Commit) — Verdikt `approve`

Keine Befunde mehr. Die Schleife endet hier — dieselbe Abbruchregel wie bei
v1.7.15 (Ende, sobald eine Runde ohne neue Befunde zurückkommt).

**Bilanz der acht Runden:** ein High-Befund (falsche SUFFIX-Kodierung, hätte
falsche Buchstaben erfunden) und zehn Medium-Befunde, alle bis auf einen
(die bewusst offen gelassene Abflug-Ernte, eine Scope-Entscheidung) gefixt.
Sechs der zehn Medium-Befunde drehten sich um dieselbe Grundfrage in immer
neuen Verkleidungen — „gehört diese Datenquelle (Szenerie, Serverkarte,
OSM-Anfrage) wirklich zum aktuell relevanten Flughafen, und zu WELCHEM
ICAO genau" — und wurden erst in Runde 7 endgültig geschlossen, als klar
wurde, dass selbst `runway_correlation_icao` nicht dieselbe Garantie bietet
wie `runway_match.airport_ident`. Das ist genau das Muster, das die
v1.7.15-Runden schon zeigten: eine Abhilfe schließt die Lücke an EINER
Stelle und öffnet oder übersieht sie an der nächsten, bis die Prüfung
konsequent an der WIRKLICH massgeblichen Referenz (hier: der Bahn-Geometrie
selbst) statt an einer plausibel klingenden, aber nicht garantiert
identischen Ersatzgröße ansetzt.

## Gegenproben

Fünfzehn Mutationen gegen den Live-Code (vier aus dem Erstbau, zwei aus
Codex-Runde 1, zwei aus Codex-Runde 2, drei aus Codex-Runde 3, eine aus
Codex-Runde 4, eine aus Codex-Runde 5, eine aus Codex-Runde 6, eine aus
Codex-Runde 7), jeweils mit Snapshot vor der Mutation und Wiederherstellung
danach (`cmp` bestätigt: alle Dateien sauber zurück).

| # | Mutation | Zusicherung, die getroffen werden muss | Ergebnis |
|---|---|---|---|
| 1 | ICAO-Riegel in `standliste_fuer` entfernt | veraltete `szenerie_auskunft` eines ANDEREN Flughafens darf nicht einspringen | ROT — `veraltete_szenerie_auskunft_eines_anderen_flughafens_wird_ignoriert` (1 von 6 fehlgeschlagen, genau die erwartete) |
| 2 | Leere-Stände-Filter in `standliste_fuer` entfernt | eine leere Szenerie-Liste muss auf OSM zurückfallen, nicht als „Szenerie hat geliefert" durchgehen | ROT — `leere_szenerie_staendeliste_faellt_auf_osm_zurueck` (1 von 6 fehlgeschlagen) |
| 3 | X-Plane-Namensindex verschoben (`t[7..]` statt `t[6..]`) | der Name eines `1300`-Rampenstarts beginnt an Spalte 6, nicht 7 | ROT — beide Namenstests (`liest_rampenstarts_mit_position_und_namen`, `rampenstart_name_kann_leerzeichen_enthalten`), 2 von 13 fehlgeschlagen |
| 4a | MSFS-Suffix (Erstbau, VOR der Codex-Korrektur): `1..=26` → `0..=26` | Suffix 0 darf keinen Buchstaben erzeugen | **Fehlgriff, keine echte Gegenprobe** — der `0 =>`-Zweig steht in `stand_name`s `match` VOR dem Bereichsmuster und gewinnt bei Rust immer zuerst; der mutierte Bereich wird für 0 nie ausgewertet. Kein Testdefizit, sondern eine unerreichbare Mutation. |
| 4b | MSFS-Suffix-Obergrenze (Erstbau, VOR der Codex-Korrektur): `1..=26` → `1..=25` | Suffix 26 muss noch „Z" ergeben | ROT — `stand_name_nummer_und_suffix` (1 von 3 gefiltert fehlgeschlagen). Die geprüfte Zusicherung selbst (obere Grenze schneidet den letzten Buchstaben ab) war korrekt getestet — nur die zugrundeliegende Kodierung (`1..=26`) war falsch, siehe R1a |
| R1a | MSFS-Suffix NACH der Codex-Korrektur: `12..=37` → `1..=26` (der alte, von Codex widerlegte Bereich) | Suffix 12 = „A", Kategorie-Codes 1..=11 bleiben ohne Buchstaben | ROT — `stand_name_nummer_und_suffix` UND `stand_name_kategorie_suffix_ist_kein_buchstabe` (2 von 7 gefiltert fehlgeschlagen) |
| R1b | `arrival_retry_ziel`: ICAO-Quelle auf `arr_stands_icao` zurückgesetzt (die alte, nur-bei-OSM-Erfolg-gesetzte Fassung) | ein angefragtes ICAO ohne OSM-Erfolg muss trotzdem durchgereicht werden; eine veraltete OSM-Liste eines ANDEREN angefragten Flughafens darf nicht durchrutschen | ROT — `retry_ziel_liefert_icao_auch_wenn_osm_nie_geliefert_hat` UND `retry_ziel_verwirft_osm_eines_anderen_angefragten_flughafens` (2 von 10 fehlgeschlagen) |
| R2a | `stand_name`: NAME-Rückfall entfernt (`buchstabe(s.suffix)` statt `buchstabe(s.suffix).or_else(‖ buchstabe(s.name))`) | `NAME=GATE_A(12)` mit `SUFFIX=NONE(0)` muss trotzdem „23A" ergeben | ROT — `stand_name_faellt_auf_name_zurueck_wenn_suffix_keinen_buchstaben_traegt` (1 von 7 gefiltert fehlgeschlagen) |
| R2b | `arrival_retry_ziel`: `arr_actual_icao`-Vorrang entfernt (zurück auf reines `arr_stands_requested_icao`) | ein bereits von `settle_at_arrival_stand` festgestelltes echtes ICAO muss ein abweichendes `arr_stands_requested_icao` schlagen | ROT — `retry_ziel_bevorzugt_arr_actual_icao_vor_dem_angefragten_icao` (1 von 12 fehlgeschlagen) |
| R3a | `stand_name`: `NAME`-Buchstabe zurück auf die SUFFIX-Position (nach der Nummer statt davor) | `NAME = GATE_A` mit `SUFFIX = NONE` muss „A23" ergeben, nicht „23A"; gleichzeitig gesetzte `NAME`+`SUFFIX` müssen beide erscheinen | ROT — `stand_name_faellt_auf_name_zurueck_wenn_suffix_keinen_buchstaben_traegt` UND `stand_name_name_und_suffix_koennen_beide_gleichzeitig_beitragen` (2 von 7 gefiltert fehlgeschlagen) |
| R3b | `arrival_fetch_ziel_icao`: `arr_actual_icao`-Vorrang entfernt (zurück auf reines `divert_hint`) | ein bekanntes `arr_actual_icao` muss ein abweichendes `divert_hint` beim OSM-Abrufziel schlagen | ROT — `fetch_ziel_bevorzugt_arr_actual_icao_vor_divert_hint_und_geplant` (1 von 15 fehlgeschlagen) |
| R3c | `PersistedFlightStats::snapshot_from`: `arr_actual_icao` beim Speichern durch `None` ersetzt | der Wert muss den Save→Restore-Rundlauf überleben | ROT — `arr_actual_icao_ueberlebt_snapshot_rundlauf` (1 von 1 fehlgeschlagen) |
| R4a | `passt_zum_angefragten_ankunftsziel` auf `true` konstant gesetzt | eine Antwort ohne je gestellte Anfrage und eine Antwort für ein ANDERES, früher angefragtes ICAO dürfen nicht als „passend" gelten | ROT — `passt_nicht_zum_ziel_ohne_je_eine_anfrage` UND `passt_nicht_zum_ziel_nach_einem_wechsel` (2 von 18 fehlgeschlagen) |
| R5a | `bahn_felder`: ICAO-Zugehörigkeitsprüfung für `arr_ground_geojson` entfernt (Rückfall direkt auf `stats.arr_ground_geojson.as_deref()`) | eine Bodenkarte mit veralteter ICAO-Markierung (`arr_ground_geojson_icao` ≠ `runway_correlation_icao`) darf keine Ausfahrten liefern, selbst bei inhaltlich gültiger Geometrie | ROT — `bodenkarte_mit_veralteter_icao_markierung_wird_verworfen` (1 von 1 fehlgeschlagen) |
| R6a | `bahn_felder`: ICAO-Prüfung für `szenerie_karte` entfernt (Rückfall auf reines `.filter(\|a\| !a.rollwege.is_empty())`) | eine veraltete Szenerie-Auskunft eines ANDEREN Flughafens darf eine korrekt zugeordnete, gültige Serverkarte nicht verdrängen | ROT — `veraltete_szenerie_unterdrueckt_die_gueltige_serverkarte_nicht` (1 von 1 fehlgeschlagen) |
| R7a | `landeplatz_der_bahn` zurück auf `stats.runway_correlation_icao.as_deref()` statt `rm.map(\|m\| m.airport_ident.as_str())` | eine für den KORRELATIONS-Platz markierte Karte darf nicht auf die Geometrie einer davon ABWEICHENDEN gematchten Bahn projiziert werden | ROT — `karte_fuer_einen_abweichenden_korrelations_platz_wird_verworfen` (1 von 1 fehlgeschlagen) |

Alle fünfzehn Zusicherungen sind damit durch eine tatsächlich rot werdende
Gegenprobe gedeckt. Nicht mutiert (bewusst, geringes Risiko): der
`bias_x`/`bias_z`-Achsentausch in `staende_zusammensetzen` — nutzt denselben,
bereits für Rollwege bewiesenen `punkt_aus_versatz` in derselben
Parameterreihenfolge wie der geprüfte Bahn-Pfad.

## Was noch aussteht

* Der reale Flug in beiden Simulatoren — für MSFS insbesondere, ob die
  gelieferten `NAME`/`SUFFIX`-Werte tatsächlich im dokumentierten Bereich
  liegen (die Kodierung UND die Zusammensetzungsposition sind jetzt gegen
  Primärquellen verifiziert, siehe oben).
* **Scope-Entscheidung offen (Thomas):** eine eigene Abflug-Ernte, damit
  auch der Boarding-Nachzieh-Tick echte Szenerie-Daten bekommt (siehe
  „Ehrliche Einschränkung" oben) — dieses Release deckt nur die Ankunft.

## Stand

Acht Runden externer Gegenprüfung (Codex, adversarialer Modus), letzte
Runde `approve` ohne Befunde. Workspace 50/50 Ziele grün
(`cargo test --workspace`), Frontend grün (`tsc -b`, `npm test`).
Commits `7ad69fb`…`53a9df6` auf `main`, CI (Windows + Linux) für jeden
Commit grün. Kein Release/Tag — dieses Dokument beschreibt den Code-Stand,
nicht eine Freigabe zum Ausliefern; die reale Flugverifikation (siehe oben)
steht noch aus.

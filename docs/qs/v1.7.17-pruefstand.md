# Prüfstand v1.7.17 — Abflug-Stand aus der Szenerie

Direkte Folgearbeit zu v1.7.16 (Parkpositionen aus der Szenerie). Der
Prüfstand dort dokumentierte ehrlich eine Lücke: Szenerie-Stände deckten
nur die ANKUNFT ab, weil weder X-Plane noch MSFS den Abflugplatz je
ernteten (`docs/qs/v1.7.16-pruefstand.md`, „Ehrliche Einschränkung").

Thomas' Rückfrage: *„Gibt es für den Abflughafen eine andere Lösung?"* —
statt der ursprünglich skizzierten eigenständigen, parallelen Erntemaschine
wurde bei genauerer Untersuchung ein deutlich billigerer Weg gefunden.

## Der Befund, der den Umfang klein hielt

Zwei Dinge existierten bereits, bevor eine Zeile Code geschrieben wurde:

1. **MSFS registriert den Abflugplatz schon heute.** `spawn_navdata_fetch`
   meldet beim Flugbeginn ALLE bekannten Plätze (Abflug, Ziel, Alternate)
   im SimConnect-Auftragsbuch an — mit `RANG_VORMERKUNG` (niedrigste
   Priorität). Es fehlte nur der ABHOLER: niemand las die Lieferung für
   den Abflugplatz je aus.
2. **Das Auftragsbuch ist ohnehin generisch.** `Auftragsbuch::wunsch_mit_rang`/
   `schnappschuss` trackt beliebig viele ICAOs unabhängig voneinander —
   keine Sonderbehandlung für einen weiteren Kandidaten nötig.

Für X-Plane ist die Situation noch einfacher: `szenerie_bahn::szenerie_flughafen`
ist ein synchroner, indexbasierter Nachschlag (kein Anfrage-Antwort-Zyklus)
— derselbe Aufruf wie am Touchdown, nur früher.

## Was gebaut wurde

**Neues Feld** `FlightStats::dep_szenerie_auskunft` — bewusst NICHT derselbe
Slot wie `szenerie_auskunft` (das Ziel-Feld). Grund: `szenerie_auskunft`
wird bereits ab dem ERSTEN Tick für das (dann noch geplante) Ziel
angefragt, nicht erst ab dem Sinkflug — ein gemeinsamer Slot hätte
entweder einen Wettlauf zweier Schreiber bedeutet (die Fehlerklasse, die
die Stand-/Generationsbuchführung an `szenerie_auskunft` verhindern soll)
oder die Abflug-Ernte hätte diese Buchführung für einen Fall mitschleppen
müssen, der nie einen Zielwechsel kennt (der Abflugplatz ändert sich nie
über einen Flug). Getrennt gehalten braucht die Abflug-Ernte keine
STAND-Zählung wie beim Ziel — aber eine GENERATIONS-Prüfung braucht sie
doch (`dep_szenerie_auskunft_generation`, seit Runde 1 unten): „schon da,
und aus derselben Verbindung? Dann nichts tun" ist die richtige Bedingung,
nicht bloß „schon da".

**Neue Funktion** `dep_szenerie_auskunft_uebernehmen`, aufgerufen im
selben Haupt-Tick wie die bestehende Ziel-Ernte:
- X-Plane: synchroner `szenerie_bahn::szenerie_flughafen`-Aufruf.
- MSFS: verbessert den Rang der bereits vorregistrierten Anfrage auf 0
  (statt 200) und liest `szenerie_schnappschuss` — reiner Abholer, keine
  neue Registrierung nötig.

**Erweiterte Merge-Funktion** `standliste_fuer` prüft jetzt BEIDE Felder
(`szenerie_auskunft` UND `dep_szenerie_auskunft`), jedes mit eigener
ICAO-Prüfung. Kein Aufrufer musste geändert werden — der bestehende
Boarding-Nachzieh-Tick (`standliste_fuer(&stats, &flight.dpt_airport, …)`)
bekommt die neue Quelle automatisch.

## Externe Gegenprüfung (Codex, adversarial)

`node codex-companion.mjs adversarial-review --base HEAD~1 --scope branch`,
dieselbe Schleife wie bei v1.7.15/v1.7.16.

### Runde 1 (gegen den ersten Commit) — Verdikt `needs-attention`, zwei Befunde

Beide Befunde drehten sich um dieselbe, vorher unterschätzte Tatsache: das
MSFS-Auftragsbuch ist EIN gemeinsamer Zustand, den jetzt ZWEI unabhängige
Verbraucher (Ziel-Ernte, Abflug-Ernte) berühren — nicht zwei getrennte
Bücher.

| Schwere | Befund | Reaktion |
|---|---|---|
| High | `raenge_setzen` (aufgerufen bei JEDEM Tick der Ziel-Ernte, ab Tick 1) setzt den Rang jedes Platzes, der NICHT in der Anflug-Zielliste steht, auf `RANG_UNBETEILIGT` (200) zurück — auch den Abflugplatz, der nie ein Anflug-Ziel ist. Meine Rang-Verbesserung auf 0 wurde dadurch im SELBEN Tick-Durchlauf sofort wieder rückgängig gemacht. Schlimmer: `anflug_ausrichten` gibt einen LAUFENDEN Auftrag, der kein Anflug-Ziel ist, sofort frei (`Laeuft`→`Offen`) — ein echt laufender Abflug-Auftrag könnte doppelt gestellt werden, bevor seine Antwort da ist | **Gefixt.** `Auftragsbuch` bekommt eine `_mit_schutz`-Variante (`raenge_setzen_mit_schutz`, `anflug_ausrichten_mit_schutz`) — ein `geschuetzt`-Parameter behält Rang UND laufenden Zustand für Plätze, die zwar im Buch stehen, aber keine Anflug-Kandidaten sind. Die Ziel-Ernte übergibt den Abflugplatz als geschützt. Vier neue sim-core-Tests, zwei Gegenproben (R1a, R1b) unten |
| Medium | `dep_szenerie_auskunft` prüfte nur „schon da?", nie die Buch-GENERATION. Eine neue MSFS-Verbindung (Neustart, Wechsel 2020↔2024) erhöht die Generation und kann eine andere Szenerie mitbringen — die einmal abgelegte Abflug-Kopie hätte das nie bemerkt, im Unterschied zu `szenerie_auskunft` (Ziel), die genau dafür schon eine Generations-Prüfung hat | **Gefixt.** Neues Feld `dep_szenerie_auskunft_generation`, geprüft mit der bereits vorhandenen, generischen `flugkopie_entwerten`-Funktion — keine neue Logik, nur dieselbe Zusicherung auf das neue Feld angewandt |

Beide Befunde bestätigten sich beim Nachlesen des Auftragsbuch-Codes exakt
wie beschrieben — keine Übertreibung, keine Abschwächung nötig.

## Gegenproben

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| 1 | `dep_szenerie_auskunft`-Quelle aus `standliste_fuer` entfernt | die neue Quelle muss tatsächlich abgefragt werden | ROT — `dep_szenerie_auskunft_wird_als_quelle_erkannt`, `dep_und_ziel_szenerie_bleiben_getrennt` (2 von 4 neuen Tests) |
| 2 | ICAO-Filter auf der neuen Quelle entfernt | eine Abflug-Auskunft für einen ANDEREN Platz darf nicht einspringen | ROT — `dep_szenerie_auskunft_eines_anderen_flughafens_wird_ignoriert`, `leere_dep_szenerie_staendeliste_faellt_auf_osm_zurueck` |
| 3 | Tick-Aufruf von `dep_szenerie_auskunft_uebernehmen` entfernt | die Ernte muss tatsächlich aus dem Tick heraus laufen | ROT — `dep_szenerie_auskunft_uebernehmen_wird_im_tick_aufgerufen` (nach Korrektur eines Selbsttreffer-Wächters, siehe unten) |
| R1a | Rang-Schutz in `raenge_setzen_mit_schutz` entfernt | ein geschützter Platz darf nicht auf `RANG_UNBETEILIGT` fallen | ROT — `geschuetzter_platz_behaelt_seinen_rang` (1 von 50 sim-core-Tests) |
| R1b | Laeuft-Schutz in `anflug_ausrichten_mit_schutz` entfernt | ein laufender Auftrag für einen geschützten Platz darf nicht vorzeitig freigegeben werden | ROT — `laufender_geschuetzter_auftrag_wird_nicht_vorzeitig_freigegeben` |
| R1c | Ziel-Ernte ruft wieder die ungeschützte `szenerie_anflug_ausrichten` auf | die Ziel-Ernte muss die geschützte Variante verwenden | ROT — `ziel_ernte_ruft_die_geschuetzte_ausrichtung_auf` (nach demselben Selbsttreffer-Muster korrigiert wie bei Gegenprobe 3) |

**Wächter-Falle zweimal gefangen (Gegenprobe 3 und R1c):** Beide Verdrahtungstests
suchten den jeweiligen Aufruf zunächst als EIN Textliteral — das exakt
gleiche Literal stand aber auch in der eigenen `assert!`-Zeile (zur
Dokumentation), und `include_str!` liest die eigene Testdatei mit. Der
Wächter fand sich selbst und blieb grün, auch als der echte Aufruf entfernt
wurde. Korrektur bei beiden: Vorkommen ZÄHLEN (`>= 2`, eines ist die
Doku/der Test selbst, mindestens eines muss der echte Aufruf sein) statt
reiner Existenzprüfung. Bei R1c wurde die Falle von Anfang an vermieden,
weil sie aus Gegenprobe 3 schon bekannt war.

Insgesamt elf neue Tests (vier Verhaltenstests an `standliste_fuer`, zwei
Verdrahtungs-Wächter, vier neue sim-core-Auftragsbuch-Tests, eine
Rundlauf-Bestätigung der bestehenden 50 sim-core-Tests — alle weiterhin
grün, der Umbau von `anflug_ausrichten` in einen duennen Wrapper um
`anflug_ausrichten_mit_schutz` aendert das bestehende Verhalten nicht).
Workspace 50/50 Ziele grün, Frontend `tsc -b` sauber.
`dep_szenerie_auskunft_uebernehmen` selbst ist — wie schon
`maybe_spawn_stand_fetch` in v1.7.16 — nicht isoliert testbar (Windows-only
async-Pfad für MSFS); verifiziert ist hier die Verdrahtung (Wächter), die
Merge-Logik, die die Ernte konsumiert (Verhaltenstests), und die
Auftragsbuch-Logik, die die Ernte ueberhaupt erst sicher macht
(sim-core-Tests + Gegenproben).

### Runde 2 (gegen den Runde-1-Commit) — Verdikt `needs-attention`, zwei Befunde

Beide Befunde trafen denselben blinden Fleck: Runde 1 hatte den Schutz nur
an der Stelle nachgewiesen, an der er zuerst auffiel, nicht an jeder
Stelle, an der derselbe Auftragszustand nochmal angefasst wird.

| Schwere | Befund | Reaktion |
|---|---|---|
| High | `anflug_ausrichten_mit_schutz` (Runde 1) schuetzte nur seine EIGENE, explizite `Laeuft`→`Offen`-Freigabestelle — ruft aber am Ende, wenn `fenster=true` (jeder Eintritt in Sinkflug/Anflug/Endanflug), zusaetzlich `neues_versuchsfenster()` auf: ein ZWEITER, unabhaengiger Reset-Pfad, der JEDEN `Laeuft`/`Offen`/`Wartet`/`Erschoepft`-Auftrag zuruecksetzt — geschuetzt oder nicht. Ein gerade laufender Abflug-Auftrag haette also bei jedem Phasenwechsel doch zurueckgesetzt werden koennen. Grund fuer die Luecke: Runde-1-Tests deckten nur `fenster=false` ab, nie den `true`-Zweig | **Gefixt.** Neue Funktion `neues_versuchsfenster_mit_schutz`, die geschuetzte Plaetze komplett uebergeht; `anflug_ausrichten_mit_schutz` ruft sie statt der ungeschuetzten Variante auf. Zwei neue sim-core-Tests (einer mit `fenster=true`, der die Luecke direkt schliesst), Gegenprobe unten |
| Medium | Die Generations-Entwertung (Runde 1) loeschte `dep_szenerie_auskunft`, aber ein bereits DARAUS abgeleiteter `dep_gate` (und sein Post-Riegel `dep_gate_field_posted`) blieben auf dem alten Stand stehen — der Nachzieh-Tick aktualisiert `dep_gate` nur, wenn er noch `None` ist. Ein Verbindungswechsel (Neustart, 2020↔2024) konnte die Abflug-Szenerie also neu laden, ohne einen bereits falsch abgeleiteten Stand je zu korrigieren | **Gefixt — mit Ruecksicht auf eine bestehende, bewusste Regel.** Es gibt bereits die Regel „ein einmal gestempelter `dep_gate` wird nie korrigiert" (frueherer QS-Befund: waehrend des Pushbacks steht der Flieger 30-80 m neben fremden Staenden, eine Neubewertung dort waere schlechter als der alte richtige Wert). Die neue Abhilfe darf diese Regel NICHT untergraben — sie loescht `dep_gate`/den Post-Riegel deshalb NUR, wenn (a) die Generation tatsaechlich entwertet wurde UND (b) die Phase noch `Boarding` ist (vor jedem pushback-bedingten Positionsrisiko). Extrahiert in eine eigene, pur-funktionale, testbare Funktion `dep_gate_bei_szenerie_wechsel_zuruecksetzen(phase) -> bool` — zwei neue Tests + Gegenprobe unten |

Beide Befunde bestätigten sich beim Nachlesen exakt wie beschrieben.

## Gegenproben (Runde 2)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R2a | `anflug_ausrichten_mit_schutz` ruft wieder die ungeschuetzte `neues_versuchsfenster()` statt der `_mit_schutz`-Variante auf | ein geschuetzter LAUFENDER Auftrag muss auch ein neues Versuchsfenster (`fenster=true`) ueberstehen | ROT — `laufender_geschuetzter_auftrag_uebersteht_auch_ein_neues_fenster` (51 passed; 1 failed; Restdatei danach byte-identisch wiederhergestellt) |
| R2b | `dep_gate_bei_szenerie_wechsel_zuruecksetzen` invertiert (`!=` statt `==` Boarding) | Rueckgabe muss GENAU fuer `Boarding` wahr sein, fuer jede andere Phase falsch | ROT — beide neuen Tests (`dep_gate_wird_nur_waehrend_boarding_zurueckgesetzt`, `dep_gate_bleibt_ausserhalb_boarding_stehen`) schlugen fehl (0 passed; 2 failed), Restdatei danach byte-identisch wiederhergestellt |

**Ehrlich zur Testbarkeitsgrenze:** `dep_szenerie_auskunft_uebernehmen` selbst
bleibt aus demselben Grund wie in Runde 1 nicht isoliert testbar
(Windows-only async-Pfad). Die Boarding-Bedingung des Medium-Fixes wurde
deshalb bewusst in eine reine, sim-/async-freie Funktion ausgelagert, damit
wenigstens SIE unabhaengig von der umgebenden Windows-Verdrahtung geprüft
werden kann — die Verdrahtung selbst (dass die Funktion an der richtigen
Stelle aufgerufen wird) bleibt unisoliert wie schon in Runde 1 dokumentiert.

Insgesamt vier neue Tests in dieser Runde (zwei sim-core-Auftragsbuch-Tests,
zwei reine Funktionstests in `lib.rs`), zwei Gegenproben, beide bestätigt
rot. Voller Workspace-Testlauf danach erneut grün (siehe unten).

### Runde 3 (gegen den Runde-2-Commit) — Verdikt `needs-attention`, zwei Befunde

Beide Befunde trafen wieder denselben blinden Fleck wie Runde 2, nur je
eine Ebene tiefer: Runde 2 hatte den Schutz vollstaendig gemacht, aber
weder seine RANDBEDINGUNG (was, wenn Schutz und Ziel auf denselben Platz
zeigen?) noch seine ZEITLICHE Reihenfolge (wessen Phase gilt, VOR oder
NACH `step_flight`?) geprueft.

| Schwere | Befund | Reaktion |
|---|---|---|
| High | `neues_versuchsfenster_mit_schutz` (Runde 2) nahm nur `geschuetzt` entgegen und uebersprang jeden geschuetzten Platz UNBEDINGT — auch wenn derselbe ICAO ZUGLEICH ein echtes Anflug-Ziel ist. Das trifft jeden Rueckflug zum Startflughafen und jeden Flug, bei dem der Abflugplatz auch Ausweichziel ist: der Aufrufer schuetzt den Abflugplatz IMMER (`vec![flight.dpt_airport...]`), unabhaengig davon, ob er in der Zielliste steht. Ein erschoepfter oder wartender Auftrag fuer diesen Platz haette dann NIE wieder sein Versuchsfenster bekommen — Bahn-, Rollweg- und Standdaten fuer die Landung waeren bis zum Ende unerreichbar geblieben | **Gefixt.** `neues_versuchsfenster_mit_schutz` nimmt jetzt zusaetzlich `ziele` entgegen und gibt ihnen Vorrang — genau dieselbe Rangordnung, die `raenge_setzen_mit_schutz` und die Freigabestelle in `anflug_ausrichten_mit_schutz` fuer ihre eigenen Mechanismen schon hatten, jetzt konsequent auf allen drei Stellen. Zwei neue sim-core-Tests (Ziel-Vorrang-Fall + Gegenprobe), Gegenprobe unten |
| Medium | Die Runde-2-Loeschung von `dep_gate` sass in `dep_szenerie_auskunft_uebernehmen`, die VOR `step_flight` im selben Tick laeuft — sie las also `stats.phase` des VORHERIGEN Ticks. Trifft ein Verbindungswechsel genau den Tick, in dem `step_flight` Boarding→Pushback ueberfuehrt, sah die Loeschung noch Boarding (alte Phase), loeschte `dep_gate` und den Post-Riegel — der Nachzieh-Tick lief direkt danach aber schon mit der FRISCHEN Phase (Pushback) und verweigerte die Neuableitung. Der Stand waere ersatzlos und dauerhaft verloren gewesen | **Gefixt — mit einer sauberen Verantwortungstrennung statt eines Zeitstempel-Flickens.** Neues Feld `dep_gate_generation` haelt fest, aus welcher Szenerie-Generation der aktuelle `dep_gate` stammt. Die Loeschentscheidung wandert VOLLSTAENDIG in den Nachzieh-Tick (der ohnehin schon mit der frischen `tick_phase` arbeitet, NACH `step_flight`) — `dep_szenerie_auskunft_uebernehmen` schreibt nur noch die Generation fort, ruehrt `dep_gate` nicht mehr an. Neue reine Funktion `dep_gate_muss_verworfen_werden` fasst alle drei Teilbedingungen (Boarding, vorhanden, Generation weicht ab) zusammen — vier neue Tests decken alle drei Teile einzeln ab, nicht nur die Boarding-Haelfte wie in Runde 2 |

Beide Befunde bestätigten sich beim Nachlesen exakt wie beschrieben.

## Gegenproben (Runde 3)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R3a | Zielvorrang in `neues_versuchsfenster_mit_schutz` entfernt (nur noch `geschuetzt` geprueft, wie Runde 2) | ein Platz, der ZUGLEICH Ziel und geschuetzt ist, muss sein Fenster bekommen | ROT — `ziel_das_zugleich_abflugplatz_ist_bekommt_sein_fenster` (53 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |
| R3b | Generations-Vergleich aus `dep_gate_muss_verworfen_werden` entfernt | ohne Generationswechsel darf `dep_gate` NICHT verworfen werden | ROT — `dep_gate_bleibt_ohne_generationswechsel_stehen` (5 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |
| R3c | `dep_gate_vorhanden`-Teilbedingung aus derselben Funktion entfernt | ohne vorhandenen `dep_gate` gibt es nichts zu verwerfen | ROT — `ohne_vorhandenen_dep_gate_gibt_es_nichts_zu_verwerfen` (5 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |

Insgesamt sechs neue Tests in dieser Runde (zwei sim-core-Auftragsbuch-Tests
inkl. einer Gegenprobe-Variante als eigener Test, vier reine
Funktionstests in `lib.rs` — deutlich vollstaendiger als Runde 2, die nur
ein Drittel der jetzt dreiteiligen Bedingung abdeckte), drei Gegenproben,
alle bestätigt rot. Voller Workspace-Testlauf danach erneut grün.

**Ehrlich zur Testbarkeitsgrenze (unveraendert seit Runde 1):**
`dep_szenerie_auskunft_uebernehmen` und der Nachzieh-Tick selbst bleiben
Windows-only async-Code und nicht isoliert testbar. Verifiziert ist auch
in Runde 3 nur, was sich verifizieren laesst: die pure Entscheidungslogik
(jetzt vollstaendig, alle drei Teilbedingungen einzeln gegengeprueft) und
die Auftragsbuch-Mechanik, die den Rest der Sicherheit traegt.

### Runde 4 (gegen den Runde-3-Commit) — Verdikt `needs-attention`, zwei Befunde

Beide Befunde zeigten, dass die Runde-3-Fixes zwar die ZWEI urspruenglichen
Luecken schlossen, dabei aber je eine NEUE, engere Randbedingung uebersahen
— eine typische Eigenschaft von Fixes, die eine Bedingung nur GROB
umkehren, statt die genaue Grenze des Alt-Verhaltens nachzuzeichnen.

| Schwere | Befund | Reaktion |
|---|---|---|
| High | Der Runde-3-Zielvorrang in `neues_versuchsfenster_mit_schutz` galt fuer JEDEN Zustand — auch einen GERADE ECHT LAUFENDEN (`Laeuft`) Auftrag. Bei einem Rueckflug zum Startflughafen (Abflugplatz = Ziel = geschuetzt) haette ein Phasenwechsel eine echte, unterwegs befindliche SimConnect-Anfrage auf `Offen` zurueckgesetzt — der Dispatcher haette moeglicherweise eine ZWEITE Anfrage fuer denselben Platz losgeschickt, bevor die erste Antwort da war. Genau der Fehler, den Runde 1 fuer den reinen Schutzfall schon verhindert hatte, kam ueber die neue Zielvorrang-Tuer zurueck | **Gefixt — praeziser statt grober Umkehrung.** Der Zielvorrang gilt jetzt nur noch fuer die RUHENDEN Zustaende (Offen/Wartet/Erschoepft); ein Platz, der zugleich geschuetzt, Ziel UND echt `Laeuft` ist, bleibt unberuehrt bis zur eigenen Antwort oder zum eigenen Timeout — unabhaengig davon, ob er ein Ziel ist. Neuer sim-core-Test + Gegenprobe unten |
| Medium | Die Runde-3-Loeschung von `dep_gate` (bei erkanntem Generationswechsel) loeschte SOFORT, noch bevor ein Ersatzkandidat feststand. Fand der Nachzieh-Tick in GENAU diesem Durchlauf weder aus der neuen Szenerie noch aus OSM einen Kandidaten (Positions- oder Datenlage noch nicht bereit), blieb `dep_gate` bis zum naechsten erfolgreichen Tick `None` — rollte der Flieger vorher zurueck (Pushback), war der Stand ERSATZLOS verloren, obwohl der ALTE (nur leicht veraltete) Wert oft noch richtig gewesen waere | **Gefixt durch atomaren Ersatz statt Vorab-Loeschung.** `dep_gate_muss_verworfen_werden` wurde zu `dep_gate_braucht_neuableitung` — sie loescht nichts mehr, sondern sagt nur noch, ob ein Ableitungsversuch lohnt. Die Aufrufstelle ersetzt `dep_gate`, seine Generation UND den Post-Riegel nur noch GEMEINSAM, in demselben Moment, in dem ein echter Kandidat gefunden wird. Ohne Kandidaten bleibt der alte Wert unangetastet stehen. Vier reine Funktionstests (umbenannt/erweitert), Gegenprobe unten |

Beide Befunde bestätigten sich beim Nachlesen exakt wie beschrieben.

## Gegenproben (Runde 4)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R4a | `echt_aktiv`-Ausnahme aus dem Zielvorrang entfernt (Runde-3-Stand: Ziel gewinnt immer, auch bei `Laeuft`) | ein Platz, der Ziel, geschuetzt UND echt `Laeuft` ist, darf NICHT zurueckgesetzt werden | ROT — `ziel_das_zugleich_abflugplatz_ist_bleibt_geschuetzt_solange_es_echt_laeuft` (54 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |
| R4b | `!dep_gate_vorhanden`-Vorrang aus `dep_gate_braucht_neuableitung` entfernt (Runde-3-Stand: nur Generationswechsel zaehlt) | OHNE vorhandenen `dep_gate` muss IMMER ein Ableitungsversuch lohnen | ROT — `ohne_vorhandenen_dep_gate_lohnt_immer_ein_versuch` (5 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |

Insgesamt fünf neue/umbenannte Tests in dieser Runde (ein neuer
sim-core-Auftragsbuch-Test, vier reine Funktionstests in `lib.rs` —
drei umbenannt von Runde 3, einer neu), zwei Gegenproben, beide
bestätigt rot. Voller Workspace-Testlauf danach erneut grün.

**Ehrlich zur Testbarkeitsgrenze (unveraendert):** wie in allen
vorigen Runden bleibt der eigentliche Windows-only async-Nachzieh-Tick
unisoliert; verifiziert ist die vollstaendige, jetzt vierfach
gegengeprüfte reine Entscheidungslogik und die Auftragsbuch-Mechanik.

### Runde 5 (gegen den Runde-4-Commit) — Verdikt `needs-attention`, zwei Befunde

Beide Befunde zeigten dieselbe Lehre wie Runde 4: ein Fix, der eine
Bedingung schuetzt, kann dabei eine ANDERE, bisher stillschweigend
mitgelieferte Eigenschaft verlieren. Runde 4 schuetzte den Zustand
(`Laeuft` bleibt `Laeuft`) — und verlor dabei die Fenster-GUTSCHRIFT
selbst. Runde 4 vermied das sofortige Loeschen von `dep_gate` — und
verlor dabei die Garantie, dass ein bekanntlich veralteter Wert NIE nach
aussen dringt.

| Schwere | Befund | Reaktion |
|---|---|---|
| High | Der Runde-4-Schutz liess einen Platz, der Ziel, geschuetzt UND `Laeuft` ist, WAEHREND des Fensters komplett unberuehrt — inklusive seines Versuchszaehlers. War diese laufende Anfrage bereits der LETZTE erlaubte Versuch, verlor sie die Fenster-Gutschrift fuer immer: scheiterte sie danach (Timeout/Ablehnung), stufte `freigeben_zu_kennung` sie sofort als `Erschoepft` ein, ohne dass je ein neues Fenster gewirkt haette — der tatsaechliche Landeplatz haette fuer den Rest des Anflugs ohne Szenerie dastehen koennen | **Gefixt durch Trennung der beiden Anliegen.** Der ZUSTAND bleibt `Laeuft` (keine doppelte Anfrage, Runde 4 bleibt bestehen), aber der VERSUCHSZAEHLER wird auf 0 zurueckgesetzt — die Fenster-Gutschrift wirkt jetzt, ohne eine zweite physische Anfrage auszuloesen. Ein neuer sim-core-Test bringt den Auftrag gezielt auf den letzten Versuch, laesst das Fenster waehrenddessen oeffnen und prueft, dass ein anschliessender Fehlschlag noch einen weiteren Versuch erlaubt (`Wartet` statt `Erschoepft`) |
| Medium | Der atomare Ersatz (Runde 4) verhinderte zwar das sofortige Loeschen, liess aber einen bekanntlich veralteten `dep_gate` UNBEGRENZT lange stehen, wenn waehrend des GESAMTEN restlichen Boardings nie ein Kandidat aus der neuen Generation gefunden wurde. Alle nachgelagerten Verbraucher (Live-Post, mehrere PIREP-Feld-Baustellen, Debrief-Report — mindestens sieben Fundstellen) lesen `dep_gate` OHNE die Generation zu pruefen; ein bekannt falscher Wert (z. B. nach einem 2020↔2024-Wechsel mit anderem Standlayout) waere als aktuell durchgegangen | **Gefixt mit der kleinsten sicheren Aenderung statt sieben Verbraucherstellen anzufassen.** Neue, eigene Funktion `dep_gate_muss_beim_boarding_ende_verworfen_werden` prueft ein letztes Mal beim VERLASSEN des Boardings (nicht waehrenddessen — das bleibt Sache des atomaren Ersatzes) und verwirft `dep_gate`, falls es noch nie aus der aktuellen Generation neu abgeleitet wurde. Kein Neuversuch, keine neue Korrektur waehrend/nach Pushback (die bleibt verboten) — nur das ehrliche Eingestehen "kein verlaesslicher Stand bekannt", statt einen bekannt falschen Wert an sieben Stellen weiterzureichen |

Beide Befunde bestätigten sich beim Nachlesen exakt wie beschrieben. Bei
der Medium-Abhilfe wurde bewusst GEGEN die von Codex vorgeschlagene
Alternative entschieden (Generations-Bewusstsein in jeden der sieben
Verbraucher einbauen) — das haette das Risiko getragen, genau eine
Stelle zu vergessen (dieselbe Fehlerklasse, die dieses Projekt schon
mehrfach als „der Wächter bewacht die Aufrufstellen, nicht die reparierte
Funktion" dokumentiert hat), fuer einen schmalen Randfall mit begrenzter
Eintrittswahrscheinlichkeit (Verbindungswechsel WAEHREND des Boardings,
UND keine erfolgreiche Neuableitung in JEDEM Tick bis zum Pushback).

## Gegenproben (Runde 5)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R5a | Versuchszaehler-Reset fuer den Laeuft+Ziel+geschuetzt-Fall entfernt (Runde-4-Stand: komplett unberuehrt) | ein echt laufender letzter Versuch muss die Fenster-Gutschrift bekommen, sonst scheitert er in Erschoepft statt Wartet | ROT — zwei Tests (`ein_echt_laufender_letzter_versuch_bekommt_die_fenster_gutschrift_trotzdem`, `ziel_das_zugleich_abflugplatz_ist_bleibt_geschuetzt_solange_es_echt_laeuft`) schlugen fehl (54 passed; 2 failed; Restdatei byte-identisch wiederhergestellt) |
| R5b | `!boarding_phase`-Teilbedingung aus `dep_gate_muss_beim_boarding_ende_verworfen_werden` entfernt | der Riegel darf NICHT schon waehrend des Boardings selbst greifen (das ist Sache des atomaren Ersatzes) | ROT — `dep_gate_wird_waehrend_des_boardings_selbst_nicht_ueber_diesen_riegel_verworfen` (9 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |
| R5c | `dep_gate_vorhanden`-Teilbedingung aus derselben Funktion entfernt | ohne vorhandenen `dep_gate` gibt es beim Boarding-Ende nichts zu verwerfen | ROT — `ohne_vorhandenen_dep_gate_gibt_es_beim_boarding_ende_nichts_zu_verwerfen` (9 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |

Insgesamt sieben neue Tests in dieser Runde (zwei sim-core-Auftragsbuch-
Tests, fünf reine Funktionstests in `lib.rs`), drei Gegenproben, alle
bestätigt rot. Voller Workspace-Testlauf danach erneut grün.

**Ehrlich zur Testbarkeitsgrenze (unveraendert):** wie in allen vorigen
Runden bleibt der Windows-only async-Nachzieh-Tick selbst unisoliert;
verifiziert ist die vollstaendige reine Entscheidungslogik (jetzt drei
zusammenwirkende Funktionen, jede fuer sich gegengeprüft) und die
Auftragsbuch-Mechanik.

### Runde 6 (gegen den Runde-5-Commit) — Verdikt `needs-attention`, zwei Befunde

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | Der Runde-5-Riegel `dep_gate_muss_beim_boarding_ende_verworfen_werden` bekam nur die AKTUELLE Phase — er feuerte deshalb bei JEDEM spaeteren Tick, in dem die Phase zufaellig nicht Boarding war, nicht nur beim tatsaechlichen Uebergang. Ein voellig unrelated MSFS-Reconnect Stunden spaeter (Cruise, Descent, Approach) haette einen laengst historischen, korrekten Abflug-Stand geloescht — der Flieger hatte das Gate laengst verlassen, eine Korrektur war weder noetig noch sinnvoll | **Gefixt.** Neue, eigene Funktion `boarding_wird_in_diesem_tick_verlassen` bildet den ECHTEN Uebergang aus ZWEI Phasen (vor und nach `step_flight` in demselben Tick) — wahr nur in der EINEN Tick-Ausfuehrung, in der Boarding tatsaechlich endet. Der Riegel selbst nimmt jetzt `boarding_wird_verlassen` direkt entgegen, nicht mehr die blosse Nicht-Boarding-Aussage. Drei neue Tests (Uebergang/waehrend/Stunden-danach), Gegenprobe unten |
| Medium | War der jetzt verworfene `dep_gate` schon LIVE an phpVMS gepostet (das passiert bei praktisch jedem normalen Flug beim ersten erfolgreichen Ableiten), reichte das lokale Loeschen nicht: `post_pirep_fields` ist ein Upsert, und ein WEGGELASSENES Feld (wie es die finalen PIREP-Baustellen bei `None` tun) ueberschreibt den Server-Stand nicht. Der bekanntlich veraltete Wert waere auf dem Server stehen geblieben — durch die gesamte spaetere PIREP-Einreichung hindurch, genau das, was der ganze Riegel eigentlich verhindern soll | **Gefixt durch eine ausdrueckliche Rueckzugs-Meldung statt stiller Loeschung.** War `dep_gate_field_posted` gesetzt (also wirklich schon live gepostet), sendet die Aufrufstelle jetzt einen expliziten Leerstring fuer „Departure Gate" ueber dieselbe `post_pirep_fields`-Maschinerie, die der bestehende Live-Post schon nutzt — EINMALIG, im selben Moment, in dem lokal geloescht wird |

Bei der zweiten Abhilfe wurde bewusst GEGEN Codex' Alternativvorschlag
entschieden (Live-Publikation grundsaetzlich bis zur Generations-
Bestaetigung zurueckhalten) — das haette den NORMALEN, haeufigen Fall
(sofortige, korrekte Live-Anzeige noch waehrend des Boardings) fuer alle
Piloten verzoegert, um einen seltenen Randfall abzudecken. Die gezielte
Rueckzugs-Meldung trifft nur den Randfall selbst.

## Gegenproben (Runde 6)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R6a | `boarding_wird_in_diesem_tick_verlassen` auf die alte Runde-5-Semantik zurueckgesetzt (`tick_phase != Boarding` allein, ohne die alte Phase zu pruefen) | ein Tick Stunden nach dem Boarding (Cruise→Cruise) darf NICHT als „Boarding wird verlassen" gelten | ROT — `boarding_gilt_stunden_spaeter_nicht_mehr_als_gerade_verlassen` (12 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |

Insgesamt sechs neue/umbenannte Tests in dieser Runde (drei fuer die neue
Uebergangsfunktion, drei fuer den bestehenden Riegel mit angepasster
Parameter-Semantik), eine Gegenprobe, bestätigt rot. Die Rueckzugs-Meldung
selbst ist Netzwerk-Code (async `post_pirep_fields`-Aufruf) und bleibt aus
demselben Grund wie der gesamte Nachzieh-Tick nicht isoliert testbar —
verifiziert ist die Bedingung, WANN sie ausgeloest wird
(`dep_gate_field_posted` als Zusicherung), nicht der HTTP-Aufruf selbst.
Voller Workspace-Testlauf danach erneut grün.

### Runde 7 (gegen den Runde-6-Commit) — Verdikt `needs-attention`, ein Befund

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | Die Runde-6-Rueckzugs-Meldung war ein EINZELNER Fire-and-Forget-Versuch (`post_pirep_fields` ohne Wiederholung). Ein einziger transienter Netzfehler (Timeout, 5xx) liess den bekanntlich falschen Server-Wert dann fuer den Rest des Fluges unkorrigiert stehen, ohne jede zweite Chance — genau das, was die Rueckzugs-Meldung eigentlich verhindern sollte | **Gefixt mit einem kurzen Retry, nicht mit der vollen von Codex vorgeschlagenen dauerhaften/geordneten Warteschlange.** Neue Funktion `post_pirep_fields_mit_kurzem_retry` (3 Versuche, 2s/8s Backoff) nutzt den bereits bestehenden `is_transient_pirep_error`-Klassifikator (schon vorhanden fuer `file_pirep_with_retry`) wieder — kein neuer Code fuer die Fehlerklassifikation, nur ein zweiter Anwendungsfall dafuer |

Codex' zweiter Teilbefund — die ORIGINALE Live-Meldung (die den jetzt
zurueckgezogenen Wert ueberhaupt erst postete) koennte theoretisch NACH
der Rueckzugs-Meldung eintreffen und sie „wiederbeleben" — wurde bewusst
NICHT behoben: das braeuchte serverseitige Versionierung (die es nicht
gibt) und setzt einen mehrminuetigen Netz-Stau ausgerechnet auf DER EINEN,
laengst abgeschlossenen Anfrage voraus. Dieses Restrisiko ist exakt das
bestehende, ueberall in dieser Datei akzeptierte Fire-and-Forget-Modell
fuer Live-Felder (siehe der unveraenderte `arr_gate`-Live-Post direkt
daneben) — eine dauerhafte, geordnete Warteschlange nur fuer dieses eine
Feld zu bauen waere unverhaeltnismaessiger Aufwand fuer ein derart
schmales, mit dem Rest der Datei konsistentes Restrisiko.

**Ehrlich zur Testbarkeitsgrenze (unveraendert):** die Rueckzugs-Meldung
selbst bleibt Netzwerk-Code und nicht isoliert testbar; verifiziert ist,
dass sie ueberhaupt ein Retry versucht (Wiederverwendung eines bereits
bewaehrten, andernorts getesteten Klassifikators), nicht der HTTP-Aufruf
selbst. Voller Workspace-Testlauf grün, `tsc -b` sauber.

### Runde 8 (gegen den Runde-7-Commit) — Verdikt `needs-attention`, ein Befund

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | `ApiError::RateLimited` traegt eine vom Server diktierte `retry_after_seconds` — die Runde-7-Fassung klassifizierte sie zwar korrekt als transient, ignorierte den Wert selbst aber und wartete trotzdem nur die festen 2s/8s. Bei jeder vom Server verlangten Wartezeit ueber 10s (der API-Client faellt sogar auf 60s zurueck, wenn der Header fehlt) waren alle drei Versuche VOR Ablauf der Sperre verbraucht — die Rueckzugs-Meldung gab dann genau in dem Fall auf, fuer den `RateLimited` ueberhaupt existiert | **Gefixt.** Neue, eigene Funktion `naechste_wartezeit_fuer_pirep_feld_retry` liefert bei `RateLimited` die Servervorgabe statt des festen Schemas; bei anderen transienten Fehlern weiterhin 2s/8s, bei harten Fehlern `None` (kein weiterer Versuch). Drei neue, unabhaengige Tests + eine bestaetigte Gegenprobe |

Diese vierte Runde in Folge zur selben Rueckzugs-Meldung (5, 6, 7, 8)
zeigt ein wiederkehrendes Muster: jede Abhilfe schloss die zuvor
gefundene Luecke sauber, deckte aber noch nicht die volle Bandbreite der
Fehlerklasse ab (erst „ueberhaupt ein Riegel", dann „am richtigen
Uebergang", dann „mit Retry", jetzt „mit der richtigen Wartezeit"). Das
ist erwartungsgemaess fuer ein Feature, das absichtlich in kleinen,
gegengeprueften Schritten gebaut wird — jede Runde hat die Angriffsflaeche
verkleinert, nicht vergroessert.

## Gegenproben (Runde 8)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R8a | `RateLimited`-Sonderfall aus `naechste_wartezeit_fuer_pirep_feld_retry` entfernt (faellt auf das feste 2s/8s-Schema zurueck) | `RateLimited` muss die SERVERVORGEGEBENE Wartezeit liefern, nicht das feste Schema | ROT — `rate_limited_liefert_die_servervorgegebene_wartezeit` (2 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |

**Ehrlich zur Testbarkeitsgrenze (unveraendert):** die volle Async-Schleife
(`post_pirep_fields_mit_kurzem_retry`) bleibt Netzwerk-Code und nicht
isoliert testbar; die extrahierte Wartezeit-Entscheidung — der Kern
dieser Runde — ist jetzt vollstaendig unabhaengig gegengeprüft. Voller
Workspace-Testlauf grün, `tsc -b` sauber.

### Runde 9 (gegen den Runde-8-Commit) — Verdikt `needs-attention`, ein Befund

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | `retry_after_seconds` kommt ungeprueft aus einem geparsten HTTP-Header. Ohne Obergrenze koennte ein extremer Wert (Server-Bug, kaputte Zwischenstation) die Deadline-Berechnung in `tokio::time::sleep` ueberlaufen lassen (`Instant + Duration` kann bei absurd grossen Werten paniken) oder den Task praktisch fuer immer schlafen legen — in beiden Faellen bliebe die Rueckzugs-Meldung, der einzige Korrekturweg, fuer den Rest des Fluges aus | **Gefixt.** Neue Konstante `RATE_LIMIT_WARTEZEIT_OBERGRENZE_SEC` (5 Minuten) deckelt die Servervorgabe, bevor sie als Wartezeit verwendet wird — grosszuegig genug fuer jede realistische Rate-Limit-Sperre, weit von jedem Ueberlaufrisiko entfernt. Zwei neue Tests (Extremwert `u64::MAX` gedeckelt, normaler Wert unter der Grenze unveraendert), Gegenprobe unten |

Codex' zusaetzlicher Vorschlag — eine dauerhafte, neustart-feste
Warteschlange fuer verzoegerte Rueckzugs-Meldungen — wurde bewusst NICHT
gebaut, aus derselben Abwaegung wie in Runde 7: ein Live-Anzeigefeld ist
kein kritischer Datenpfad, und das bestehende Fire-and-Forget-Modell gilt
fuer diese Feldklasse in der gesamten Datei. Die Deckelung selbst
schliesst den einzigen ECHTEN Absturz-/Haenger-Pfad vollstaendig, ohne
diese Abwaegung zu aendern.

## Gegenproben (Runde 9)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R9a | Deckelung (`.min(RATE_LIMIT_WARTEZEIT_OBERGRENZE_SEC)`) aus `naechste_wartezeit_fuer_pirep_feld_retry` entfernt | ein extremer Wert (`u64::MAX`) muss auf die Obergrenze gedeckelt werden | ROT — `extreme_rate_limit_wartezeit_wird_gedeckelt` (4 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |

Fuenfte Runde in Folge zur selben Rueckzugs-Meldung (5–9) — jede Abhilfe
hat die Angriffsflaeche weiter verkleinert (Riegel → richtiger Uebergang
→ Retry → richtige Wartezeit → sichere Grenzen fuer diese Wartezeit).
Voller Workspace-Testlauf grün, `tsc -b` sauber.

### Runde 10 (gegen den Runde-9-Commit) — Verdikt `needs-attention`, ein Befund

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | Die Runde-9-Obergrenze (5 Minuten) war viel enger als das tatsaechliche Ueberlaufrisiko — `Instant + Duration` ueberlaeuft erst bei astronomischen Werten (der volle `u64`-Sekundenbereich entspricht hunderten Milliarden Jahren). Mit nur 5 Minuten haette ein LEGITIMER, laengerer Retry-After (z. B. 15-60 Minuten) die drei Versuche VOR Ablauf der echten Sperre verbraucht — genau der Fehler, den Runde 8 beheben sollte, kam ueber eine zu enge statt eine fehlende Grenze zurueck | **Gefixt.** Obergrenze auf eine Stunde angehoben — grosszuegig genug fuer praktisch jede reale Rate-Limit-Sperre, immer noch viele Groessenordnungen vom Ueberlaufrisiko entfernt. Ein neuer Test bestaetigt, dass eine realistische 30-Minuten-Sperre unveraendert durchgereicht wird, nicht nur, dass ein Extremwert gedeckelt wird |

Dieser Befund zeigt exemplarisch, warum QS-Runden bis zum Versiegen der
Funde laufen: Runde 9s Abhilfe war in sich korrekt (schloss den
Ueberlauf-Pfad), aber die GEWAEHLTE ZAHL war zu konservativ und haette
in der Praxis haeufiger zugeschlagen als das eigentliche Risiko, das sie
verhindern sollte — ein Fix kann formal richtig UND praktisch zu
aggressiv sein.

## Gegenproben (Runde 10)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R10a | Obergrenze von 3600 zurueck auf die Runde-9-Grenze (300) gesetzt | eine realistische 30-Minuten-Sperre (1800s) darf NICHT verkuerzt werden | ROT — `eine_realistisch_lange_rate_limit_sperre_wird_nicht_verkuerzt` (5 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |

Sechste Runde in Folge zur selben Rueckzugs-Meldung (5–10). Voller
Workspace-Testlauf grün, `tsc -b` sauber.

### Runde 11 (gegen den Runde-10-Commit) — Verdikt `needs-attention`, ein Befund; Prinzipwechsel statt Zahlenanpassung

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | Mit bis zu drei Versuchen und je bis zu einer Stunde Wartezeit (Runde 10) haette die Rueckzugs-Meldung bis zu ZWEI Stunden in einem einzelnen, NICHT persistenten, abgetrennten Tokio-Task schlafen koennen. Endet der Flug oder die App in diesem Fenster — bei zwei Stunden durchaus realistisch, anders als bei den urspruenglichen Sekunden aus Runde 7 —, ist die Korrektur ersatzlos weg: genau der Fall, den diese ganze Rueckzugs-Meldung seit Runde 6 verhindern soll | **Kein neuer Zahlenwert — ein anderes Prinzip.** Ab jetzt wird eine zu lange Sperre nicht mehr GEDECKELT (das haette bei einer echten langen Sperre sofort wieder den Runde-8-Fehlschlag provoziert), sondern die Rueckzugs-Meldung GIBT AUF, sobald die Servervorgabe eine kurze Obergrenze (eine Minute) uebersteigt. Eine einzelne Konstante kann den Widerspruch "kurz genug fuer die Prozesslebensdauer, lang genug fuer jede legitime Sperre" nicht auflösen, solange der Task nicht persistiert — also wird ehrlich aufgegeben statt eine Zahl vorzutaeuschen, die diesen Widerspruch nur verdeckt |

Diese Runde markiert einen Wendepunkt in der Rueckzugs-Meldungs-Serie
(Runden 5–11): die ersten vier Runden verfeinerten dieselbe Grundidee
(Riegel → Uebergang → Retry → richtige Wartezeit → Obergrenze). Runde 11
zeigte, dass die Grundidee selbst (eine lange Sperre in einem
abgetrennten Task abwarten) an eine harte Grenze stiess, die keine
Zahlenanpassung mehr loesen konnte — der Fix ist deshalb ein
Prinzipwechsel (aufgeben statt warten), keine weitere Kalibrierung.
Codex' Alternativvorschlag — eine dauerhafte, neustart-feste
Warteschlange fuer lange Sperren — bleibt aus derselben Runde-7/9-
Abwaegung heraus bewusst aussen vor: reines Live-Anzeigefeld, kein
kritischer Datenpfad.

## Gegenproben (Runde 11)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R11a | Grenzfall-Bedingung von `<=` auf unbedingtes `None` gesetzt (jede `RateLimited`-Sperre fuehrt sofort zum Aufgeben) | eine Sperre GENAU an der Obergrenze und knapp darunter muss noch voll honoriert werden | ROT — zwei Tests (`rate_limited_liefert_die_servervorgegebene_wartezeit`, `genau_die_obergrenze_wird_noch_voll_honoriert`) schlugen fehl (4 passed; 2 failed; Restdatei byte-identisch wiederhergestellt) |

**Ehrlich zur ueberholten Runde-10-Zusicherung:** der Runde-10-Test
(„eine legitime 30-Minuten-Sperre darf nicht verkuerzt werden") wurde
nicht stillschweigend geloescht, sondern durch
`eine_lange_rate_limit_sperre_fuehrt_jetzt_zum_aufgeben_statt_zum_vollen_warten`
ersetzt — mit einem Kommentar, der dokumentiert, DASS und WARUM Runde 11
die Runde-10-Annahme ueberholt hat. Sieben Runden in Folge zur selben
Rueckzugs-Meldung (5–11). Voller Workspace-Testlauf grün, `tsc -b`
sauber.

### Runde 12 (gegen den Runde-11-Commit) — Verdikt `needs-attention`, ein Befund; Umschwenken auf bestehende dauerhafte Infrastruktur

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | Eine gueltige, laengere Rate-Limit-Sperre (uber 60s, vom Projekt selbst als legitim anerkannt) fuehrt seit Runde 11 zum sofortigen Aufgeben der Live-Rueckzugs-Meldung — ohne jeden spaeteren Nachholversuch. Da `post_pirep_fields` ein Upsert ist und die finale PIREP-Einreichung das Feld bei fehlendem lokalem Wert bisher WEGLIESS (statt zu leeren), blieb der bekanntlich veraltete Abflug-Stand auf dem Dashboard UND im fertigen, abgeschlossenen PIREP dauerhaft stehen — genau der Fall, den die gesamte Rueckzugs-Meldungs-Serie seit Runde 6 verhindern sollte | **Kein achter Tuning-Versuch an der Live-Meldung — stattdessen die vorhandene dauerhafte Infrastruktur genutzt.** `build_pirep_fields` (die Felder der FINALEN Einreichung) traegt "Departure Gate" jetzt IMMER als Schluessel, mit leerem String, wenn kein Stand (mehr) vorliegt — statt das Feld wegzulassen. Die Einreichung laeuft bereits ueber `file_pirep_with_retry` + einen persistenten `pirep_queue`-Fallback (fuer genau diesen Zweck seit Langem gebaut) — der EINZIGE in dieser Datei bereits garantiert dauerhafte, neustart-feste Zustellweg. Die Live-Rueckzugs-Meldung (Runden 6-11) bleibt als schneller Best-Effort-Pfad WAEHREND des Fluges bestehen; scheitert sie (z. B. wegen einer langen, legitimen Sperre), korrigiert die Einreichung den FINALEN, dauerhaften Datensatz garantiert |

Diese Runde beendet die Sackgasse der Runden 8–11 (Tuning einer
einzelnen Zahl/Bedingung in einem nicht-persistenten Task kann den
Widerspruch "kurz genug fuer die Prozesslebensdauer, lang genug fuer
jede legitime Sperre" strukturell nicht aufloesen). Die Loesung war
nicht mehr Tuning, sondern der Blick auf eine ANDERE, bereits bestehende
Stelle in derselben Datei, die das Problem "muss garantiert ankommen"
schon frueher fuer die Einreichung selbst geloest hatte (`pirep_queue`,
v0.5.49) — dieselbe Infrastruktur fuer denselben Zweck wiederverwenden,
statt eine zweite, neue dauerhafte Warteschlange nur fuer dieses eine
Feld zu bauen.

## Gegenproben (Runde 12)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R12a | Der unbedingte `f.insert("Departure Gate", ...)` zurueck auf die alte, bedingte `if let Some(...) { f.insert(...) }`-Form (Feld wird bei fehlendem Stand weggelassen) | ohne `dep_gate` muss "Departure Gate" als leerer String IM Feld-Set stehen, nicht fehlen | ROT — `departure_gate_feld_wird_bei_fehlendem_stand_explizit_geleert` (1 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |

Zwei neue Tests an `build_pirep_fields` (leer + echter Stand), eine
Gegenprobe, bestätigt rot. Achte Runde in Folge zur selben
dep_gate-Korrektheitsfrage (5–12), aber die erste, die eine bereits
vorhandene, garantiert dauerhafte Zustellstelle nutzt statt eine neue,
schwaechere zu bauen. Voller Workspace-Testlauf grün, `tsc -b` sauber.

### Runde 13 (gegen den Runde-12-Commit) — Verdikt `needs-attention`, zwei Befunde

| Schwere | Befund | Reaktion |
|---|---|---|
| High | Die Behauptung "garantiert dauerhaft" (Runde 12) war ueberzogen: `file_pirep_with_retry` uebergibt einen fehlgeschlagenen PIREP mit `attempt_count: 3` an die `pirep_queue`, deren Worker alle 60s genau EINMAL versucht und bei `MAX_ATTEMPTS = 50` fuer immer aufgibt (stiller Dead-Letter). Das ergibt nur noch ~47 weitere Versuche — eine Stoerung/Sperre ueber ~47 Minuten wuerde also auch die "dauerhafte" Einreichung endgueltig verlieren | **Ehrlich korrigiert, nicht behoben.** `pirep_queue`s Grenze ist eine VORBESTEHENDE, fuer die GESAMTE PIREP-Einreichung geltende Eigenschaft (seit v0.5.49, lange vor diesem Feature) — sie hier zu aendern (dauerhafter Dead-Letter, `Retry-After`-bewusstes Nachfassen) waere eine eigene, groessere Initiative fuer die ganze Einreichungs-Infrastruktur, kein Teil dieser Szenerie-Ernte-QS-Runde. Die Doku (dieser Abschnitt) nennt die Grenze jetzt explizit statt "garantiert" zu behaupten; der Befund wurde separat als Vorschlag fuer eine eigene Sitzung vorgemerkt |
| Medium | `build_pirep_fields` (Runde 12) leerte "Departure Gate" UNBEDINGT, sobald lokal kein Stand vorlag — das trifft aber auch den weit ueberwiegenden NORMALFALL (kein Stand je erkannt, kein Fehler, keine Staleness). Ein moeglicherweise von ANDERSWO gesetzter Wert (Dispatcher, anderes Tool) waere bei JEDEM solchen Flug zerstoert worden, obwohl ASA-ACARS selbst nie etwas Falsches gepostet hatte | **Gefixt.** Neues Feld `dep_gate_withdrawal_pending` (persistiert, ueberlebt einen Resume) haelt fest, dass ASA-ACARS SELBST einen jetzt veralteten Wert live gepostet hat — gesetzt exakt an der Stelle, an der die Live-Rueckzugs-Meldung (Runde 6) das ohnehin schon fuer sich selbst braucht. `build_pirep_fields` leert das Feld nur noch, wenn dieses Flag gesetzt ist; sonst bleibt es wie vor Runde 12 einfach weg |

Die Persistierung von `dep_gate_withdrawal_pending` wurde nicht von
Codex verlangt, aber aus derselben Ueberlegung ergaenzt, die Runde 11
bereits zur Kernfrage machte: ein In-Memory-Flag, das zwischen
Boarding-Ende und finaler Einreichung (potenziell Stunden) einen
App-Neustart nicht ueberlebt, haette denselben Fehler nur verschoben,
nicht behoben.

## Gegenproben (Runde 13)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R13a | Bedingung auf `dep_gate_withdrawal_pending` aus `build_pirep_fields` entfernt (Runde-12-Stand: immer leeren) | ohne anhaengigen Rueckzug darf ein fremder Wert NICHT ueberschrieben werden | ROT — `departure_gate_feld_bleibt_ohne_anhaengigen_rueckzug_unangetastet` (2 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |
| R13b | Persistierungs-Zeile (`stats.dep_gate_withdrawal_pending = self.dep_gate_withdrawal_pending;`) aus `apply_to` entfernt | das Flag muss einen Resume (Serialisieren → Deserialisieren → `apply_to`) ueberleben | ROT — `dep_gate_withdrawal_pending_ueberlebt_einen_resume` (0 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |

Vier neue Tests in dieser Runde (drei an `build_pirep_fields`, einer am
Persistierungs-Rundlauf), zwei Gegenproben, beide bestätigt rot. Neunte
Runde in Folge zur selben dep_gate-Korrektheitsfrage (5–13) — die
laengste zusammenhaengende QS-Kette dieses gesamten Features, und ein
Lehrstueck dafuer, wie viele Randbedingungen ein scheinbar einfacher
"Server-Wert korrigieren"-Pfad tatsaechlich hat: Zustand (Laeuft),
Zeitpunkt (Uebergang), Zustellung (Retry), Wartezeit (Server-Vorgabe),
Grenzen dieser Wartezeit (Ueberlauf), Prinzip bei zu langer Wartezeit
(Aufgeben), Zustaendigkeit fuer die endgueltige Korrektur (Einreichung
statt Live-Post), und schliesslich Reichweite (nur der eigene Fehler,
nicht jeder fehlende Wert) UND Haltbarkeit (Resume) dieser Korrektur.
Voller Workspace-Testlauf grün, `tsc -b` sauber.

### Runde 14 (gegen den Runde-13-Commit) — Verdikt `needs-attention`, drei Befunde

Alle drei Befunde drehten sich um dieselbe Wurzel: `dep_gate_field_posted`
(seit v1.5.5) bedeutet „ein Post wurde VERSUCHT", nicht „ein Post ist
BESTAETIGT gelungen" — fuer die urspruengliche Idempotenz-Aufgabe
(„nicht doppelt posten") war das immer harmlos, aber Runde 13 baute
darauf eine DESTRUKTIVE Entscheidung (das Feld explizit leeren), fuer
die ein blosser Versuch keine ausreichende Zusicherung ist.

| Schwere | Befund | Reaktion |
|---|---|---|
| High | `dep_gate_field_posted` wird SYNCHRON gesetzt, BEVOR die asynchrone Anfrage ueberhaupt lief, und ein Fehlschlag setzt es nie zurueck. Schlug der Live-Post fehl, waehrend jemand anderes (Dispatcher, anderes Tool) bereits einen echten Wert am Server gesetzt hatte, haette Runde 13 trotzdem `dep_gate_withdrawal_pending` gesetzt — die finale Einreichung haette den FREMDEN Wert geloescht, obwohl ASA-ACARS nie erfolgreich etwas gepostet hat. Genau die Beschaedigung, die Runde 13 verhindern sollte | **Gefixt.** Neues, persistiertes Feld `dep_gate_field_post_confirmed` wird NUR noch INNERHALB des Erfolgs-Zweigs des Live-Post-Callbacks gesetzt (nach `Ok(())`), nicht mehr optimistisch davor. Die Rueckzugs-Entscheidung stuetzt sich jetzt auf dieses bestaetigte Feld statt auf das weiterhin fuer seine urspruengliche Idempotenz-Aufgabe unveraendert bestehende `dep_gate_field_posted` |
| Medium | Da die Bestaetigung (vor diesem Fix) nur im Laufzeit-Flag `dep_gate_field_posted` lebte, haette ein App-Neustart NACH einem erfolgreichen Live-Post, aber VOR dem Boarding-Ende, die Bestaetigung verloren — ein sofortiger Boarding-Austritt nach dem Resume haette den Riegel faelschlich "nie gepostet" sehen lassen und `dep_gate_withdrawal_pending` nie gesetzt | **Gefixt durch Persistierung.** `dep_gate_field_post_confirmed` wird wie `dep_gate_withdrawal_pending` (Runde 13) in `PersistedFlightStats` mitgefuehrt und ueberlebt damit denselben Resume |
| Medium | Das Setzen von `dep_gate_withdrawal_pending = true` (Runde 13) veraenderte nur den Speicher — der naechste tatsaechliche Schreibzugriff ist der periodische Checkpoint (Sekunden bis Minuten spaeter). Ein Absturz/Neustart in diesem schmalen Fenster haette das gerade gesetzte Flag verloren, bevor es je auf die Platte kam | **Gefixt.** `save_active_flight` wird jetzt SOFORT aufgerufen, direkt nachdem das Flag gesetzt wird — noch bevor die Rueckzugs-Anfrage ueberhaupt losgeschickt wird |

## Gegenproben (Runde 14)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R14a | Persistierungs-Zeile fuer `dep_gate_field_post_confirmed` aus `apply_to` entfernt | die Bestaetigung muss einen Resume (Serialisieren → Deserialisieren → `apply_to`) ueberleben | ROT — `dep_gate_field_post_confirmed_ueberlebt_einen_resume` (0 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |

Ein neuer Test in dieser Runde, eine Gegenprobe, bestätigt rot. Die
zentrale Verhaltensaenderung (Bestaetigung nur nach `Ok(())` statt vor
dem Versuch, Sofort-Checkpoint vor dem Versand) sitzt im selben
Windows-only/async-Pfad wie der gesamte Nachzieh-Tick und bleibt aus
demselben, seit Runde 1 dokumentierten Grund nicht isoliert testbar —
verifiziert ist die PERSISTENZ der neuen Bestaetigung, nicht der
HTTP-Aufruf selbst. Zehnte Runde in Folge zur selben
dep_gate-Korrektheitsfrage (5–14). Voller Workspace-Testlauf grün,
`tsc -b` sauber.

### Runde 15 (gegen den Runde-14-Commit) — Verdikt `needs-attention`, zwei Befunde

| Schwere | Befund | Reaktion |
|---|---|---|
| High | Der Boarding-Ende-Riegel entscheidet EINMALIG, in genau EINEM Tick. War der Live-Post fuer "Departure Gate" GENAU in diesem Tick noch unterwegs (dispatcht, aber die Antwort noch nicht da), sah der Riegel `dep_gate_field_post_confirmed = false` und entschied faelschlich "nichts zu verwerfen" — auch wenn die Anfrage Sekunden spaeter erfolgreich zurueckkam. Ohne einen zweiten Boarding-Ende-Tick blieb diese verspaetete Bestaetigung folgenlos: `dep_gate_withdrawal_pending` wurde nie gesetzt, die finale Einreichung liess das Feld weg statt es zu leeren, und ein tatsaechlich von ASA-ACARS gepostetеr, mittlerweile veralteter Wert blieb dauerhaft auf dem Server und im fertigen PIREP stehen | **Gefixt durch eine nachgeholte Entscheidung im Erfolgs-Callback selbst.** Bestaetigt der Live-Post NACHTRAEGLICH (nachdem der Boarding-Ende-Tick schon gelaufen ist), prueft der Erfolgs-Callback dieselbe Bedingung (`dep_gate_muss_beim_boarding_ende_verworfen_werden`) ein zweites Mal — diesmal mit der zu diesem Zeitpunkt AKTUELLEN Phase — und holt Loeschung, Riegel-Setzung und Rueckzugs-Meldung nach, falls noetig. Idempotent (`!dep_gate_withdrawal_pending`-Wache verhindert doppelte Ausloesung). Der zweite Teilbefund derselben Fundstelle (ein Fehlschlag KOENNTE trotzdem serverseitig angekommen sein, z. B. bei einem verlorenen Antwort-Paket) bleibt bewusst unbehandelt: diese Mehrdeutigkeit ist inhaerent zu JEDEM Fire-and-Forget-HTTP-Schreibvorgang in dieser gesamten Datei (jedes andere Live-Feld hat dieselbe Eigenschaft) und nicht spezifisch fuer diesen Pfad — eine echte Loesung braeuchte serverseitige Versionierung/Ideompotenz-Schluessel, die es nicht gibt |
| Medium | Der Erfolgs-Callback setzte `dep_gate_field_post_confirmed = true` (Runde 14) nur im Speicher — der naechste tatsaechliche periodische Checkpoint war laut Code mehrere Ticks entfernt. Ein Absturz genau in diesem Fenster haette die frisch bestaetigte Zustellung verloren; ein Resume haette dann wieder "nie gepostet" angenommen | **Gefixt.** `save_active_flight` wird jetzt UNBEDINGT direkt nach dem Setzen von `dep_gate_field_post_confirmed` aufgerufen — nicht nur, wenn zusaetzlich ein nachgeholter Rueckzug noetig war |

Diese Runde loeste damit auch das strukturelle Problem, das Runde 14
noch offen liess: Runde 14 sorgte dafuer, dass NUR eine bestaetigte
Zustellung eine Loeschung autorisiert — aber der Boarding-Ende-Riegel
hatte nur EINE Gelegenheit, diese Bestaetigung zu sehen. Runde 15 gibt
ihm eine ZWEITE, nachgeholte Gelegenheit, ohne die Runde-14-Sicherheit
(nur bestaetigte Zustellungen loesen etwas aus) aufzuweichen.

**Ehrlich zur Testbarkeitsgrenze (unveraendert):** die nachgeholte
Entscheidung sitzt komplett im selben Windows-only/async-Erfolgs-
Callback wie der Rest des Nachzieh-Ticks und ist nicht isoliert
testbar — verifiziert ist die wiederverwendete, bereits mehrfach
gegengeprüfte reine Bedingungsfunktion selbst, nicht die neue
Verdrahtung. Elfte Runde in Folge zur selben dep_gate-Korrektheitsfrage
(5–15) — die mit Abstand laengste QS-Kette dieses Features. Voller
Workspace-Testlauf grün, `tsc -b` sauber.

### Runde 16 (gegen den Runde-15-Commit) — Verdikt `needs-attention`, drei Befunde; Runde-15-Fix war strukturell fehlerhaft

Alle drei Befunde trafen genau die Stelle, die Runde 15 einfuehrte —
und zeigten, dass deren Neubewertungs-Ansatz gleich zweifach falsch
war, plus eine unabhaengige Lifecycle-Luecke.

| Schwere | Befund | Reaktion |
|---|---|---|
| High | Der Runde-15-Erfolgs-Callback pruefte `dep_gate_muss_beim_boarding_ende_verworfen_werden(..., stats.dep_gate.is_some(), ...)` NEU — aber der Boarding-Ende-Riegel setzt `dep_gate` IMMER auf `None`, sobald er eine Generationsabweichung erkennt, VOELLIG UNABHAENGIG davon, ob der Post bestaetigt ist. Zum Zeitpunkt des Callbacks war `dep_gate` also IMMER schon `None` — die Neubewertung fand darum NIE etwas zum Zurueckziehen, exakt in dem Fall, den sie loesen sollte | **Gefixt durch ein eingefrorenes Flag statt einer Neubewertung.** Neues, persistiertes Feld `dep_gate_withdrawal_awaiting_confirmation` — der Boarding-Ende-Riegel FRIERT die Entscheidung "hier ist ein Rueckzug faellig" JETZT ein, wenn die Bestaetigung noch aussteht, statt sie zu verwerfen. Der Erfolgs-Callback fuehrt spaeter nur noch diese BEREITS getroffene Entscheidung aus — bewertet nichts neu |
| High | Dieselbe Neubewertung im Callback verwendete `stats.phase != FlightPhase::Boarding` statt des echten Uebergangs (die Luecke, die Runde 6 fuer den Original-Riegel schon einmal schloss). Ein voellig UNRELATED Reconnect NACH einem sauberen Boarding-Ende (ohne Generationsabweichung zu diesem Zeitpunkt) haette eine EIGENE, spaetere Generationsabweichung erzeugt — die verspaetete Neubewertung haette das faelschlich als "der verpasste Uebergang" gelesen und einen laengst historischen, korrekten Stand geloescht | **Durch dieselbe Abhilfe gefixt.** Da der Callback jetzt NUR noch das am ECHTEN Uebergangsmoment eingefrorene Flag ausfuehrt statt die Bedingung neu mit dem DANN aktuellen Generationsstand zu bewerten, kann ein spaeterer, unrelated Reconnect die Entscheidung nicht mehr rueckwirkend verfaelschen |
| High | Sowohl der Boarding-Ende-Riegel (Runde 14) als auch der Runde-15-Callback riefen `save_active_flight` UNBEDINGT auf. Der bestehende periodische Checkpoint prueft `!flight.stop` UNMITTELBAR vor jedem Schreibzugriff, mit dem dokumentierten Grund "flight_end kann seitdem gelaufen sein und die Ablage geloescht haben — sonst wird sie wiederbelebt". Beide neuen Schreibzugriffe hatten diese Wache NICHT — ein bereits gefiletes/abgebrochenes/vergessenes Fluges haette seine geloeschte Ablage wiederbelebt bekommen, potenziell sogar die eines INZWISCHEN GESTARTETEN NEUEN Fluges ueberschrieben | **Gefixt.** Beide Schreibstellen (Boarding-Ende-Riegel, Erfolgs-Callback) pruefen jetzt `!flight.stop.load(Ordering::Relaxed)` unmittelbar vor dem Schreiben — derselbe Schutz wie beim bestehenden periodischen Checkpoint |

## Gegenproben (Runde 16)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R16a | Persistierungs-Zeile fuer `dep_gate_withdrawal_awaiting_confirmation` aus `apply_to` entfernt | das eingefrorene, noch unbestaetigte Rueckzugs-Flag muss einen Resume (Serialisieren → Deserialisieren → `apply_to`) ueberleben | ROT — `dep_gate_withdrawal_awaiting_confirmation_ueberlebt_einen_resume` (0 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |

Ein neuer Test in dieser Runde, eine Gegenprobe, bestätigt rot. Die
beiden `flight.stop`-Wachen (High-Befund 3) sind reiner
Lifecycle-Schutzcode ohne eigene sinnvolle Unit-Test-Isolierung (das
Verhalten haengt vom gesamten Flug-Lebenszyklus ab) — verifiziert ist
hier, dass das Muster exakt dem bereits bestehenden, dokumentierten
Schutz am periodischen Checkpoint entspricht. Zwoelfte Runde in Folge
zur selben dep_gate-Korrektheitsfrage (5–16). Voller Workspace-Testlauf
grün, `tsc -b` sauber.

### Runde 17 (gegen den Runde-16-Commit) — Verdikt `needs-attention`, zwei Befunde; erste bewusst NICHT vollstaendig geschlossene Luecke

| Schwere | Befund | Reaktion |
|---|---|---|
| High | Das Runde-16-Flag `dep_gate_withdrawal_awaiting_confirmation` ueberlebte zwar einen Neustart — aber die EINZIGE Stelle, die es je in eine echte Rueckzugs-Meldung umsetzt, ist der Erfolgs-Callback der URSPRUENGLICHEN Anfrage, und der stirbt mit dem alten Prozess. Ein Absturz zwischen "Flag gesetzt" und "Antwort kommt zurueck" liesse das Flag fuer immer als unaufgeloesten, folgenlosen Zustand liegen — trifft die urspruengliche Anfrage server-seitig doch noch ein, bliebe der bekanntlich veraltete Stand dauerhaft stehen | **Bewusst NICHT durch eine dauerhafte Reconciliation-Infrastruktur geloest — durch ehrliches Aufgeben statt Raten.** Es gibt KEINE sichere dritte Option: automatisch zurueckziehen koennte einen fremden, nie von ASA-ACARS beruehrten Wert zerstoeren (Runde 14); automatisch ignorieren liesse einen tatsaechlich geposteten, veralteten Wert dauerhaft stehen (der urspruengliche Fehler). Ohne serverseitige Versionierung/Read-Back ist diese Ambiguitaet grundsaetzlich nicht aufloesbar. Das Flag wird beim Resume deshalb bewusst zurueckgesetzt (mit Protokollzeile), statt als ewig haengender, nie erledigter Zustand liegen zu bleiben — eine kleine Hygiene-Verbesserung, keine vollstaendige Loesung. Der Runde-16-Test, der das Ueberleben des Flags bewies, wurde nicht geloescht, sondern durch einen Test ersetzt, der die Korrektur der Korrektur dokumentiert |
| High | Beide `!flight.stop`-Wachen (Runde 16) sind selbst ein Time-of-Check-to-Time-of-Use-Rennen: zwischen dem Lesen von `flight.stop` und dem tatsaechlichen Schreiben koennte `flight_forget`/`flight_end` dazwischenfunken. Nachweislich KEINE neue, durch diese Feature-Runde eingefuehrte Schwaeche: derselbe Aufbau (Lesen, dann unbedingt schreiben, kein gemeinsames Schloss) steht bereits im LANGE bestehenden periodischen Checkpoint, mit demselben, dort schon dokumentierten Vorbehalt | **Bewusst NICHT hier behoben — als separater, systemweiter Befund vorgemerkt.** Eine echte Loesung braeuchte ein Lifecycle-Schloss ueber die GESAMTE Aktiv-Flug-Persistenz (Speichern, Loeschen, neuer Flug), nicht nur ueber die zwei in dieser Runde hinzugekommenen Stellen — eine eigene, groessere Initiative fuer die gesamte Datei, kein Teil dieser Szenerie-Ernte-QS-Runde |

## Gegenproben (Runde 17)

| # | Mutation | Zusicherung | Ergebnis |
|---|---|---|---|
| R17a | `apply_to` gibt `dep_gate_withdrawal_awaiting_confirmation` wieder unveraendert weiter (Runde-16-Stand) statt es zurueckzusetzen | ein ueber einen Neustart hinweg nie aufloesbares Flag muss beim Resume zurueckgesetzt werden | ROT — `dep_gate_withdrawal_awaiting_confirmation_wird_beim_resume_bewusst_zurueckgesetzt` (0 passed; 1 failed; Restdatei byte-identisch wiederhergestellt) |

Dreizehnte Runde in Folge zur selben dep_gate-Korrektheitsfrage (5–17).
Diese Runde ist die erste, in der zwei Befunde bewusst NICHT vollstaendig
geschlossen wurden — beide mit ausfuehrlich begruendetem Grund
(grundsaetzliche Unaufloesbarkeit ohne Serverinfrastruktur; vorbestehende,
systemweite statt feature-spezifische Schwaeche) statt aus Bequemlichkeit.
Beide sind als separate Vorschlaege vorgemerkt. Voller Workspace-Testlauf
grün, `tsc -b` sauber.

### Runde 18 (gegen den Runde-17-Commit) — Verdikt `needs-attention`, ein Befund; bewusst nicht weiter verfolgt

Codex verlangte im Kern dasselbe wie in Runde 17 bereits ausfuehrlich
begruendet abgelehnt: eine dauerhafte Reconciliation fuer den Fall, dass
der urspruengliche Live-Post server-seitig ankommt, der Prozess aber
VOR der lokalen Bestaetigung abstuerzt. Diesmal mit einem konkreteren
Alternativvorschlag: „wenn keine Reconciliation moeglich ist, den
unaufgeloesten Zustand fuer eine explizite Nutzer-/Admin-Entscheidung
sichtbar machen, statt ihn stillschweigend zu loeschen."

**Dieser Vorschlag wurde ernsthaft geprueft, nicht nur zurueckgewiesen.**
`docs/spec/v0.13.0-mid-session-integrity-and-resume-policy.md` zeigte
zunaechst vielversprechend aus, ist bei genauerem Lesen aber NICHT
dieselbe Problemklasse: v0.13.0 behandelt Telemetrie-Luecken,
Sim-Zustands-Resume-Klassifikation und die Integritaet der FSM-/Score-
Berechnung — nicht die Zustellungs-Ambiguitaet eines einzelnen
`post_pirep_fields`-Aufrufs. **Runde 19 (Codex, zu Recht) korrigierte
diese anfaengliche Fehlzuordnung** — die Klasse „unaufgeloester
`/fields`-POST nach Absturz" ist durch KEINE bestehende Spezifikation
abgedeckt.

**Entscheidung: weiterhin nicht in dieser Runde umgesetzt, aber jetzt
korrekt als eigener, ungeloester Punkt vorgemerkt statt einem
bestehenden Vorhaben faelschlich zugeschrieben.** Der in Runde 17
dokumentierte Kompromiss (Flag beim Resume zuruecksetzen +
protokollieren, statt stillschweigend haengen zu lassen) bleibt
bestehen. Das Restrisiko bleibt offen und ehrlich benannt: verlaesst ein
Prozess das Boarding-Ende mit einer Generationsabweichung, ist der
zugehoerige Live-Post noch unterwegs, UND stuerzt der Prozess vor dessen
Antwort ab, kann ein tatsaechlich erfolgreich geposteter, jetzt
veralteter "Departure Gate"-Wert dauerhaft auf dem Server und im
fertigen PIREP stehen bleiben — die Verkettung von VIER unabhaengigen,
je seltenen Ereignissen. Fuer ein Best-Effort-Live-Anzeigefeld (kein
kritischer Datenpfad, siehe die durchgaengige Abwaegung seit Runde 7)
wird dieses Restrisiko als vertretbar akzeptiert — mit einem echten
Eintrag unter „Was noch aussteht" weiter unten, nicht nur in dieser
historischen Runden-Erzaehlung (Codex, Runde 20, hatte zu Recht bemaengelt,
dass „vorgemerkt" ohne greifbaren Eintrag im Dokument eine leere
Behauptung ist). Eine echte Loesung braucht serverseitige Versionierung/
Read-Back oder eine explizite Nutzer-/Admin-Oberflaeche, beides eigene,
groessere Vorhaben.

### Runde 19 (gegen den Runde-18-Commit) — Verdikt `needs-attention`, ein Befund; Dokumentations-Korrektur

| Schwere | Befund | Reaktion |
|---|---|---|
| High | Die Runde-18-Doku behauptete, `v0.13.0-mid-session-integrity-and-resume-policy.md` decke dieselbe Problemklasse ab und eine gezielte Abhilfe wuerde diese Arbeit doppeln. Beim genauen Lesen der Spec stimmt das nicht: v0.13.0 behandelt Telemetrie-Luecken, Sim-Zustands-Resume-Klassifikation und FSM-/Score-Integritaet — nicht die Zustellungs-Ambiguitaet eines einzelnen HTTP-POSTs. Die Doku machte damit ein ungeloestes Risiko faelschlich als "bereits anderswo geplant" erscheinen | **Korrigiert.** Der betroffene Abschnitt (Runde 18) wurde berichtigt: die Zuordnung zu v0.13.0 entfernt, das Restrisiko weiterhin offen und ehrlich benannt, jetzt als eigener, ungeloester Punkt (nicht Teil eines bestehenden Vorhabens) vorgemerkt |

**Kein Code-Aenderung in dieser Runde** — der zugrunde liegende
technische Befund (Runde 17/18) bleibt unveraendert unentschieden;
korrigiert wurde nur eine ungenaue Begruendung dafuer, WARUM er nicht in
dieser QS-Runde behoben wird.

### Runde 20 (gegen den Runde-19-Commit) — Verdikt `needs-attention`, ein Befund; fehlender greifbarer Eintrag

| Schwere | Befund | Reaktion |
|---|---|---|
| High | Runde 19 sprach von einem „separaten Vorschlag", ohne dass im Repository irgendein greifbares Artefakt (Issue, Backlog-Eintrag, Abschnitt) dafuer existierte — auch der eigene „Was noch aussteht"-Abschnitt dieses Dokuments erwaehnte den Punkt nicht. Ein Risiko, das nur in der historischen Runden-Erzaehlung eines QS-Dokuments steht, kann aus zukuenftiger Planung stillschweigend verschwinden | **Korrigiert.** Ein konkreter, dauerhafter Eintrag unter „Was noch aussteht" weiter unten beschreibt den Befund, seine Voraussetzungen und den Loesungsbedarf — kein Verweis mehr auf eine nur session-interne Vormerkung |

Sechzehnte Runde in Folge zur selben dep_gate-Korrektheitsfrage (5–20)
— die dritte davon ohne Code-Aenderung, sondern zur Praezisierung, wie
ehrlich diese Dokumentation ihre eigenen offenen Punkte tatsaechlich
festhaelt.

### Runde 21 (gegen den Runde-20-Commit) — Verdikt `needs-attention`, ein Befund (Medium); Grenze der QS-Runde erreicht

| Schwere | Befund | Reaktion |
|---|---|---|
| Medium | Der Runde-20-Eintrag haelt das Risiko fest, aber ohne Eigentuemer, Prioritaet oder Ziel-/Revisionsdatum — ein Risiko ohne verantwortliche Stelle kann trotzdem aus der Planung fallen | **Nicht durch die QS-Runde selbst aufloesbar.** Eigentuemerschaft, Prioritaet und Zieltermin fuer ein akzeptiertes Restrisiko sind eine Produktentscheidung, keine technische — sie zu erfinden waere selbst eine unverifizierte Behauptung. Der Eintrag wurde stattdessen als FORMALE Risikoakzeptanz mit Datum umformuliert, die Entscheidung ueber Eigentuemerschaft/Prioritaet ausdruecklich an Thomas verwiesen, und alle drei aus dieser Session hervorgegangenen Vorschlaege (pirep_queue-Grenze, `save_active_flight`-Wettlaeufe, diese Reconciliation-Luecke) explizit benannt |

**Ende der automatisierten Runden zu diesem einen Punkt.** Ab Runde 17
drehten sich fuenf aufeinanderfolgende Runden (17-21) um dieselbe,
inhaerent unaufloesbare Zustellungs-Ambiguitaet — zunaechst echte
Code-Luecken (17), dann zunehmend Dokumentations-/Prozessfragen (18-21),
zuletzt eine reine Eigentuemerschafts-/Prioritaets-Frage, die keine
weitere Code- oder Text-Iteration mehr aufloesen kann, weil sie
grundsaetzlich eine menschliche Entscheidung ist. Die QS-Schleife wird
hier fuer diesen spezifischen Punkt beendet und dem Nutzer zur
Entscheidung vorgelegt, statt weiter im Kreis zu drehen.

### Nachtrag (05.09.2026): Vorschlag b umgesetzt — sichtbare Warnung beim Resume

Thomas aktivierte den `spawn_task`-Vorschlag fuer diese Luecke direkt
(nicht als eigene Sitzung). Umgesetzt wurde **Vorschlag b** aus dem
Auftrag: `try_resume_flight` schreibt jetzt einen sichtbaren Activity-
Log-Eintrag ("Abflug-Stand: Zustellung nach Neustart unklar"), wenn
`dep_gate_withdrawal_awaiting_confirmation` einen Resume unaufgeloest
ueberlebt hat — vorher gab es dafuer nur eine `tracing::warn!`-Zeile, die
kein Pilot je zu sehen bekam. **Vorschlag a** (Server-Read-Back ueber
`get_pirep`/`PirepFull`) wurde NICHT umgesetzt: phpVMS liefert ueber den
bestehenden `GET /api/pireps/{id}`-Endpunkt nachweislich keine Custom-
Field-Werte (`PirepFull` laesst sie bewusst aus, siehe deren
Dokumentation in `crates/api-client/src/lib.rs`), und dessen JSON-Form
zu ERRATEN haette gegen die eigene Regel „keine unverifizierten
Behauptungen" verstossen. Die formale Risikoakzeptanz oben bleibt
inhaltlich unveraendert — das Restrisiko ist jetzt nur nicht mehr
UNSICHTBAR fuer den Piloten. Wiring-Waechter-Test:
`resume_meldet_unklare_dep_gate_zustellung_sichtbar`.

## Was noch aussteht

* Der reale Flug — insbesondere ob der MSFS-Abholer die vorregistrierte
  Lieferung rechtzeitig vor dem Pushback bekommt.
* Weitere Runden externer Gegenprüfung (Codex, adversarialer Modus), bis
  keine neuen Befunde mehr kommen.
* **Formale Risikoakzeptanz (Stand 05.09.2026, Runde 17-21) — Entscheidung
  ueber Prioritaet/Zieltermin steht bei Thomas aus:** stuerzt der Prozess
  ab, WAEHREND der Live-Post fuer "Departure Gate" noch unterwegs ist
  (nach einer am Boarding-Ende erkannten Generationsabweichung), kann die
  Zustellungs-Ambiguitaet nie mehr aufgeloest werden — der Callback, der
  sie aufloesen wuerde, stirbt mit dem Prozess. Auf Resume wird das Flag
  bewusst zurueckgesetzt (nicht geraten), protokolliert, aber NICHT
  automatisch korrigiert. Eine echte Loesung braucht entweder
  serverseitiges Read-Back/Versionierung von PIREP-Custom-Fields (aktuell
  nicht in `get_pirep`/`PirepFull` vorhanden) oder eine explizite Nutzer-/
  Admin-Oberflaeche fuer unaufgeloeste Zustellungen — NICHT abgedeckt
  durch `docs/spec/v0.13.0-mid-session-integrity-and-resume-policy.md`
  (siehe Runde 19, dort zunaechst faelschlich zugeordnet). Voraussetzung
  fuer den Schadensfall: die Verkettung von VIER unabhaengigen, je
  seltenen Ereignissen (Reconnect mit Generationsabweichung waehrend des
  Boardings; ein Live-Post genau dabei in der Luft; ein Prozessabsturz vor
  dessen Antwort; die Anfrage kam server-seitig trotzdem an). Die
  Ingenieurs-Einschaetzung dieser QS-Runde (5-21): fuer ein Best-Effort-
  Live-Anzeigefeld vertretbar, waehrend die FINALE PIREP-Einreichung durch
  einen unabhaengigen, bereits dauerhaften Mechanismus geschuetzt bleibt
  (Runde 12) — **aber Eigentuemer, Prioritaet und Zieltermin sind eine
  Produktentscheidung, keine technische, und werden hier bewusst nicht
  erfunden.** Drei konkrete Vorschlaege liegen aus dieser Session bereit
  (pirep_queue-Warteschlangen-Grenze, `save_active_flight`-Wettlaeufe,
  diese Reconciliation-Luecke) und warten auf Thomas' Priorisierung.

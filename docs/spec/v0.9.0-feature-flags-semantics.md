# v0.9.0 — Feature-Flags-Semantik (Defaults, Persistenz, Kill-Switch)

**Status:** PROPOSAL — Erweiterung des Telemetry-Contracts Sektion 7
**Datum:** 2026-05-18
**Quelle:** QS-Round-3-Befund („Feature-Flags-Tabelle ist Kurzform — Defaults, Persistenz, UI-Sichtbarkeit, Migration, Kill-Switch und Daten-Verhalten bei Disable fehlen pro Flag")
**Ziel:** Pro Flag aus `v0.9.0-telemetry-contract.md` Sektion 7 vollständig präzisieren: Default-Wert, Persistenzort, UI-Sichtbarkeit, Remote-vs-Local, Migrations-Verhalten v0.8.4 → v0.9.0, Verhalten bei Disable nach bereits erzeugten Daten und Kill-Switch-Pfad.

## Pflicht-Vorgabe — Cross-Reference

Dieses Dokument ist **KEINE Konsumenten-Spec** sondern eine **Erweiterung** des Telemetry-Contracts. Konkret:

- **Referenz-Dokument:** `docs/spec/v0.9.0-telemetry-contract.md` Sektion 7 „Feature-Flags / Rollout-Gates" (8 Flags in tabellarischer Kurzform)
- **Verbindlichkeits-Reihenfolge:** Telemetry-Contract > diese Spec > Detail-Specs. Diese Spec präzisiert die Kurzform-Tabelle, ändert aber keine Flag-Namen oder Defaults der Kurzform-Tabelle. Wenn ein Detail in dieser Spec mit dem Contract kollidiert, ist der Contract autoritativ.
- **Synchronisations-Pflicht:** Jede Änderung an der Flag-Tabelle im Contract muss diese Spec im SELBEN PR aktualisieren. Andernfalls Roll-Back.
- **Konsumenten:** Diese Spec wird vom Implementierungs-Sprint für die Settings-UI gelesen, von der QS für Test-Cases und von der Release-Pipeline für Migrations-Smoke-Tests.

## Kurzfassung — warum Kill-Switch-Semantik kritisch ist

Die 8 v0.9.0-Feature-Flags steuern Verhalten, das in zwei Klassen kritisch werden kann:

1. **Externe Side-Effects mit Datenschutz-Auswirkung** — GlitchTip und Discord-RPC senden Daten an Dritt-Systeme (eigener VPS bzw. Discord-Client). Wenn dort ein Bug auftritt (z.B. „Discord-RPC sendet pilot_id ungehasht trotz Anonymisierungs-Flag"), darf das nicht erst beim nächsten App-Release gefixt werden. Pilot oder VA-Admin braucht einen Notfall-Pfad „aus binnen 5 Minuten".
2. **Datenerzeugung mit Konsistenz-Wirkung** — Per-Bounce und Expanded-Phases erzeugen neue Felder in PIREPs. Wenn ein Flag mid-Sprint ausgeschaltet wird (z.B. „GO_AROUND falsch detektiert, deshalb 50 phantomäre go_around_events in der Webapp"), darf das keine Mischzustands-Datenkorruption verursachen. Klar definierte „Disable nach erzeugten Daten"-Semantik schützt den historischen PIREP-Bestand.

Ohne explizite Semantik passieren typische Bugs der Klasse:
- „Pilot hat in Settings GlitchTip ausgeschaltet, aber app sendet weiter weil Settings-Cache stale ist"
- „Per-Bounce-Flag wurde am Tag 3 ausgeschaltet, alte PIREPs zeigen leere bounces[] obwohl Daten da sind"
- „Discord-RPC-Bug am Live-Release-Tag, kein Kill-Switch ausser neuem Release"

Diese Spec schreibt diese Pfade verbindlich vor.

## Allgemeine Prinzipien

### P1. Single Source of Truth pro Flag

Jeder Flag hat **GENAU EINEN Speicherort** und **GENAU EINE Lese-Funktion**. Mehrere Caches sind verboten. Konkret:

- Lese-Funktion: `feature_flag::get<T>(key: &str) -> T` (Rust) bzw. `useFeatureFlag<T>(key)` (React)
- Schreibe-Funktion: `feature_flag::set<T>(key: &str, value: T) -> Result<(), Error>` (Rust)
- Beide Funktionen sind die EINZIGEN Pfade — Komponenten dürfen den Speicherort nicht direkt lesen.
- Begründung: Wenn ein Flag mid-Flight geändert wird, ist garantiert dass der nächste `get` den neuen Wert liefert (= keine Memoization-Inkonsistenz).

### P2. Persistenz-Strategie — `config.json` als zentrale Datei

**Entscheidung:** Wir verwenden die bestehende `config.json` im Tauri-AppData-Pfad (`%APPDATA%/ASA-ACARS/config.json` unter Windows, `~/Library/Application Support/ASA-ACARS/config.json` unter macOS). Keine separate `feature-flags.json`.

**Begründung:**
- **Pro `config.json` zentral:** Bestehende Settings-UI liest/schreibt bereits dort (SimBrief-Settings, MQTT-Config). Pilot findet alle Optionen an einer Stelle. Backup ist eine Datei. Migration ist ein Schema-Bump.
- **Contra separate `feature-flags.json`:** Würde Tooling verdoppeln (zwei JSON-Schemas, zwei Validierungen, zwei Backup-Pfade) ohne klaren Nutzen. Power-User wollen einen Editor öffnen, nicht zwei.
- **Power-User-Edit:** Datei ist plain JSON mit Schema-Version. Edit-with-Notepad ist unterstützt (Notfall-Pfad K2 unten).

**Schema-Versionierung:** `config.json` bekommt ein top-level Feld `"schema_version": 2` (v0.8.4 hatte implizit Version 1 ohne expliziten Marker). Beim ersten Start nach v0.9.0-Upgrade läuft ein Migrator (siehe Sektion „Migration-Pfade") der Schema 1 → Schema 2 transformiert und das Backup `config.json.v1.bak` schreibt.

### P3. Migration-Strategie — Conservative Inherit + Explicit New Defaults

Bei Upgrade von v0.8.4 auf v0.9.0:

- **Bestehende Schlüssel** (SimBrief, MQTT, Approach-Stable-Config) werden **verlustfrei übernommen** — keine Defaults überschrieben.
- **Neue Flags** werden mit dem im Contract definierten Default geschrieben. Flag-Werte aus dem 8er-Set sind im Backup nicht vorhanden (= waren in v0.8.4 hartkodiert oder gar nicht existent).
- **Unbekannte Schlüssel** im alten config.json bleiben bei der Migration erhalten (= forward-compat falls jemand ein Feld manuell ergänzt hat).
- **Schema-Bump:** `schema_version` wird von 1 → 2 erhöht NUR wenn die Migration ohne Fehler durchläuft. Bei Migrator-Crash bleibt v1 und v0.9.0-Start scheitert mit klarer Fehlermeldung im Activity-Log + Modal.

### P4. Notfall-Pfade (Kill-Switch-Klassifikation)

Drei Klassen von Kill-Switches mit unterschiedlicher Latenz und Berechtigungs-Anforderung:

| Klasse | Pfad | Latenz | Berechtigung | Use-Case |
|---|---|---|---|---|
| **K1** | Settings-UI-Toggle | <5s wirksam | Pilot selbst | Normaler Opt-out |
| **K2** | `config.json` Edit + App-Restart | 1-2 Min | Pilot selbst (Power-User) | UI-bug oder Pre-Crash-Disable |
| **K3** | VA-Discord-Broadcast „bitte jetzt ausschalten" | 5-30 Min (manuell, Pilot-abhängig) | VA-Admin | Live-Bug-Notfall ohne Remote-Channel |

**Bewusst NICHT enthalten in v0.9.0:** Remote-Push-Channel (z.B. „VA-Admin setzt Flag für alle Pilots gleichzeitig"). Siehe „Out of Scope" — Begründung: Remote-Push würde Auth + Push-Channel-Mgmt + Acknowledgment-Tracking + Conflict-Resolution erfordern, das ist eine Spec für sich. Der manuelle Pfad K3 reicht für die VA-Größe (3-15 aktive Pilots, alle in einem Discord erreichbar).

### P5. Defensive Defaults

Bei Konflikt zwischen „Convenience-für-Pilot" und „Privacy-Risiko" gilt:

- Privacy-relevante Flags (= externes Daten-Pfad: GlitchTip, Discord-RPC) sind **default off**, explizites Pilot-Opt-In.
- Convenience-Flags (= reine Pilot-Client-Verhaltens-Optimierung: Delta-Sampling, Expanded-Phases, Per-Bounce) sind **default on**.
- Anonymisierungs-Flags sind **default on** (= konservative Wahl: lieber zu wenig Tracking als zu viel).

## Pro-Flag-Sektionen

### F1. `position_sampling.delta_enabled` — Delta-Sampling

**Detail-Spec:** `v0.9.0-delta-position-sampling.md`

| Aspekt | Wert |
|---|---|
| **Default Fresh-Install v0.9.0** | `true` |
| **Default bei Upgrade von v0.8.4** | `true` (neuer Key, in v0.8.4 nicht existent) |
| **Persistenzort** | `config.json` → `position_sampling.delta_enabled` |
| **UI-Sichtbarkeit** | Standard-User-Tab „Verbindung & Stream" — sichtbar, kein Power-User-Mode nötig |
| **Remote/Local** | Lokal pro Pilot. Kein Remote-Pfad in v0.9.0 |
| **Wirksamkeits-Latenz** | Sofort (= kein App-Restart). Streamer-Tick liest Flag bei jedem Tick |
| **Warnung im UI** | „Standard ist EIN. Ausschalten erhöht Bandwidth und Recorder-DB-Wachstum um Faktor ~5" |

**Migration bei Upgrade v0.8.4 → v0.9.0:**
- Key existiert in v0.8.4 nicht → wird beim ersten v0.9.0-Start mit `true` geschrieben (= neuer Default)
- Pilot, der explizit v0.8.x-Verhalten will, kann nach Upgrade auf `false` setzen
- Activity-Log beim ersten Start: „Delta-Sampling aktiviert (neu in v0.9.0). Settings → Verbindung & Stream"

**Verhalten bei Disable nach bereits erzeugten Daten:**

| Datenklasse | Was passiert | UI-Sicht Pilot-Client | UI-Sicht Webapp |
|---|---|---|---|
| Bereits gesendete Position-Samples in Recorder-DB | Bleiben unverändert (sparse) | n/a (Stream-Daten) | Live-Map zeigt alte sparse Samples genauso wie vorher |
| Zukünftige Samples | Fixed-Rate-Tick alle 5-8s | n/a | Live-Map dichter |
| Historische PIREPs | unverändert | unverändert | unverändert |

→ **Daten werden nicht gelöscht.** Mischzustand (= sparse historische + dense neue Samples nach Disable) ist akzeptabel weil die Position-Stream-Daten temporär sind (30 Tage Retention).

**Kill-Switch-Pfad:**
- **K1 (UI):** Settings → Verbindung & Stream → Toggle „Delta-Sampling". Sofort wirksam, kein Restart
- **K2 (config.json):** `position_sampling.delta_enabled = false`. App-Restart nicht zwingend, aber empfohlen damit der Streamer den Wert neu lädt
- **K3 (Notfall):** „Alle Pilots: bitte in Settings Delta-Sampling ausschalten" — typisch wenn Recorder-DB-Schema-Mismatch im Server auftritt

---

### F2. `phases.rejected_take_off_enabled` — RTO-Phase

**Detail-Spec:** `v0.9.0-flight-phases-expanded.md`

| Aspekt | Wert |
|---|---|
| **Default Fresh-Install v0.9.0** | `true` |
| **Default bei Upgrade von v0.8.4** | `true` |
| **Persistenzort** | `config.json` → `phases.rejected_take_off_enabled` |
| **UI-Sichtbarkeit** | Erweitert-Tab → „Flugphasen-Erkennung" — sichtbar |
| **Remote/Local** | Lokal pro Pilot |
| **Wirksamkeits-Latenz** | Sofort. StateMachine liest Flag pro Phase-Check |
| **Warnung im UI** | „Standard ist EIN. Bei AUS sieht der Pilot weiterhin `TAKEOFF` auch wenn er den Start abbricht — kein RTO-Event im PIREP" |

**Migration bei Upgrade v0.8.4 → v0.9.0:**
- Neuer Key → Default `true`. Backward-Kompat: alte PIREPs ohne `rto_event` bleiben gültig (`Option<RtoEvent>` mit `serde(default)`)

**Verhalten bei Disable nach bereits erzeugten Daten:**

| Datenklasse | Was passiert | UI-Sicht Pilot-Client | UI-Sicht Webapp |
|---|---|---|---|
| Bereits erzeugte `rto_event` in PIREPs | Bleiben in payload_json (=keine Löschung) | LandingPanel zeigt alte RTOs weiterhin (Lese-Pfad ist unconditional) | Webapp zeigt alte RTOs weiter |
| Zukünftige RTOs | KEINE Detection — StateMachine springt direkt von `TAKEOFF_ROLL` → `TAXI_IN` (= legacy v0.8.x) | Keine Phase-Pill „RTO" | dito |
| Zukünftige PIREPs | Kein `rto_event`-Feld serialisiert | dito | dito |

→ **Keine Daten-Löschung.** Historische Detections bleiben les- und sichtbar.

**Kill-Switch-Pfad:**
- **K1:** Settings → Erweitert → Flugphasen → Toggle „Rejected Take-Off erkennen"
- **K2:** `phases.rejected_take_off_enabled = false`
- **K3:** Typischer Trigger wäre „RTO wird zu oft falsch detektiert (= Pilot wechselt Runway → RTO?)". Discord-Broadcast „bitte ausschalten bis Fix"

---

### F3. `phases.go_around_enabled` — GO_AROUND-Phase

**Detail-Spec:** `v0.9.0-flight-phases-expanded.md`

| Aspekt | Wert |
|---|---|
| **Default Fresh-Install v0.9.0** | `true` |
| **Default bei Upgrade von v0.8.4** | `true` |
| **Persistenzort** | `config.json` → `phases.go_around_enabled` |
| **UI-Sichtbarkeit** | Erweitert-Tab → „Flugphasen-Erkennung" — sichtbar |
| **Remote/Local** | Lokal pro Pilot |
| **Wirksamkeits-Latenz** | Sofort |
| **Warnung im UI** | „Standard ist EIN. Bei AUS bleibt der Pilot in `APPROACH`/`FINAL` auch bei Pull-Up — keine go_around_events im PIREP" |

**Migration bei Upgrade v0.8.4 → v0.9.0:** Neuer Key → Default `true`.

**Verhalten bei Disable nach bereits erzeugten Daten:**

| Datenklasse | Was passiert | UI-Sicht Pilot-Client | UI-Sicht Webapp |
|---|---|---|---|
| Bereits erzeugte `go_around_events` in PIREPs | Bleiben in payload_json | Lese-Pfad unconditional → Anzeige unverändert | Anzeige unverändert |
| Zukünftige GO_AROUNDs | Kein State-Wechsel zu `GO_AROUND` | Phase bleibt `APPROACH`/`FINAL` während Pull-Up | dito |
| Zukünftige PIREPs | Kein `go_around_events`-Feld | dito | dito |

**Kill-Switch-Pfad:**
- **K1:** Settings → Erweitert → Flugphasen → „Go-Around erkennen"
- **K2:** `phases.go_around_enabled = false`
- **K3:** „GO_AROUND triggert bei jedem Stable-Glideslope-Restart" → Broadcast

---

### F4. `phases.deboarding_enabled` — DEBOARDING-Phase

**Detail-Spec:** `v0.9.0-flight-phases-expanded.md`

| Aspekt | Wert |
|---|---|
| **Default Fresh-Install v0.9.0** | `true` |
| **Default bei Upgrade von v0.8.4** | `true` |
| **Persistenzort** | `config.json` → `phases.deboarding_enabled` |
| **UI-Sichtbarkeit** | Erweitert-Tab → „Flugphasen-Erkennung" — sichtbar |
| **Remote/Local** | Lokal pro Pilot |
| **Wirksamkeits-Latenz** | Sofort |
| **Warnung im UI** | „Standard ist EIN. Bei AUS überspringt die State-Machine `DEBOARDING` → `ARRIVED` direkt bei Block-Snapshot — sonst wartet sie auf Parking-Brake + Engines-Off" |

**Migration bei Upgrade v0.8.4 → v0.9.0:** Neuer Key → Default `true`.

**Verhalten bei Disable nach bereits erzeugten Daten:**

| Datenklasse | Was passiert | UI-Sicht Pilot-Client | UI-Sicht Webapp |
|---|---|---|---|
| Bestehende `phase=DEBOARDING`-Einträge im flight_events | Bleiben in DB | Activity-Log zeigt alte DEBOARDING-Phasen | Phase-Verlauf zeigt sie weiter |
| Zukünftige Flüge | Direkt `ARRIVED` → `BLOCKS_ON` | Kein DEBOARDING-Pill | dito |

**Kill-Switch-Pfad:** wie F2/F3, plus Notfall-Pfad „PIREP wird zu früh gefiled weil DEBOARDING zu kurz" → Toggle aus.

---

### F5. `forensics.per_bounce_enabled` — Per-Bounce JSON-Detail

**Detail-Spec:** `v0.9.0-per-bounce-detail.md`

| Aspekt | Wert |
|---|---|
| **Default Fresh-Install v0.9.0** | `true` |
| **Default bei Upgrade von v0.8.4** | `true` |
| **Persistenzort** | `config.json` → `forensics.per_bounce_enabled` |
| **UI-Sichtbarkeit** | Erweitert-Tab → „Landungs-Forensik" — sichtbar |
| **Remote/Local** | Lokal pro Pilot |
| **Wirksamkeits-Latenz** | Sofort. Touchdown-Detector liest Flag beim Touchdown-Edge |
| **Warnung im UI** | „Standard ist EIN. Bei AUS verlierst du die Detail-Records pro Bounce — Counter (`bounce_count`) bleibt erhalten (= v0.8.x-Verhalten)" |

**Migration bei Upgrade v0.8.4 → v0.9.0:** Neuer Key → Default `true`. Touchdown-Detector arbeitet weiter wie in v0.8.x wenn Flag = false.

**Verhalten bei Disable nach bereits erzeugten Daten:**

Dies ist der **kritischste Disable-Case** weil PIREPs ein neues persistentes Feld bekommen.

| Datenklasse | Was passiert | UI-Sicht Pilot-Client | UI-Sicht Webapp |
|---|---|---|---|
| Bereits erzeugte `bounces[]` in alten PIREPs | Bleiben in `landing_history.json` UND in Recorder-DB-`payload_json` | LandingPanel-Detail-Modal funktioniert für alte PIREPs ungeändert (Lese-Pfad ist `record.bounces ?? []`, also leerer Array bei neuen Disable-Files) | LandingAnalysis zeigt alte Bounce-Tabellen weiter |
| Zukünftige Landungen | `bounces[]` wird NICHT in `touchdown`-Payload geschrieben. `bounce_count` (Legacy-Feld) bleibt | LandingPanel zeigt nur Counter „3 Bounces", kein Detail-Modal-Button | dito |
| Zukünftige PIREPs | Kein `bounces`-Feld im payload_json | dito | dito |

**Wichtig — Konsistenz-Garantie:** Wenn Pilot zwischendurch Flag toggled (= Tag 1 AN, Tag 2 AUS, Tag 3 AN), liegen einige PIREPs mit `bounces[]` und einige ohne. Das ist **kein Bug** — Lese-Pfad behandelt beide Fälle (siehe Telemetry-Contract Sektion 5 „Backward-Compat-Matrix").

**Kill-Switch-Pfad:**
- **K1:** Settings → Erweitert → Landungs-Forensik → „Per-Bounce-Detail aktivieren"
- **K2:** `forensics.per_bounce_enabled = false`
- **K3:** „Per-Bounce-Detector schreibt fehlerhafte Records (= bounce_max_agl widerspricht max(bounces[].max_agl_ft))" → Broadcast „bitte ausschalten bis v0.9.1"

---

### F6. `error_reporting.enabled` — GlitchTip Error-Reporting

**Detail-Spec:** `v0.9.0-glitchtip-self-hosted.md`

| Aspekt | Wert |
|---|---|
| **Default Fresh-Install v0.9.0** | **`false`** (Opt-in!) |
| **Default bei Upgrade von v0.8.4** | **`false`** — auch wenn Pilot „erfahrener Beta-Tester" ist, kein automatisches Opt-in |
| **Persistenzort** | `config.json` → `error_reporting.enabled` |
| **UI-Sichtbarkeit** | Standard-User-Tab „Datenschutz & Telemetrie" — prominent sichtbar mit ausführlicher Erklärung |
| **Remote/Local** | Lokal pro Pilot |
| **Wirksamkeits-Latenz** | Beim Toggle ON: Sentry-SDK wird sofort initialisiert. Beim Toggle OFF: Sentry-SDK wird sofort deinitialisiert (= laufender In-Memory-Buffer wird verworfen, kein Flush an Server) |
| **Warnung im UI** | „Standard ist AUS. Bei EIN sendest du anonymisierte Crash- und Fehler-Reports an `tip.kant.ovh`. Siehe Datenschutz-Seite für Details" |

**Spezial — Boot-Behavior:**
- Wenn `false`: Sentry-SDK wird NICHT initialisiert. `init_sentry()` wird gar nicht aufgerufen. Keine DSN im Speicher.
- Wenn `true`: Sentry-SDK init mit DSN aus Build-Env (= kompiliert ins Binary, kein Runtime-Secret-Risk).

**Migration bei Upgrade v0.8.4 → v0.9.0:**
- v0.8.4 hatte kein GlitchTip → es gibt nichts zu inherit. Default `false` schützt vor unbeabsichtigtem Opt-In.
- Beim ersten Start nach Upgrade zeigt das Settings-Panel einen **One-Time-Banner**: „Neu in v0.9.0: GlitchTip Crash-Reports. Standard ist AUS. Bei Interesse hier opt-in." Banner dismissed nach Klick oder Settings-Besuch.

**Verhalten bei Disable nach bereits erzeugten Daten:**

| Datenklasse | Was passiert | UI-Sicht Pilot-Client | UI-Sicht GlitchTip-Web |
|---|---|---|---|
| Bereits an Sentry gesendete Events | Bleiben in GlitchTip-Postgres (30 Tage Retention via GlitchTip-Spec) | n/a (Server-Daten) | Admin sieht alte Events weiter |
| In-Memory-Buffer (= noch nicht gesendete Events) | Wird beim Disable VERWORFEN, nicht geflusht | n/a | nichts Neues |
| Zukünftige Crashes | KEIN Sentry-Event. App-eigene Activity-Log-WARN/ERROR-Pfad bleibt unverändert | unverändert | nichts |

**Daten-Löschung auf Server-Seite:** Pilot kann via Datenschutz-Seite + E-Mail an Admin eine Löschanforderung stellen (= DSGVO Art. 17). Kein automatischer Löschpfad in v0.9.0 — manueller VPS-Pfad.

**Kill-Switch-Pfad:**
- **K1:** Settings → Datenschutz & Telemetrie → Toggle „Crash-Reports an GlitchTip senden". **Sofort wirksam — kein App-Restart erforderlich (DS7-Pflicht aus Telemetry-Contract Sektion 9).** Implementations-Anforderung: `sentry::close()` wird beim Toggle-Aus aufgerufen (= Sentry-Rust-SDK unterstützt das ab v0.32, [docs.rs/sentry](https://docs.rs/sentry/latest/sentry/fn.close.html)). Lokaler Sentry-Buffer wird VERWORFEN, nicht nachgeflusht. Beim nächsten Toggle-AN wird Sentry neu initialisiert.
- **K2:** `config.json` Edit: `error_reporting.enabled = false`. App-Restart NICHT zwingend nötig — Settings-Watcher liest den Flag pro Tick neu. Restart als Power-User-Option falls Settings-UI hängt.
- **K3 — Notfall (kritischer Live-Bug):** Discord-Broadcast „bitte Datenschutz & Telemetrie sofort ausschalten" → Pilots klicken Settings → Toggle aus → sofort wirksam (siehe K1). Falls Pilots nicht erreichbar: VPS-seitiger Notfall-Pfad: GlitchTip-Endpoint wird auf 503 gesetzt → SDK-POSTs scheitern → Pilots merken nichts, Daten gehen nicht raus.
- **K3 — extreme Notfall (VPS-Stop):** `systemctl stop glitchtip` auf VPS → alle SDKs scheitern beim POST → Pilots merken nichts (keine UI-Wirkung), aber Daten gehen nicht raus.

**Warum „Sofort wirksam"-Pflicht:** DS7 (Telemetry-Contract Sektion 9) sagt expliziert: „Opt-out funktioniert sofort, ohne App-Restart." Das ist Privacy-Anforderung — Pilot muss zu jedem Zeitpunkt seine Einwilligung widerrufen können und das muss SOFORT wirksam sein. Ein „Restart empfohlen"-Hinweis würde die Datenschutz-Validität des Toggles untergraben.

---

### F7. `integrations.discord_rpc.enabled` — Discord Rich Presence

**Detail-Spec:** `v0.9.0-discord-rich-presence.md`

| Aspekt | Wert |
|---|---|
| **Default Fresh-Install v0.9.0** | **`false`** (Opt-in!) |
| **Default bei Upgrade von v0.8.4** | **`false`** |
| **Persistenzort** | `config.json` → `integrations.discord_rpc.enabled` |
| **UI-Sichtbarkeit** | Standard-User-Tab „Integrationen" — sichtbar mit „Discord muss installiert sein"-Hinweis |
| **Remote/Local** | Lokal pro Pilot |
| **Wirksamkeits-Latenz** | Beim Toggle ON: DiscordPresenceManager wird sofort initialisiert (= IPC-Connect zu lokalem Discord-Client). Beim Toggle OFF: Discord-Status wird sofort gecleart (binnen 5s, siehe DS8 in Telemetry-Contract Sektion 9) |
| **Warnung im UI** | „Standard ist AUS. Bei EIN zeigt dein Discord-Profil deinen aktuellen Flug-Status. Siehe Datenschutz-Seite für angezeigte Felder" |

**Spezial — Sub-Flag `integrations.discord_rpc.anonymize_callsign`:**
- Default `true` bei Fresh + Upgrade
- Wenn `true`: State-Text zeigt „ASA-Flug" statt „GSG3184"
- Wenn `false`: Voller Callsign sichtbar
- Wird im selben Sub-Bereich des Settings-Tabs gezeigt, nur sichtbar wenn `discord_rpc.enabled = true`

**Migration bei Upgrade v0.8.4 → v0.9.0:**
- Neuer Top-Level-Key `integrations.discord_rpc` → komplettes Sub-Objekt wird mit Defaults geschrieben (= `{ enabled: false, anonymize_callsign: true }`)
- Pilot, der schon einen anderen Discord-Bot nutzt (z.B. SimToolkitPro), wird in Settings darauf hingewiesen: „Hinweis: Aktivierung kann mit anderen RPC-Apps kollidieren — Discord zeigt nur eine Quelle gleichzeitig"

**Verhalten bei Disable nach bereits erzeugten Daten:**

Discord-RPC ist **live-only** (siehe Telemetry-Contract Sektion 4: „Discord-Activity-State: live-only, persistiert nirgendwo"). Daher:

| Datenklasse | Was passiert | UI-Sicht Pilot-Client | UI-Sicht Discord |
|---|---|---|---|
| Aktuell angezeigter Discord-Status | Wird gecleart binnen 5s (= `clear_activity()` Call) | Settings zeigt „Status gecleart" | Profil zeigt keinen RPC-Status mehr |
| Aktivierungs-History | n/a (kein Logging in DB) | n/a | n/a |
| Andere Pilots / VA-Server | n/a — RPC ist Discord-Client-lokal, kein Server-Side-State | n/a | n/a |

→ **Keine Daten-Frage** weil Discord-RPC keine persistenten Daten erzeugt.

**Kill-Switch-Pfad:**
- **K1:** Settings → Integrationen → Toggle „Discord Rich Presence"
- **K2:** `integrations.discord_rpc.enabled = false`. Restart empfohlen wenn DiscordPresenceManager fehlerhaft hängt
- **K3 — extrem schnell wegen IPC-Crash-Risiko:** Discord-RPC ist die **wahrscheinlichste Quelle eines Live-Bug-Notfalls** (third-party IPC zu fremdem Prozess). Notfall-Pfade:
  - Discord-Broadcast „bitte Discord-RPC sofort ausschalten + Restart"
  - **Sub-Flag-Override via VPS-Webhook (NICHT in v0.9.0):** wäre möglich aber bewusst nicht gebaut, siehe „Out of Scope"
  - **Notbehelf falls Pilot nicht erreichbar ist:** Discord-Application-ID auf Discord-Developer-Portal deaktivieren → IPC-Connect schlägt fehl → RPC-Manager logged Error im Activity-Log und macht weiter (kein App-Crash). Effektiv: Status verschwindet bei allen Pilots binnen einem Discord-Restart-Cycle (≤ 60s)

---

### F8. `error_reporting.anonymize_pilot_id` — Hash-Funktion ein/aus

**Detail-Spec:** `v0.9.0-glitchtip-self-hosted.md` (Privacy-Sektion)

| Aspekt | Wert |
|---|---|
| **Default Fresh-Install v0.9.0** | `true` |
| **Default bei Upgrade von v0.8.4** | `true` (neuer Key) |
| **Persistenzort** | `config.json` → `error_reporting.anonymize_pilot_id` |
| **UI-Sichtbarkeit** | **Power-User-Mode** (= versteckt hinter „Erweiterte Einstellungen" Toggle in Datenschutz-Tab) |
| **Remote/Local** | Lokal pro Pilot |
| **Wirksamkeits-Latenz** | Sofort beim nächsten Sentry-Event (= Tag-Computation liest Flag pro Event) |
| **Warnung im UI** | **„NICHT EMPFOHLEN ZU DEAKTIVIEREN.** Bei AUS wird deine pilot_id ungehasht an GlitchTip gesendet — nur für lokale Tests gegen `localhost:9000`. Auf Produktion (= tip.kant.ovh) bleibt der Hash zwingend." |

**Spezial — Server-Side-Enforcement:**
- Auch wenn ein Pilot diesen Flag `false` setzt, wird das Sentry-Backend einen ungehashten pilot_id-Tag **droppen** (= Pre-Processor-Hook in GlitchTip-Config). Das ist die Defense-in-Depth-Schicht. Der Client-Flag ist also nur für Debug-Sessions relevant — kein Privacy-Loophole.
- Begründung für Server-Drop: ungehashte pilot_id ist DSGVO-relevant, Client-Bug darf keine Daten leaken

**Migration bei Upgrade v0.8.4 → v0.9.0:** Neuer Key → Default `true`. Power-User-Bereich ist beim ersten Start kollabiert (= Pilot sieht den Toggle gar nicht).

**Verhalten bei Disable nach bereits erzeugten Daten:**

| Datenklasse | Was passiert | UI-Sicht | Server-Sicht |
|---|---|---|---|
| Bereits gesendete Events (gehashte pilot_id) | Bleiben in GlitchTip | n/a | Admin sieht weiter den Hash |
| Zukünftige Events nach Disable + ON GlitchTip | Client würde ungehashte pilot_id senden — Server droppt sie | n/a | Tag fehlt schlicht im Event |

→ **Kein Daten-Konsistenz-Problem.** Hash und Klar-Form sind unterschiedliche Tag-Werte (= Sentry-Filterung muss beide kennen), aber das ist eine Search-Convenience-Sache, kein Bug.

**Kill-Switch-Pfad:**
- **K1:** Settings → Datenschutz → „Erweiterte Einstellungen anzeigen" → Toggle „pilot_id anonymisieren"
- **K2:** `error_reporting.anonymize_pilot_id = false`
- **K3:** Kein realer Notfall-Pfad denkbar — Anonymisierung schützt den Pilot, nicht den Server. Bei Bug („Hash-Funktion crasht") wird F6 (`error_reporting.enabled`) komplett geknockt

---

## Migration-Pfade — komplettes Flussbild

### Erste-Start-Sequenz nach v0.9.0-Upgrade

```
┌────────────────────────────────────────────────────┐
│ App-Start v0.9.0                                   │
└──────────────────┬─────────────────────────────────┘
                   │
                   ▼
┌────────────────────────────────────────────────────┐
│ load_config_json()                                 │
│  → liest %APPDATA%/ASA-ACARS/config.json           │
└──────────────────┬─────────────────────────────────┘
                   │
                   ▼
┌────────────────────────────────────────────────────┐
│ Check `schema_version` Feld                        │
└──────────────────┬─────────────────────────────────┘
                   │
       ┌───────────┴───────────────┐
       │                           │
       ▼                           ▼
  schema_version == 2        schema_version == 1
  (= v0.9.0 schon hier)      ODER nicht vorhanden
       │                     (= v0.8.4 Bestand)
       │                           │
       │                           ▼
       │                  ┌────────────────────────────────┐
       │                  │ migrate_v1_to_v2()             │
       │                  │  - Backup `config.json.v1.bak` │
       │                  │  - Behalte alle bestehenden    │
       │                  │    Keys (SimBrief, MQTT, ...)  │
       │                  │  - Schreibe alle 8 neuen Flags │
       │                  │    mit ihren Contract-Defaults │
       │                  │  - Setze schema_version=2      │
       │                  │  - Atomic-Write via Temp-File  │
       │                  └────────────┬───────────────────┘
       │                               │
       │                               ▼
       │                  ┌────────────────────────────────┐
       │                  │ Activity-Log:                  │
       │                  │  „Config migriert v1→v2.       │
       │                  │   8 neue Feature-Flags         │
       │                  │   gesetzt. Siehe Settings."    │
       │                  │ Modal beim ersten Settings-    │
       │                  │   Besuch zeigt Diff-Summary    │
       │                  └────────────┬───────────────────┘
       │                               │
       └───────────────┬───────────────┘
                       │
                       ▼
┌────────────────────────────────────────────────────┐
│ App läuft mit gültiger v2-Config                   │
└────────────────────────────────────────────────────┘
```

### Inheritance-Tabelle: was wird übernommen, was wird neu

| Flag | v0.8.4-Bestand vorhanden? | Bei Upgrade: |
|---|---|---|
| `position_sampling.delta_enabled` | nein | `true` (neuer Default) |
| `phases.rejected_take_off_enabled` | nein | `true` |
| `phases.go_around_enabled` | nein | `true` |
| `phases.deboarding_enabled` | nein | `true` |
| `forensics.per_bounce_enabled` | nein | `true` |
| `error_reporting.enabled` | nein | `false` (defensiver Default) |
| `integrations.discord_rpc.enabled` | nein | `false` |
| `error_reporting.anonymize_pilot_id` | nein | `true` |

**Alle 8 Flags sind neu.** Keine Inheritance-Logik nötig — Migrator schreibt einfach die Defaults.

### Was passiert bei Migration-Fehler?

| Fehler | Verhalten |
|---|---|
| Backup-Schreiben scheitert (= Disk-Full) | App-Start abbrechen, Fehler-Modal „Bitte Speicherplatz freimachen oder config.json manuell editieren" |
| Atomic-Write des neuen v2-config scheitert | Backup bleibt, alte v1-config bleibt, App startet mit v1 (= keine v0.9.0-Flags verfügbar — Flag-Reads liefern hartkodierte Defaults aus `feature_flag::default(key)`) |
| Backup existiert bereits (= 2. Upgrade-Versuch) | Backup wird mit Timestamp gerename (`config.json.v1.bak.2026-05-18T14-22-01`) |

### Downgrade v0.9.0 → v0.8.4 — was passiert?

**Entscheidung:** v0.9.0-Config wird von v0.8.4 als „mit unbekannten Extra-Keys" gelesen. v0.8.4 hat KEIN Schema-Validierungs-Pfad und ignoriert unbekannte Keys passiv. Konkretes Verhalten:

| Situation | v0.8.4-Verhalten |
|---|---|
| Pilot installiert v0.8.4 über v0.9.0 (= Downgrade) | v0.8.4 liest `config.json`, ignoriert `schema_version=2` und alle 8 neuen Flags. Bestehende Keys (SimBrief, MQTT) funktionieren weiter |
| v0.8.4 schreibt config zurück (= Pilot ändert was in Settings) | v0.8.4 SERIALISIERT NUR die Keys die es kennt → 8 v0.9.0-Flags gehen verloren. `schema_version` geht verloren |
| Pilot installiert wieder v0.9.0 nach Downgrade | Migrator sieht fehlendes `schema_version` → läuft erneut → schreibt 8 Flags mit Defaults. ABER: Pilot-Customizations (z.B. „GlitchTip aktiviert") gehen verloren → werden auf Default zurückgesetzt |

**Begründung dieser Entscheidung:**
- **Pro:** Schema-Versionierung ist trivial (1 Feld), keine forward-compat-Komplexität in v0.8.4 nötig (was wir eh nicht mehr ändern können)
- **Contra:** Pilot-Customizations gehen bei Downgrade+Re-Upgrade verloren
- **Akzeptanz:** Downgrade ist ein Notfall-Pfad. Pilot-Customizations bei einem Notfall-Downgrade zu verlieren ist vertretbar. Wer Downgrade braucht hat eh größere Probleme als Flag-Einstellungen

**Schema-Versioned-Config-Alternative wurde verworfen** weil sie für v0.9.0 overkill ist (würde JSON-Schema-Lib, Validator, Multi-Version-Migrator und Test-Matrix für jede Schema-Version erfordern). Bei v1.0.0 evaluieren wir das Thema erneut wenn die Flag-Anzahl steigt.

---

## Kill-Switch-Mechanismen — konsolidierte Matrix

| Klasse | Pfad | Wer | Latenz | Verwendbar bei |
|---|---|---|---|---|
| **K1 — Settings-UI** | Toggle in Settings-Panel | Pilot | 0-5s | Normale Opt-out / Opt-in |
| **K2 — config.json Edit** | Datei mit Notepad/VS Code editieren | Pilot (Power-User) | 30-90s (= Restart-Zeit) | UI-Bug verhindert K1 / Vor-Crash-Disable |
| **K3a — VA-Discord-Broadcast** | „bitte ausschalten" + Manual-Mute | VA-Admin | 5-30 Min | Live-Bug, alle Pilots reagieren manuell |
| **K3b — Server-Side-Notbremse** | Sentry-Endpoint 503 / GlitchTip stoppen | VA-Admin auf VPS | <2 Min | GlitchTip-spezifischer Notfall (F6) |
| **K3c — Discord-App-Deaktivieren** | Discord-Developer-Portal | VA-Admin / Thomas | <5 Min, Wirkung ≤ 60s | Discord-RPC-spezifisch (F7) |
| **K4 — neuer Build** | v0.9.0.1 mit hartkodiertem `false` | Maintainer | 24-48 h (Build, Sign, Release) | Letzte Eskalation wenn K1-K3 nicht reichen |

**KEIN Remote-Push-Pfad in v0.9.0.** Begründung:
- Push-Channel würde Auth + Ack + Conflict-Resolution + UI für „Flag wurde remote überschrieben"-Indicator brauchen — eigene Spec
- Für die VA-Größe (3-15 aktive Pilots) reicht K3a (Discord-Broadcast)
- K3b/c decken die zwei wahrscheinlichsten Live-Bug-Klassen (GlitchTip, Discord) server-/third-party-seitig ab

### Notfall-Disable-Playbook „Discord-RPC crasht alle Pilots"

Konkretes Szenario aus dem Briefing („Discord-RPC crasht alle Pilots → wie in 5 Min global ausschalten?"):

```
T+0    Live-Bug-Report von Adrian: „App crasht beim Aktivieren Discord-RPC"
T+1    Thomas verifiziert lokal → bestätigt
T+2    Discord-Broadcast in #asa-acars-status:
       „⚠ Bitte SOFORT in ASA-ACARS Settings → Integrationen
        → Discord Rich Presence AUSSCHALTEN. Bug, Fix kommt."
T+3    Parallel: Thomas öffnet Discord-Developer-Portal
       → Application „ASA-ACARS" deaktiviert
       → ab jetzt: jeder neue Pilot der Discord-RPC versucht zu aktivieren,
         sieht IPC-Connect-Fail im Activity-Log, App läuft normal weiter
T+5    Pilots die online sind haben K1 ausgeführt (5 min reaktive Latenz)
T+10   Status-Update in #asa-acars-status: „Discord-RPC global gestoppt,
       Untersuchung läuft"
T+24h  v0.9.0.1 mit Hotfix
```

→ **5-Min-Ziel erreicht** durch Kombination K3a + K3c. Kein App-Update nötig für die Notbremse, nur für den endgültigen Fix.

---

## Verhalten bei Disable nach erzeugten Daten — Konsolidierte Tabelle

| Flag | Alte Daten bleiben? | Pilot-UI für alte Daten | Webapp für alte Daten | Daten-Löschung? |
|---|---|---|---|---|
| F1 Delta-Sampling | Ja (sparse Positions in DB) | n/a — Stream-Daten | Live-Map zeigt sparse Samples normal | Nein (30d Retention regulär) |
| F2 RTO-Phase | Ja (`rto_event` in payload_json) | LandingPanel-RTO-Indicator weiter sichtbar | RTO-Tab weiter sichtbar | Nein |
| F3 GO_AROUND | Ja (`go_around_events` in payload_json) | Phase-Verlauf weiter sichtbar | Phase-Verlauf weiter sichtbar | Nein |
| F4 DEBOARDING | Ja (phase=DEBOARDING in flight_events) | Activity-Log weiter sichtbar | Phase-Verlauf weiter sichtbar | Nein |
| F5 Per-Bounce | Ja (`bounces[]` in payload_json + landing_history.json) | Detail-Modal weiter sichtbar für alte PIREPs | Detail-Tabelle weiter sichtbar | Nein |
| F6 GlitchTip | Ja (auf Server, 30d Retention) | n/a — Pilot-seitig keine Persistenz | n/a (Pilot hat keine GlitchTip-Sicht) | DSGVO-Löschanforderung via E-Mail an Admin |
| F7 Discord-RPC | Nein (live-only) | n/a | n/a | n/a |
| F8 Anonymize-pilot_id | Mischzustand (Hash + Klar als unterschiedliche Tag-Werte) | n/a | n/a | Server-Drop für ungehashte schützt Live-Daten |

**Pflicht-Regel:** Bei JEDEM Flag ist der Lese-Pfad in UI/Webapp **unabhängig** vom aktuellen Flag-Zustand. Anzeige alter Daten funktioniert auch wenn der erzeugende Flag deaktiviert wurde. Konkret im Code:

```ts
// React-Komponente — KORREKT
const bounces = record.bounces ?? [];
if (bounces.length > 0) {
  return <BounceDetailModal bounces={bounces} />;
}

// FALSCH (Anti-Pattern):
const flagEnabled = useFeatureFlag('forensics.per_bounce_enabled');
if (flagEnabled && record.bounces) { ... }
// → würde alte Daten verstecken sobald Flag aus ist
```

---

## Settings-UI-Layout — Mockup

### Tab-Struktur

```
┌────────────────────────────────────────────────────────────────────┐
│ ASA-ACARS Settings                                          [✕]    │
├────────────────────────────────────────────────────────────────────┤
│ [Allgemein] [Verbindung & Stream] [Integrationen]                  │
│ [Datenschutz & Telemetrie] [Erweitert] [Über]                      │
└────────────────────────────────────────────────────────────────────┘
```

### Tab „Verbindung & Stream" (Standard-User)

```
┌────────────────────────────────────────────────────────────────────┐
│  MQTT-Broker                                                       │
│  URL:   wss://live.kant.ovh/mqtt                          [Edit]   │
│  Pilot-ID: 1234                                                    │
│                                                                    │
│  ─────────────────────────────────────────────────────────────     │
│  Position-Stream                                                   │
│                                                                    │
│  [✓] Delta-Sampling  (F1)                              Sofort      │
│      Sendet Positionen nur bei signifikanter Änderung +            │
│      5s-Heartbeat. Empfohlen: EIN.                                 │
│      Bei AUS: Fixed-Rate alle 5-8s wie in v0.8.x.                  │
└────────────────────────────────────────────────────────────────────┘
```

### Tab „Integrationen" (Standard-User)

```
┌────────────────────────────────────────────────────────────────────┐
│  Discord Rich Presence  (F7)                                       │
│                                                                    │
│  [ ] Discord Rich Presence aktivieren                  Sofort      │
│      Standard: AUS. Zeigt deinen aktuellen Flug-Status in deinem   │
│      Discord-Profil. Discord muss installiert sein.                │
│      → Datenschutz-Details                                         │
│                                                                    │
│  ┌─ Wenn aktiv: ─────────────────────────────────────────┐         │
│  │ [✓] Callsign anonymisieren („ASA-Flug" statt          │         │
│  │     „GSG3184")                                        │         │
│  │     Empfohlen: EIN.                                   │         │
│  └───────────────────────────────────────────────────────┘         │
│                                                                    │
│  ─────────────────────────────────────────────────────────────     │
│  SimBrief                                                          │
│  Username: ...                                          [Edit]     │
└────────────────────────────────────────────────────────────────────┘
```

### Tab „Datenschutz & Telemetrie" (Standard-User + Power-Mode)

```
┌────────────────────────────────────────────────────────────────────┐
│  GlitchTip Error-Reporting  (F6)                                   │
│                                                                    │
│  [ ] Crash- und Fehler-Reports an tip.kant.ovh senden        │
│      Standard: AUS. Hilft bei der Fehlersuche. Anonymisiert.       │
│      Was wird gesendet? Welche Tags? → Datenschutz-Seite           │
│      ✓ Sofort wirksam — kein App-Restart nötig (DS7-Pflicht).      │
│                                                                    │
│  ─────────────────────────────────────────────────────────────     │
│  ▶ Erweiterte Einstellungen (Power-User)                           │
│                                                                    │
│   ┌──── kollabiert standard, sichtbar nach Klick ────────┐         │
│   │                                                       │         │
│   │ [✓] pilot_id für GlitchTip anonymisieren  (F8) Sofort │         │
│   │     ⚠ NICHT EMPFOHLEN ZU DEAKTIVIEREN.               │         │
│   │     Bei AUS wird deine pilot_id ungehasht gesendet — │         │
│   │     nur für lokale Tests gegen localhost:9000.       │         │
│   │     Auf Produktion droppt der Server den Tag.        │         │
│   │                                                       │         │
│   └───────────────────────────────────────────────────────┘         │
└────────────────────────────────────────────────────────────────────┘
```

### Tab „Erweitert" (Standard-User mit Warn-Hints)

```
┌────────────────────────────────────────────────────────────────────┐
│  Flugphasen-Erkennung                                              │
│                                                                    │
│  [✓] Rejected Take-Off erkennen  (F2)                 Sofort       │
│      Standard: EIN. Erkennt abgebrochene Starts und                │
│      schreibt `rto_event` ins PIREP.                               │
│                                                                    │
│  [✓] Go-Around erkennen  (F3)                          Sofort       │
│      Standard: EIN. Erkennt Pull-Up im Final/Approach.             │
│                                                                    │
│  [✓] Deboarding-Phase erkennen  (F4)                  Sofort       │
│      Standard: EIN. Wartet auf Parking-Brake + Engines-Off         │
│      vor PIREP-Submit.                                             │
│                                                                    │
│  ─────────────────────────────────────────────────────────────     │
│  Landungs-Forensik                                                 │
│                                                                    │
│  [✓] Per-Bounce-Detail aktivieren  (F5)               Sofort       │
│      Standard: EIN. Speichert pro Bounce einen Detail-Record       │
│      (max-AGL, G-Force, Speed). Bei AUS bleibt nur der Counter.    │
└────────────────────────────────────────────────────────────────────┘
```

### Sichtbarkeits-Matrix

| Flag | Tab | Sichtbarkeit | Wirksamkeits-Indikator | Warnung? |
|---|---|---|---|---|
| F1 Delta-Sampling | Verbindung & Stream | Standard | „Sofort" | nein |
| F2 RTO-Phase | Erweitert | Standard | „Sofort" | nein |
| F3 GO_AROUND | Erweitert | Standard | „Sofort" | nein |
| F4 DEBOARDING | Erweitert | Standard | „Sofort" | nein |
| F5 Per-Bounce | Erweitert | Standard | „Sofort" | nein |
| F6 GlitchTip | Datenschutz & Telemetrie | Standard | **„Sofort" (DS7-Pflicht)** | „Standard ist AUS" |
| F7 Discord-RPC | Integrationen | Standard | „Sofort" | „Discord muss installiert sein" |
| F8 anonymize_pilot_id | Datenschutz & Telemetrie | **Power-User** (kollabierter Sub-Tree) | „Sofort" | **„NICHT EMPFOHLEN ZU DEAKTIVIEREN"** |

### UI-Komponenten-Vertrag

```tsx
// React-Schnipsel — alle Flag-Toggles nutzen das gleiche Component
<FeatureFlagToggle
  flag="forensics.per_bounce_enabled"
  label="Per-Bounce-Detail aktivieren"
  description="Speichert pro Bounce einen Detail-Record …"
  effectIndicator="immediate" // "immediate" | "restart-recommended" | "restart-required"
  warning={null} // oder { level: "info" | "warning" | "danger", text: "…" }
  visibility="standard" // "standard" | "power-user"
/>
```

---

## Test-Cases pro Flag

Für jeden der 8 Flags gelten die folgenden 5 Standard-Test-Cases. Test-Cases werden in `client/src/__tests__/feature-flags/` als Jest+Tauri-Mock-Suite implementiert.

### Test-Case-Schablone

```
TC-{FLAG}-{N}: {Beschreibung}
  Vorbedingung: ...
  Aktion: ...
  Erwartung: ...
```

### Test-Cases F1 — Delta-Sampling

| ID | Vorbedingung | Aktion | Erwartung |
|---|---|---|---|
| TC-F1-1 | Fresh v0.9.0-Install | App-Start → Settings prüfen | `delta_enabled = true`. Streamer published Delta-Trigger |
| TC-F1-2 | Fresh v0.9.0-Install, Flag in Setup-Wizard AUS | App-Start → Sim-Verbindung | Streamer published Fixed-Rate alle 5-8s (= v0.8.x-Modus) |
| TC-F1-3 | App läuft mit Sim-Verbindung, Flag AN | Mid-Flight Toggle AUS | Nächster Tick = Fixed-Rate, vorherige Delta-Samples bleiben in DB |
| TC-F1-4 | v0.8.4-config.json vorhanden | v0.9.0-Start, Migration | `delta_enabled = true` nach Migration. Backup `config.json.v1.bak` existiert |
| TC-F1-5 | v0.9.0-config.json mit `delta_enabled=false` und Custom-SimBrief | Downgrade auf v0.8.4 dann zurück auf v0.9.0 | SimBrief-Custom geht verloren NUR wenn v0.8.4 die config geschrieben hat. `delta_enabled` ist nach Re-Upgrade `true` (= Default) |

### Test-Cases F2-F4 — Phases (RTO, GO_AROUND, DEBOARDING)

Analog zu F1, plus Spezial-Tests:

| ID | Vorbedingung | Aktion | Erwartung |
|---|---|---|---|
| TC-F2-3 | App läuft, RTO-Flag AN, Pilot taxispeed | Toggle AUS während Takeoff-Roll | StateMachine wechselt nicht mehr zu REJECTED_TAKE_OFF wenn Abbruch passiert (= aktueller Roll wird mit Legacy-Logik fortgeführt) |
| TC-F2-6 | PIREP mit `rto_event` existiert, Flag wird AUS | LandingPanel öffnen | RTO-Pill ist weiterhin sichtbar (Lese-Pfad unconditional) |

### Test-Cases F5 — Per-Bounce

| ID | Vorbedingung | Aktion | Erwartung |
|---|---|---|---|
| TC-F5-1 | Fresh v0.9.0, Flag AN | Landung mit 3 Bounces | `touchdown`-Payload enthält `bounces[3]`, PIREP-payload_json enthält `bounces[3]` |
| TC-F5-2 | Fresh v0.9.0, Flag AUS | Landung mit 3 Bounces | `touchdown`-Payload OHNE `bounces` (= `Option::is_none`), `bounce_count=3` bleibt |
| TC-F5-3 | Flag AN, mid-Flight | Touchdown mit 1 Bounce → Toggle AUS → Touchdown mit 2 Bounces | 1. PIREP hat `bounces[1]`, 2. PIREP hat KEIN `bounces` aber `bounce_count=2` |
| TC-F5-4 | v0.8.4-config + alte landing_history.json ohne `bounces` | v0.9.0-Start | Flag = `true` (Default), aber alte PIREPs zeigen kein Bounce-Modal (= leerer Array) |
| TC-F5-5 | v0.9.0-PIREP mit `bounces[3]` | Downgrade auf v0.8.4 | v0.8.4 ignoriert `bounces`-Feld, zeigt nur `bounce_count` |

### Test-Cases F6 — GlitchTip

| ID | Vorbedingung | Aktion | Erwartung |
|---|---|---|---|
| TC-F6-1 | Fresh v0.9.0 | App-Start | `error_reporting.enabled = false`. Sentry-SDK NICHT initialisiert. Aktivity-Log: kein Sentry-Init-Eintrag |
| TC-F6-2 | Fresh v0.9.0 | Settings → Toggle AN (kein Restart) | Sentry-SDK wird sofort initialisiert via `sentry::init()`-Lazy-Load. DSN aus Env. Activity-Log: „Sentry initialized" binnen 1s. **Anti-Pattern:** App-Restart darf NICHT erforderlich sein (DS7-Pflicht). |
| TC-F6-3 | Flag AN, App läuft | Forciere WARN-Event | Sentry erhält Event mit kanonischen Tags |
| TC-F6-4 | Flag AN, App läuft | Toggle AUS | Sentry-Buffer wird verworfen. Nächster forcierter WARN → kein Sentry-Event |
| TC-F6-5 | v0.8.4-config | v0.9.0-Start, Migration | `error_reporting.enabled = false` (defensiv) trotz Pilot evtl. „GlitchTip-Beta-Tester" |
| TC-F6-6 | Flag AN, GlitchTip ist offline (= Wireshark zeigt 503) | WARN forcieren | App crasht NICHT, Activity-Log zeigt „Sentry transport failed" einmalig (kein Spam) |

### Test-Cases F7 — Discord-RPC

| ID | Vorbedingung | Aktion | Erwartung |
|---|---|---|---|
| TC-F7-1 | Fresh v0.9.0 | App-Start | `enabled = false`. DiscordPresenceManager NICHT initialisiert |
| TC-F7-2 | Fresh v0.9.0, Discord nicht installiert | Settings → Toggle AN | Manager versucht IPC, scheitert → Settings zeigt „Discord nicht gefunden", Toggle bleibt AN (= Pilot kann später Discord starten) |
| TC-F7-3 | Flag AN, Discord läuft | Sim-Connect | Discord-Status zeigt „Boarding ASA-Flug" (Anonymisierung an) |
| TC-F7-4 | Flag AN, Discord läuft, anonymize_callsign AUS | Sim-Connect | Discord-Status zeigt „Boarding GSG3184" |
| TC-F7-5 | Flag AN, Discord läuft | Toggle AUS | Discord-Status gecleart binnen 5s |
| TC-F7-6 | Mid-Flight, Discord-Application im Dev-Portal deaktiviert | warten | RPC-Manager logged Connect-Fail, App läuft, andere Pilots: Status verschwindet binnen 60s |

### Test-Cases F8 — anonymize_pilot_id

| ID | Vorbedingung | Aktion | Erwartung |
|---|---|---|---|
| TC-F8-1 | Fresh v0.9.0, Power-User-Mode NICHT aktiv | Settings öffnen → Datenschutz-Tab | Flag-Toggle ist NICHT sichtbar (kollabiert) |
| TC-F8-2 | Power-User-Mode AN | Datenschutz-Tab | Flag-Toggle ist sichtbar, ungeprüft default → angeklickt zeigt Warnung-Dialog |
| TC-F8-3 | Flag = `false`, GlitchTip = AN | Forciere WARN, Backend = staging | Sentry-Event enthält pilot_id ungehasht im Tag |
| TC-F8-4 | Flag = `false`, GlitchTip = AN, Backend = Produktion | Forciere WARN | Backend droppt pilot_id-Tag in Pre-Processor-Hook |
| TC-F8-5 | v0.8.4-config | v0.9.0-Start | `anonymize_pilot_id = true` (Default) |

### Übergreifende E2E-Test-Cases

| ID | Beschreibung | Wo getestet |
|---|---|---|
| TC-FF-E1 | „Pilot deaktiviert ALLE 8 v0.9.0-Flags" → Verhalten = v0.8.4 exakt | G10 in Telemetry-Contract Sektion 8 |
| TC-FF-E2 | „Migration v0.8.4 → v0.9.0 → Downgrade v0.8.4 → Re-Upgrade v0.9.0" → keine Datenkorruption | Manuell durch QS |
| TC-FF-E3 | „config.json mit ungültigem JSON" → App-Start verhält sich graceful | Jest+Mock |

---

## Akzeptanz-Kriterien

| ID | Kriterium | Verifikation |
|---|---|---|
| **AC1** | Alle 8 Flags sind im `config.json` unter `schema_version=2` definiert | Manueller File-Check nach Fresh-Install |
| **AC2** | Defaults Fresh-Install matchen die im Telemetry-Contract Sektion 7 spezifizierten Werte | TC-F{1..8}-1 |
| **AC3** | Migration v0.8.4 → v0.9.0 schreibt alle 8 neuen Keys ohne bestehende Keys zu zerstören | TC-FF-E2 + Manual mit echtem v0.8.4-config |
| **AC4** | Settings-UI zeigt alle Flags in der spezifizierten Sichtbarkeits-Klasse | Manueller QA-Durchgang DS4 |
| **AC5** | Toggle eines Flags ist innerhalb der spezifizierten Latenz wirksam (sofort vs. Restart) | TC-F1-3, TC-F5-3, TC-F7-5, TC-F6-4 |
| **AC6** | Bei Disable nach erzeugten Daten sind alte Daten in UI/Webapp weiterhin sichtbar | TC-F2-6, TC-F5-3 |
| **AC7** | Kein Flag erzeugt einen Crash beim Toggle (vorwärts oder rückwärts) | Test-Suite + TC-F7-2 |
| **AC8** | Discord-Application-Notfall-Disable (K3c) wirkt innerhalb 60s auf alle Pilots | TC-F7-6 |
| **AC9** | GlitchTip-Server-Notfall-Disable (K3b) wirkt sofort, App crasht nicht | TC-F6-6 |
| **AC10** | F8-Anonymisierungs-Server-Drop ist im GlitchTip-Pre-Processor implementiert | Manual mit Wireshark auf VPS |
| **AC11** | Downgrade-Pfad (v0.9.0 → v0.8.4) bricht v0.8.4 nicht (= unbekannte Keys ignoriert) | TC-F1-5 |
| **AC12** | Re-Upgrade nach Downgrade läuft Migrator erneut + setzt Defaults | TC-FF-E2 |
| **AC13** | Alle Flag-Lese-Pfade in UI/Webapp sind unconditional (= `record.bounces ?? []` Muster) | Code-Review aller `record.*`-Accessor-Pfade |
| **AC14** | `feature_flag::get` und `useFeatureFlag` sind die einzigen Lese-Pfade — keine direkten `config.json`-Reads in Komponenten | grep-Audit `config\.json` außerhalb des Flag-Service |
| **AC15** | Migration-Fehler hinterlassen die App in einem nutzbaren Zustand (= Hard-Coded-Defaults bei Lese-Fehler) | TC-FF-E3 |

---

## Implementierungs-Skizze

### Rust-Seite (`client/src-tauri/crates/feature-flags/`)

```rust
use serde::{Deserialize, Serialize};
use std::sync::RwLock;
use std::path::PathBuf;

pub const SCHEMA_VERSION_CURRENT: u32 = 2;

#[derive(Debug, Serialize, Deserialize, Clone)]
#[serde(default)]
pub struct FeatureFlags {
    pub schema_version: u32,
    pub position_sampling: PositionSamplingFlags,
    pub phases: PhasesFlags,
    pub forensics: ForensicsFlags,
    pub error_reporting: ErrorReportingFlags,
    pub integrations: IntegrationsFlags,
}

impl Default for FeatureFlags {
    fn default() -> Self {
        Self {
            schema_version: SCHEMA_VERSION_CURRENT,
            position_sampling: PositionSamplingFlags { delta_enabled: true },
            phases: PhasesFlags {
                rejected_take_off_enabled: true,
                go_around_enabled: true,
                deboarding_enabled: true,
            },
            forensics: ForensicsFlags { per_bounce_enabled: true },
            error_reporting: ErrorReportingFlags {
                enabled: false,             // defensiv default
                anonymize_pilot_id: true,
            },
            integrations: IntegrationsFlags {
                discord_rpc: DiscordRpcFlags {
                    enabled: false,
                    anonymize_callsign: true,
                },
            },
        }
    }
}

pub struct FeatureFlagsService {
    path: PathBuf,
    state: RwLock<FeatureFlags>,
}

impl FeatureFlagsService {
    pub fn load(app_data_dir: &PathBuf) -> Result<Self, FlagError> { /* ... */ }
    pub fn get_bool(&self, key: &str) -> Option<bool> { /* ... */ }
    pub fn set_bool(&self, key: &str, value: bool) -> Result<(), FlagError> { /* ... */ }
    pub fn migrate_if_needed(&self) -> Result<MigrationResult, FlagError> { /* ... */ }
}
```

### TypeScript-Seite (`client/src/services/featureFlags.ts`)

```ts
import { invoke } from '@tauri-apps/api/tauri';
import { useEffect, useState } from 'react';

export type FlagKey =
  | 'position_sampling.delta_enabled'
  | 'phases.rejected_take_off_enabled'
  | 'phases.go_around_enabled'
  | 'phases.deboarding_enabled'
  | 'forensics.per_bounce_enabled'
  | 'error_reporting.enabled'
  | 'error_reporting.anonymize_pilot_id'
  | 'integrations.discord_rpc.enabled'
  | 'integrations.discord_rpc.anonymize_callsign';

export async function getFlag(key: FlagKey): Promise<boolean> {
  return invoke<boolean>('feature_flag_get', { key });
}

export async function setFlag(key: FlagKey, value: boolean): Promise<void> {
  return invoke('feature_flag_set', { key, value });
}

export function useFeatureFlag(key: FlagKey): boolean {
  const [value, setValue] = useState<boolean>(false);
  useEffect(() => {
    let cancelled = false;
    getFlag(key).then(v => { if (!cancelled) setValue(v); });
    const unlistenPromise = listen<{key: FlagKey, value: boolean}>(
      'feature-flag-changed',
      (e) => { if (!cancelled && e.payload.key === key) setValue(e.payload.value); }
    );
    return () => {
      cancelled = true;
      unlistenPromise.then(u => u());
    };
  }, [key]);
  return value;
}
```

### Beispiel-config.json nach v0.9.0-Migration

```json
{
  "schema_version": 2,
  "simbrief": {
    "username": "thomask",
    "user_id": "123456"
  },
  "mqtt": {
    "broker_url": "wss://live.kant.ovh/mqtt",
    "pilot_id": "1234"
  },
  "approach_stable_config": true,
  "position_sampling": {
    "delta_enabled": true
  },
  "phases": {
    "rejected_take_off_enabled": true,
    "go_around_enabled": true,
    "deboarding_enabled": true
  },
  "forensics": {
    "per_bounce_enabled": true
  },
  "error_reporting": {
    "enabled": false,
    "anonymize_pilot_id": true
  },
  "integrations": {
    "discord_rpc": {
      "enabled": false,
      "anonymize_callsign": true
    }
  }
}
```

---

## Datenschutz-Auswirkungen

| Flag | Datenschutz-Klasse | Begründung |
|---|---|---|
| F1-F5 | irrelevant | nur Pilot-Client-internes Verhalten, keine Daten an Dritte |
| F6 GlitchTip | **relevant** | Crash-Daten an `tip.kant.ovh` (= Thomas-VPS, EU). DSGVO-Schutz via Anonymisierung + opt-in + Retention 30d |
| F7 Discord-RPC | **relevant** | Anwesenheits-Status an Discord-Client (= an Discord Inc., USA). Opt-in + Callsign-Anonymisierung |
| F8 anonymize_pilot_id | **kritisch** | Schutzschicht für F6. Server-Side-Drop als Defense-in-Depth |

Siehe Datenschutz-Release-Gate in Telemetry-Contract Sektion 9 (DS1-DS8) — diese Spec ergänzt keine zusätzlichen Gates, sondern stützt die bestehenden Gates durch klare Default-Wahl.

---

## Out of Scope

Folgendes ist **bewusst nicht** in v0.9.0:

| Item | Begründung |
|---|---|
| **Remote-Config-Management-Server** | Erfordert Auth + Push-Channel + Ack-Tracking + Conflict-Resolution + UI für „remote-overridden"-Indicator. Eigene Spec (Kandidat für v1.0). VA-Größe (3-15 Pilots) rechtfertigt Aufwand nicht |
| **A/B-Testing-Infrastruktur** | Keine User-Base-Größe die A/B-Tests statistisch sinnvoll macht. Wenn überhaupt, dann später als Cloud-Config-Erweiterung |
| **Feature-Flag-Analytics** | Wir wollen nicht tracken wer welchen Flag wann toggled (= Privacy-bewusste Default-Haltung). Wenn ein Pilot Hilfe braucht, Discord-Frage |
| **Multi-Profile-Support** | Pilot hat genau 1 Config. Test- vs. Prod-Profil-Switching ist nicht relevant |
| **Schema-Versioning > v2** | Solange wir nicht in v1.0 Flag-Konzepte umbauen, reicht ein einmaliger v1→v2-Migrator |
| **Forward-Compat-Hooks** | v0.9.0-Config kann in v0.9.x-Patches additiv erweitert werden (= neue Keys mit serde-default). Schema-Bump erst bei v1.0 |
| **Per-Flag-Audit-Log** | Activity-Log loggt Flag-Wechsel als single-line-info-event. Voll-Audit (= „wann hat wer welchen Flag wie geändert") ist out of scope |
| **Flag-Dependency-Graph** | z.B. „F8 darf nur aktivierbar sein wenn F6 aktiv ist". Aktuell wird Dependency manuell im UI gezeigt (= F7-Sub-Flag-Box ist nur sichtbar bei F7=AN). Generischer Graph ist Overkill |
| **Pilot-Override durch VA via VPS** | Wäre nett für „bitte aktiviert alle Per-Bounce für QS-Sprint", aber siehe Remote-Config-Management oben |

---

## Cross-Reference-Updates erforderlich

Diese Spec ergänzt — ändert aber nicht — den Telemetry-Contract. Trotzdem sollen folgende Specs zumindest einen Verweis auf diese Datei bekommen (kein Reword nötig):

| Spec | Verweis |
|---|---|
| `v0.9.0-telemetry-contract.md` Sektion 7 | „Vollständige Semantik der Flags siehe `v0.9.0-feature-flags-semantics.md`" |
| `v0.9.0-delta-position-sampling.md` | „Flag-Verhalten bei Disable: siehe FF-Spec F1" |
| `v0.9.0-flight-phases-expanded.md` | „Flag-Verhalten bei Disable: siehe FF-Spec F2/F3/F4" |
| `v0.9.0-per-bounce-detail.md` | „Flag-Verhalten bei Disable: siehe FF-Spec F5. Lese-Pfad muss `record.bounces ?? []` sein" |
| `v0.9.0-glitchtip-self-hosted.md` | „Flag F6 + F8: siehe FF-Spec. Server-Side-Drop für ungehashte pilot_id im Pre-Processor implementieren" |
| `v0.9.0-discord-rich-presence.md` | „Flag F7 + Sub-Flag anonymize_callsign: siehe FF-Spec F7" |
| `v0.9.0-roadmap.md` | „Settings-UI-Implementation (Tab-Layout siehe FF-Spec)" |

---

## Tracker

- **Anchor:** QS-Round-3 vom 2026-05-18 (User-Auftrag: „pro Flag Defaults, Persistenz, UI, Migration, Disable-Verhalten, Kill-Switch präzisieren")
- **Blockiert bis:** Diese Spec ist gemerged + Cross-Reference-Updates in den 5 Detail-Specs sind gemerged
- **Pflicht für Release:** Implementierungs-Checklist (AC1-AC15) muss erfüllt sein vor v0.9.0-Beta-Phase
- **Folge-Specs (v0.9.x oder v1.0):** Remote-Config-Channel (wenn überhaupt nötig), Schema-Versioned-Config-Migrator-Framework, Flag-Dependency-DSL

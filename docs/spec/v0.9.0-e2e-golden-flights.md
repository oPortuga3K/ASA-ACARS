# v0.9.0 — E2E Golden Flights (Ausführbare QS-Test-Spezifikation)

**Status:** PROPOSAL — Pflicht-Vorgabe für v0.9.0-Release-Gate
**Datum:** 2026-05-18
**Quelle:** QS-Round-3-Befund („Die G1–G10-Tabelle in telemetry-contract.md Sektion 8 ist eine gute Testliste, aber kein QS-Gate — pro Test fehlen Fixture, Phase-Sequenz, Bounce-Erwartungen, PIREP-Felder, Privacy- und Discord/GlitchTip-Verhalten")
**Ziel:** Die 10 E2E-Golden-Flights G1–G10 aus dem Telemetry-Contract werden zu reproduzierbaren, durch Beta-Pilots ausführbaren Tests mit klaren Pass/Fail-Kriterien. Jeder Test ist so spezifiziert, dass ein Tester ohne weitere Rückfragen die Sim-Session, die Auswertung und das Release-Voting durchführen kann.

## Pflicht-Vorgabe und Cross-References

Dieses Spec ist **vorrangig** gegenüber den 5 v0.9.0-Detail-Specs in QS-Belangen. Bei Konflikt zwischen Detail-Spec-Test-Hinweisen und diesem Dokument gilt dieses Dokument.

Cross-References:

| Quell-Spec | Wofür referenziert |
|---|---|
| `docs/spec/v0.9.0-telemetry-contract.md` Sektion 8 | Ursprüngliche G1–G10-Kurzbeschreibung — wird hier 1:1 ausgebaut |
| `docs/spec/v0.9.0-telemetry-contract.md` Sektion 9 | Datenschutz-Release-Gate (DS1–DS8) — fließt in jeden Privacy-Check-Abschnitt ein |
| `docs/spec/v0.9.0-telemetry-contract.md` Sektion 1.1–1.4 | Kanonische Telemetrie-Felder — definiert was die PIREP-Erwartungs-Tabellen prüfen |
| `docs/spec/v0.9.0-telemetry-contract.md` Sektion 7 | Feature-Flags — G10 prüft den vollständigen Opt-Out |
| `docs/spec/v0.9.0-delta-position-sampling.md` | Delta-Sampling-Heartbeat-Verhalten — G1 + G6 referenzieren |
| `docs/spec/v0.9.0-flight-phases-expanded.md` | RTO/GO_AROUND/DEBOARDING-Trigger — G3 + G4 |
| `docs/spec/v0.9.0-per-bounce-detail.md` | `bounces[]`-Struktur — G2 |
| `docs/spec/v0.9.0-glitchtip-self-hosted.md` | Sentry-DSN, Tag-Set, Anonymisierung — G8 + Privacy-Checks aller Tests |
| `docs/spec/v0.9.0-discord-rich-presence.md` | Discord-RPC-State-Format — G9 + RPC-Check aller Tests |

**Regel:** Wenn ein Detail-Spec sich ändert und Felder/Phasen/Events betroffen sind, MUSS dieses Spec im selben PR aktualisiert werden. Andernfalls Roll-Back des PRs.

## Kurzfassung

Die 10 Golden-Flights decken zusammen den vollen v0.9.0-Featureumfang ab:

- **G1** validiert den Standardfall (90-Min-Linienflug ohne Besonderheiten) — Hauptkennwert ist Delta-Sampling-Effizienz und PIREP-Vollständigkeit
- **G2–G4** validieren die drei v0.9.0-Hauptfeatures (Per-Bounce, RTO, GO_AROUND) je in einem dedizierten Szenario
- **G5 + G6** validieren Resilienz unter Sim-Stress (Pause/Disconnect, FPS-Drops)
- **G7** validiert Backward-Compat (alte PIREPs in neuer Webapp)
- **G8 + G9** validieren Degradation bei externen Ausfällen (GlitchTip-offline, Discord nicht installiert)
- **G10** validiert die vollständige Opt-Out-Garantie (jedes neue Feature einzeln abschaltbar)

Jedes Szenario hat: Reproduktions-Anweisung, Fixture-Pfad, erwartete Phase-Sequenz, PIREP-Feld-Tabelle, Bounce-Erwartungen, GlitchTip- und Discord-Verhalten, Privacy-Check und Pass/Fail-Checkliste. Total-QS-Budget am Dokument-Ende.

Out of scope: Performance-Benchmarks (sind eigene Spec), Multi-Pilot-Race-Conditions auf dem VPS (eigene Spec), Webapp-UI-Snapshot-Tests (separater Visual-Regression-Lauf).

## Wie ich diese Tests durchführe

### Setup-Voraussetzungen pro Test-Maschine

Vor dem ersten G-Test muss die Test-Maschine folgenden Zustand haben:

```bash
# 1. ASA-ACARS-Pilot-Client im v0.9.0-Release-Candidate-Build installiert
#    (aus GitHub Actions Artifact des aktuellen claude/*-Branches)
$ asa-acars-cli --version
ASA-ACARS 0.9.0-rc.N (commit abc1234)

# 2. Recorder-VPS live.kant.ovh erreichbar
$ curl -fsS https://live.kant.ovh/healthz
{"status":"ok","version":"0.9.0-rc.N"}

# 3. GlitchTip-Instanz hat eine dedizierte Test-Org "asa-acars-qs"
#    DSN ist in test-config.json hinterlegt (NICHT der Production-DSN)
$ jq '.error_reporting.dsn' test-config.json
"https://abc@tip.kant.ovh/2"

# 4. Discord-Client läuft (für RPC-Tests) ODER ist deinstalliert (G9)
$ pidof Discord && echo "running" || echo "absent"

# 5. Sim-Auswahl steht vorab fest (MSFS 2024 standard, X-Plane 12 als Backup)
#    Aircraft-Auswahl in der Test-Tabelle pro G-Test angegeben.
```

### Tools

| Tool | Verwendung |
|---|---|
| **Wireshark + SSLKEYLOGFILE** | DS1-Check für G1, G3, G4 — HTTPS-POST-Body an GlitchTip dekrypten und auf Pflicht-Nicht-Inhalte prüfen |
| **mosquitto_sub** | Live-Stream der MQTT-Topics während Test mitschneiden (= JSONL-Roh-Aufzeichnung für spätere Auswertung) |
| **ASA-ACARS-Replay-CLI** (`asa-acars-cli replay`) | Aufgenommene JSONL-Fixture deterministisch in einen Mock-Pilot-Client einspeisen (= reproduzierbar ohne Sim-Session) |
| **ASA-ACARS-Recorder-Test-Dump** (`asa-acars-recorder dump --pirep <id>`) | PIREP-JSON nach Test auslesen und gegen Erwartungs-Tabelle diffen |
| **GlitchTip-API** (`/api/0/projects/asa-acars-qs/issues/`) | Events der letzten N Minuten abfragen + auf Tag-Vollständigkeit prüfen |
| **Discord-Developer-Tools** (`Settings → Advanced → Developer Mode`) | RPC-State manuell abfotografieren, OCR-Vergleich mit Erwartungs-String |
| **`jq`** | PIREP-JSON-Validation in der Pass/Fail-Checkliste |
| **`hyperfine`** | nur bei G1/G6 — Vergleich Delta-Sampling-Output-Volumen gegen v0.8.4-Referenz-Fixture |

### Aufzeichnungs-Workflow für neue Fixtures

Wenn die in den Test-Specs angegebenen Fixture-Dateien noch fehlen („muss noch aufgezeichnet werden"), gilt folgender Workflow:

```bash
# 1. Pilot-Client mit Recording-Mode starten
$ asa-acars-cli --record-fixture tests/fixtures/golden-flights/G1-eddb-eddm-a320.jsonl

# 2. Sim öffnen, Sim-Setup gemäß Test-Spec ausführen
# 3. Flug komplett durchziehen
# 4. Recording stoppen (Strg+C nach „PIREP filed"-Activity-Log)
# 5. Fixture komprimieren
$ gzip tests/fixtures/golden-flights/G1-eddb-eddm-a320.jsonl
# 6. Fixture-Header (= erste Zeile mit Metadata) prüfen
$ zcat tests/fixtures/golden-flights/G1-eddb-eddm-a320.jsonl.gz | head -1
{"_fixture":"G1","sim":"msfs2024","aircraft":"A320","route":"EDDB-EDDM","recorded_at":"2026-05-18T..","client_version":"0.9.0-rc.N"}
# 7. In Git mit LFS-Track committen
$ git lfs track "tests/fixtures/golden-flights/*.jsonl.gz"
$ git add tests/fixtures/golden-flights/ .gitattributes
$ git commit -m "fixtures: G1 baseline 90-min A320 flight (rc.N)"
```

**Regel:** Fixtures sind reproduzierbar dokumentiert (= Sim, Aircraft, Wetter-Seed, Pilot-Aktionen). Wenn eine Fixture „verloren" geht, kann die Reproduktions-Anweisung neue erzeugen.

### Replay-vs-Live-Modus

Jeder G-Test ist in zwei Modi durchführbar:

| Modus | Was passiert | Wann verwenden |
|---|---|---|
| **Live-Sim** | Pilot setzt sich vor MSFS/X-Plane und fliegt das Szenario echt | Mindestens 1× pro Release-Kandidat — fängt Sim-Adapter-Bugs |
| **Replay-Mock** | `asa-acars-cli replay <fixture>` füttert den Client mit aufgezeichneten Snapshots | Für CI-Pipeline, schnelle Iteration, Tag-Validation |

**Pflicht für Release-Voting:** Jedes G1–G10 muss MINDESTENS einmal Live-Sim bestanden haben. Replay alleine reicht für CI, aber nicht fürs Release-Gate.

### Test-Pilot-Personen

| Person | Verantwortung | Verfügbare Tests |
|---|---|---|
| **Thomas K** (Maintainer) | Setup-Owner, GlitchTip-/Wireshark-Auswertung, finale Pass/Fail-Entscheidung | G1, G3, G4, G5, G7, G8, G10 |
| **Adrian** (Beta-Pilot, A320-Spezialist) | Live-Sim G1 + G4 (A320 in MSFS), Replay-Validation G6 | G1, G4, G6 |
| **Svenny** (Beta-Pilot, GA-Pilot, Bonanza-Besitzer) | Live-Sim G2 — der einzige Test der echte Bounce-Aktion braucht | G2 |
| **Michel** (Beta-Pilot, ohne Discord) | Live-Sim G9 — Discord absichtlich nicht installiert | G9 |
| **Thomas oder Adrian** | Live-Sim G3 (RTO ist heikel — braucht entspannten Test-Pilot mit Fenix-A32x) | G3 |

**Eskalation:** Findet ein Beta-Pilot ein Fail, postet er das Ergebnis in `#asa-acars-beta` mit Tag `@thomas` plus Fixture-Upload. Release-Gate wird angehalten bis Thomas Root-Cause-Analyse abgeschlossen hat.

### Output-Artefakte pro Test

Jeder G-Test produziert:

1. **`results/G{N}/run-{date}/pirep.json`** — der finale PIREP-Body
2. **`results/G{N}/run-{date}/mqtt.jsonl`** — der MQTT-Capture (alle Topics)
3. **`results/G{N}/run-{date}/glitchtip-events.json`** — alle GlitchTip-Events der Test-Periode
4. **`results/G{N}/run-{date}/discord-rpc.txt`** — Screenshot-OCR oder manuelle Notiz
5. **`results/G{N}/run-{date}/checklist.md`** — die Pass/Fail-Checkliste mit Häkchen + Tester-Name
6. **`results/G{N}/run-{date}/notes.md`** — Freitext (z.B. „Sim-FPS-Drop bei Min 47 — siehe Heartbeat-Verhalten G6")

**Aufbewahrung:** Mindestens bis 30 Tage nach Release. Bei Release-Block bis Root-Cause + Re-Test.

## Übersichts-Tabelle

| # | Szenario | Live-Pilot | Sim | Aircraft | Dauer (Sim) | Auswertung | Total |
|---|---|---|---|---|---|---|---|
| G1 | 90-Min A320 EDDB→EDDM | Adrian | MSFS 2024 | Airbus A320 Fenix | ~95 min | ~25 min | ~120 min |
| G2 | 3-Bounce-Landung Bonanza | Svenny | X-Plane 12 | Beechcraft Bonanza G36 | ~25 min | ~30 min | ~55 min |
| G3 | RTO bei 38 kt | Thomas/Adrian | MSFS 2024 | Airbus A320 Fenix | ~15 min | ~25 min | ~40 min |
| G4 | Doppelter GO_AROUND | Adrian | MSFS 2024 | Airbus A320 Fenix | ~40 min | ~30 min | ~70 min |
| G5 | Pause/Slew/Disconnect | Thomas | MSFS 2024 | beliebig | ~25 min | ~20 min | ~45 min |
| G6 | Schlechte FPS / Gaps | Adrian (Replay) | n/a | n/a | n/a (Replay) | ~30 min | ~30 min |
| G7 | v0.8.4-PIREP in v0.9.0 | Thomas | n/a (Webapp) | n/a | n/a | ~15 min | ~15 min |
| G8 | GlitchTip offline | Thomas | MSFS 2024 | beliebig | ~10 min | ~20 min | ~30 min |
| G9 | Discord nicht installiert | Michel | MSFS 2024 | beliebig | ~15 min | ~15 min | ~30 min |
| G10 | Alle Features aus | Thomas | MSFS 2024 | beliebig | ~30 min | ~30 min | ~60 min |
| | | | | | | **TOTAL** | **~495 min ≈ 8.3 h** |

---

# G1 — Normaler 90-Min-Flug A320 EDDB→EDDM

**Test-Pilot:** Adrian
**Sim:** MSFS 2024
**Aircraft:** Fenix A320 (mit ASA-ACARS-Cockpit-State-Beta aktiv)
**Route:** EDDB (Berlin Brandenburg) → EDDM (München Franz Josef Strauß)
**Hauptzweck:** Validierung des „Happy Path" — Delta-Sampling-Effizienz, vollständige Phase-Sequenz, sauberer PIREP ohne neue Edge-Case-Felder, keine Sentry-Events, korrekter Discord-RPC.

## Reproduktions-Anweisung

### Sim-Setup (vor dem Start)

```
1. MSFS 2024 starten, Free Flight Mode
2. Aircraft: Fenix A320, Livery „Lufthansa D-AIQT" (oder ASA-Default)
3. Departure: EDDB Gate „Apron 1 A12" (Cold & Dark)
4. Arrival: EDDM (für Sim-Auto-Setup)
5. Wetter: Live-Weather DEAKTIVIEREN, manuell „Few Clouds, 8 kt 240°, OAT 12°C, QNH 1018"
   (Reproduzierbarkeit — Live-Weather macht jeden Lauf anders)
6. Time: Local Noon (Sim-Time 12:00 UTC)
7. Failures: NONE (kein zufälliger Engine-Failure mitten im Test)
8. Realism: alles auf Standard, KEINE Slew-Mode-Tasten bind
```

### ASA-ACARS-Setup

```
1. Pilot-Client v0.9.0-rc.N starten
2. Bid auswählen: GSG3184 (EDDB→EDDM, Airbus A320)
3. In Settings: ALLE v0.9.0-Features auf Default (= Delta=ein, Phases=ein, Per-Bounce=ein,
   GlitchTip=AUS — wir aktivieren ihn manuell, Discord=AUS analog)
4. Diese beiden Toggles für G1 manuell EIN:
   - error_reporting.enabled = true (mit Test-DSN aus test-config.json)
   - integrations.discord_rpc.enabled = true
5. MQTT-Capture starten:
   $ mosquitto_sub -h live.kant.ovh -p 8883 --capath /etc/ssl/certs \
     -t 'asa-acars/gsg/+/#' -F '%I %t %p' > results/G1/run-$(date +%F)/mqtt.jsonl &
6. Wireshark starten (für DS1-Privacy-Check), Capture-Filter `host tip.kant.ovh`
7. „Flight Start" im Pilot-Client klicken
```

### Pilot-Aktionen mit Timing

Die Aktionen werden gegen die Sim-Uhr getimed (= ab dem Moment, wenn Pilot „Flight Start" klickt; Sim ist noch Cold & Dark).

| Sim-Zeit (mm:ss) | Aktion | Erwartete Phase |
|---|---|---|
| 00:00 | Flight Start klicken, Cockpit Cold | PREFLIGHT |
| 02:00 | EFB Boarding starten, APU on, Beacon on | BOARDING |
| 12:00 | Boarding complete, Engines start request | BOARDING → PUSHBACK (wenn parking_brake → off) |
| 14:00 | Pushback Truck connect, Pushback complete | PUSHBACK |
| 16:00 | Engine 1 + 2 start, Taxi clearance | TAXI_OUT |
| 22:30 | Aligned RWY 25R, Takeoff Power | TAKEOFF_ROLL → TAKEOFF |
| 23:00 | Rotate (V_R ~140 kt), Positive Rate, Gear Up | TAKEOFF → CLIMB |
| 30:00 | FL100, accelerate to 300 kt | CLIMB |
| 42:00 | FL360, level off | CRUISE |
| 65:00 | Top of Descent, Initial Descent | DESCENT |
| 78:00 | Approach Briefing, ILS established | APPROACH |
| 84:00 | Localizer alive, Glideslope alive | FINAL |
| 87:00 | 1000 ft AGL, Stable | LANDING |
| 87:45 | Touchdown RWY 26R EDDM, soft landing (-180 fpm, kein Bounce) | LANDING → ARRIVED |
| 89:00 | Vacate, Taxi to Gate | TAXI_IN |
| 91:30 | Park at Gate, Parking Brake set | ARRIVED → BLOCKS_ON |
| 92:00 | Engines off, Beacon off | BLOCKS_ON → DEBOARDING |
| 93:00 | „File PIREP" Button | (PIREP filed) |

**Wetter:** identisch zum Setup oben — falls Live-Weather doch noch geändert wird, ist die Fixture nicht-reproduzierbar.

## Fixture / Replay

**Pfad:** `tests/fixtures/golden-flights/G1-eddb-eddm-a320.jsonl.gz`

**Status:** muss noch aufgezeichnet werden (Erst-Aufnahme durch Adrian beim ersten v0.9.0-rc.1-Lauf — danach permanent als Replay-Quelle).

**Replay-Befehl:**

```bash
$ asa-acars-cli replay tests/fixtures/golden-flights/G1-eddb-eddm-a320.jsonl.gz \
    --speed 1.0 \
    --output results/G1/replay-$(date +%F)/
```

**Erwartete Fixture-Größe:** ~2–3 MB komprimiert (= ~30–50 MB unkomprimiert bei v0.8.4-Sampling-Rate; mit v0.9.0-Delta-Sampling erwartet ~6–10 MB unkomprimiert).

## Erwartete Phase-Sequence

```
PREFLIGHT      (0:00)    → BOARDING       (2:00)    → PUSHBACK       (12:00)
→ TAXI_OUT     (16:00)   → TAKEOFF_ROLL   (22:30)   → TAKEOFF        (22:45)
→ CLIMB        (23:15)   → CRUISE         (42:00)   → DESCENT        (65:00)
→ APPROACH     (78:00)   → FINAL          (84:00)   → LANDING        (87:00)
→ ARRIVED      (87:45)   → TAXI_IN        (89:00)   → BLOCKS_ON      (91:30)
→ DEBOARDING   (92:00)
```

**Negativ-Erwartung:** weder `REJECTED_TAKE_OFF` noch `GO_AROUND` im Stream. Wenn sie auftauchen → Phase-StateMachine ist false-positive, Fail.

**Timing-Toleranz:** ±60 Sekunden pro Übergang (Pilot ist Mensch, nicht Roboter). Längere Drifts sind ein Sim-Setup-Problem, kein Bug.

## Erwartete PIREP-Felder

| Feld | Erwarteter Wert | Toleranz / Validation |
|---|---|---|
| `callsign` | `"GSG3184"` | exakt |
| `aircraft_icao` | `"A320"` | exakt |
| `aircraft_registration` | `"D-AIQT"` | exakt (oder ASA-Default) |
| `dep` | `"EDDB"` | exakt |
| `arr` | `"EDDM"` | exakt |
| `flight_time_min` | ~90 | 80 ≤ x ≤ 100 |
| `block_time_min` | ~93 | 85 ≤ x ≤ 105 |
| `fuel_used_kg` | ~3800 | 3000 ≤ x ≤ 4500 |
| `touchdown.vs_fpm` | -180 | -250 ≤ x ≤ -120 |
| `touchdown.airport` | `"EDDM"` | exakt |
| `touchdown.runway` | `"26R"` | exakt |
| `touchdown.score` | ≥ 80 | „Excellent"-Range |
| `touchdown.bounce` | `false` | exakt |
| `touchdown.bounce_count` | `0` | exakt |
| `touchdown.forensic_bounce_count` | `0` | exakt |
| `touchdown.scored_bounce_count` | `0` | exakt |
| `touchdown.bounce_max_agl_ft` | `null` ODER `0.0` | beides ok |
| `touchdown.bounces` | `null` ODER nicht vorhanden | KEIN Array |
| `bounces` (PIREP top-level) | nicht vorhanden | `#[serde(skip_serializing_if=Option::is_none)]` |
| `rto_event` | nicht vorhanden | dito |
| `go_around_events` | nicht vorhanden | dito |
| `forensics_version` | `2` | v0.9.0-Default |
| `client_version` | `"0.9.0-rc.N"` | aktuelle RC-Version |

**Validation-Skript:**

```bash
$ jq -e '
  .callsign=="GSG3184" and
  .aircraft_icao=="A320" and
  .dep=="EDDB" and .arr=="EDDM" and
  (.touchdown.bounce|not) and
  .touchdown.bounce_count==0 and
  (.touchdown.bounces==null or (.touchdown.bounces|length==0)) and
  (.bounces==null) and
  (.rto_event==null) and
  (.go_around_events==null)
' results/G1/run-$(date +%F)/pirep.json
```

Erwarteter Exit-Code: `0` (= true). Bei `1` → Pass-Kriterium failed.

## Erwartete Bounce-Werte

```json
{
  "forensic_bounce_count": 0,
  "scored_bounce_count": 0,
  "bounce_max_agl_ft": null,
  "bounces": null
}
```

Identisch im MQTT-Touchdown-Topic UND im PIREP-JSON.

## Erwartetes GlitchTip-Verhalten

**Erwartete Events:** **0** (null).

Ein 90-Min-Standard-Flug ohne Crashes, ohne WARN/ERROR-Log-Lines, ohne Hard-Landing darf KEINE GlitchTip-Events produzieren. Wenn doch welche da sind:

| Beobachtetes Event | Bedeutung | Aktion |
|---|---|---|
| `aircraft_mismatch_warning` | Bid sagt A320, Sim meldet was anderes | False-Positive — Fix nötig |
| `phase_skip_detected` | StateMachine sprang PUSHBACK über | Bug in Expanded-Phases-Spec |
| `delta_sampling_overpublish` | Mehr Events gesendet als v0.8.4 | Delta-Sampling-Spec-Verletzung |
| irgendein `error.*` | Generischer Error | Investigation Pflicht |

**Validation:**

```bash
$ curl -s -H "Authorization: Bearer $GLITCHTIP_API_TOKEN" \
    "https://tip.kant.ovh/api/0/projects/asa-acars-qs/issues/?query=callsign:GSG3184+age:90m" \
  | jq 'length'
0
```

## Erwartetes Discord-RPC-Verhalten

**State während des Fluges** (Updates bei jeder Phase-Transition + 60s Heartbeat):

| Sim-Zeit | Discord-Details | Discord-State | Large-Image | Small-Image |
|---|---|---|---|---|
| 00:00 | `GSG3184 · EDDB → EDDM` | `PREFLIGHT · A320` | „asa-acars-logo" | „msfs-icon" |
| 23:15 | `GSG3184 · EDDB → EDDM` | `CLIMB · A320` | „asa-acars-logo" | „msfs-icon" |
| 42:00 | `GSG3184 · EDDB → EDDM` | `CRUISE · A320 · FL360` | dito | dito |
| 65:00 | `GSG3184 · EDDB → EDDM` | `DESCENT · A320` | dito | dito |
| 87:45 | `GSG3184 · EDDB → EDDM` | `LANDED · A320 · -180 fpm` | dito | dito |
| 92:00 | `GSG3184 · EDDB → EDDM` | `DEBOARDING · A320` | dito | dito |
| 93:00 (PIREP filed) | (Status gecleart binnen 5 s) | (leer) | (leer) | (leer) |

**Pflicht:** Bei Phase-Transition wird RPC sofort gepushed (≤ 1 s nach Transition). Zusätzlich alle 60 s ein Heartbeat-Update (gleicher Inhalt, hält Discord-Stand "frisch").

**Anti-Bug:** Nach „File PIREP" muss `clear_activity()` aufgerufen werden — Discord-Status verschwindet. Wenn er noch sichtbar bleibt: Bug in Discord-RPC-Spec.

## Privacy-Check

**Wireshark-Capture-Auswertung:** Alle HTTPS-POSTs an `tip.kant.ovh` müssen folgende Felder NICHT enthalten:

| Verbotenes Feld | Hintergrund | Wie geprüft |
|---|---|---|
| `pilot_email` | DSGVO | `grep -i 'email' decrypted.json` → 0 Matches |
| `phpvms_api_token` | Auth-Secret | `grep -i 'token' decrypted.json` → 0 Matches |
| `phpvms_password` | Auth-Secret | `grep -i 'password' decrypted.json` → 0 Matches |
| `raw_pilot_id` (= un-gehashter Pilot-ID) | DSGVO | `grep '"pilot_id"' decrypted.json` → leer (nur `pilot_id_hash` erlaubt) |
| PIREP-Notes-Volltext | u.a. potenziell PII | `grep 'PIREP-NOTES' decrypted.json` → 0 Matches |
| Pilot-Klartext-Name | DSGVO | `grep -i 'firstname\|lastname' decrypted.json` → 0 Matches |
| IP-Adresse des Piloten | DSGVO (`server_name` reicht) | Sentry-Default ist mit `send_default_pii=false` deaktiviert — verifizieren |

**Erlaubte Tags (Positivliste):** `va_code`, `pilot_id_hash`, `callsign`, `aircraft_icao`, `simulator`, `client_version`, `phase` (zum Crash-Zeitpunkt), `route` (= `dep-arr`-String).

**G1-spezifisch:** Da 0 Events erwartet werden, ist der Wireshark-Capture trivially „leer" (außer DNS/TLS-Handshake). Wenn doch ein Event da ist, gilt die Negativliste oben.

## Pass/Fail-Kriterien

- [ ] Phase-Sequenz vollständig in MQTT-Capture (= alle 14 Phase-Transitions sichtbar, keine fehlend, keine zusätzlich)
- [ ] Phase-Timing innerhalb ±60 s Toleranz pro Übergang
- [ ] PIREP-JSON validiert mit jq-Skript oben (Exit 0)
- [ ] `touchdown.bounces == null` oder leer (KEIN Array mit Einträgen)
- [ ] `rto_event == null` und `go_around_events == null` im PIREP
- [ ] Position-Stream-Volumen mind. 50% kleiner als v0.8.4-Referenz-Fixture (Delta-Sampling-Wirkung) — gemessen via `wc -l results/G1/run-*/mqtt.jsonl | grep position`
- [ ] Alle 60 s ein Position-Heartbeat im MQTT-Capture (auch wenn keine Delta-Auslöser)
- [ ] 0 GlitchTip-Events während Test-Periode
- [ ] Discord-RPC-State entspricht der Tabelle (mind. 5 Screenshots zur Validierung)
- [ ] Discord-RPC nach PIREP-File binnen 5 s gecleart
- [ ] Wireshark-Capture zeigt keine verbotenen Felder (Privacy-Check)
- [ ] Tester-Signatur + Datum in `checklist.md`

## Estimated QS-Time

- Sim-Zeit: ~95 min (Cold & Dark bis File PIREP)
- Auswertung: ~25 min (PIREP-Diff, GlitchTip-Query, Discord-Screenshot-Review, Privacy-Capture-Lesen)
- **Total: ~120 min ≈ 2 h**

Bei Replay-Modus (= zweiter Lauf zur Validierung): ~30 min Total.

---

# G2 — 3-Bounce-Landung Bonanza auf GA-Platz

**Test-Pilot:** Svenny (Bonanza-Spezialist, GA-Hintergrund)
**Sim:** X-Plane 12 (Bonanza-Modelle dort historisch genauer)
**Aircraft:** Beechcraft Bonanza G36 (Laminar-Default oder Carenado)
**Route:** EDQA (Bamberg-Breitenau) lokales Pattern — Takeoff + Pattern + Landung
**Hauptzweck:** Validierung von Per-Bounce-Detail-Records (`bounces[]`-Array) mit exakt 3 Bouncing-Events. Konsistenz zwischen `forensic_bounce_count`, `scored_bounce_count` und `bounces.length`. Hard-Bounce-Sentry-Event auslösen.

## Reproduktions-Anweisung

### Sim-Setup

```
1. X-Plane 12 starten, Custom Scenery EDQA aktiv (oder Default, beide ok)
2. Aircraft: Bonanza G36, Cold & Dark am Apron
3. Wetter: Wind „180° / 12 kt gusting 16 kt", FEW 030, OAT 18°C, QNH 1015
4. Time: 14:00 Local
5. Failures: NONE
```

### ASA-ACARS-Setup

```
1. Pilot-Client v0.9.0-rc.N, Bid „ASA-VFR-Pattern EDQA-EDQA Bonanza"
2. Features: alle Defaults + error_reporting + discord_rpc EIN (wie G1)
3. forensics.per_bounce_enabled = true (verifizieren, Default ist true)
4. MQTT-Capture + Wireshark wie G1
```

### Pilot-Aktionen mit Timing

| Sim-Zeit (mm:ss) | Aktion |
|---|---|
| 00:00 | Flight Start, Cold & Dark |
| 03:00 | Engine Start, Taxi to RWY 27 |
| 06:00 | Lineup RWY 27 |
| 06:30 | Takeoff, Climb to Pattern-Altitude 1000 ft AGL |
| 09:00 | Downwind 27 |
| 11:00 | Base 27 |
| 12:30 | Final RWY 27 — **bewusst zu schnell** (95 kt statt 75 kt Vref+5), Pitch flach |
| 13:00 | **Bounce 1**: Touchdown bei -350 fpm + 95 kt, Aircraft springt auf ~12 ft AGL |
| 13:05 | **Bounce 2**: Wieder Touchdown, springt auf ~7 ft AGL (Pilot hält Yoke leicht zurück, nicht panisch) |
| 13:10 | **Bounce 3**: Letzter Touchdown bei -200 fpm, springt auf ~3 ft AGL, dann sauber gerollt |
| 13:30 | Roll-out, Vacate |
| 15:00 | Taxi back to Apron, Park, Engines off |
| 16:00 | File PIREP |

**Wichtig:** Die 3 Bounces müssen alle drei VOR Spoiler-Activation / Final-Roll-Out passieren — Touchdown-Detektor-Window muss alle 3 als ein Touchdown-Event sehen.

**Warum genau 3?** Per-Bounce-Spec setzt Detail-Records ab ≥ 5 ft AGL (= `forensic_bounce_count`). Wir wollen 3 verschiedene Höhen testen (12/7/3 ft). Die 3 ft sollte gerade NICHT als Scored zählen (`scored_bounce_count` Schwelle = 15 ft AGL), aber als Forensic.

## Fixture / Replay

**Pfad:** `tests/fixtures/golden-flights/G2-edqa-bonanza-3-bounce.jsonl.gz`

**Status:** muss aufgezeichnet werden. Svenny hat zugesagt beim ersten v0.9.0-rc.1 die Aufnahme zu machen. Wenn Svenny ausfällt: Adrian kann es im X-Plane simulieren (Bonanza im Pattern ist nicht schwer für einen geübten Piloten).

**Replay-Befehl:**

```bash
$ asa-acars-cli replay tests/fixtures/golden-flights/G2-edqa-bonanza-3-bounce.jsonl.gz \
    --speed 1.0 --output results/G2/replay-$(date +%F)/
```

## Erwartete Phase-Sequence

```
PREFLIGHT  (0:00)  → BOARDING   (0:30)  → PUSHBACK   nicht ausgelöst (GA, kein Pushback)
→ TAXI_OUT (3:00)  → TAKEOFF_ROLL (6:30) → TAKEOFF   (6:45)
→ CLIMB    (7:00)  → CRUISE     nicht ausgelöst (Pattern bleibt unter 3000 ft AGL)
→ DESCENT  nicht ausgelöst (Pattern hat keinen klassischen TOD)
→ APPROACH (10:00) → FINAL      (12:30) → LANDING   (12:55)
→ ARRIVED  (13:30) → TAXI_IN    (13:35) → BLOCKS_ON (15:30) → DEBOARDING (16:00)
```

**Anmerkung:** Pattern-Flüge sind ein Stress-Test für die Phase-StateMachine — sie darf nicht in CRUISE springen, weil der Flug nie ≥3000 ft AGL erreicht. APPROACH/FINAL kommt früh.

## Erwartete PIREP-Felder

| Feld | Erwarteter Wert | Toleranz |
|---|---|---|
| `aircraft_icao` | `"BE36"` ODER `"BE35"` | je nach X-Plane-ICAO |
| `dep` | `"EDQA"` | exakt |
| `arr` | `"EDQA"` | exakt |
| `flight_time_min` | ~13 | 10 ≤ x ≤ 17 |
| `touchdown.vs_fpm` | -350 (Bounce 1) | -400 ≤ x ≤ -250 |
| `touchdown.bounce` | `true` | exakt |
| `touchdown.bounce_count` | `2` | exakt (= scored ≥ 15 ft AGL) |
| `touchdown.forensic_bounce_count` | `3` | exakt (= forensic ≥ 5 ft AGL) |
| `touchdown.scored_bounce_count` | `2` | exakt |
| `touchdown.bounce_max_agl_ft` | ~12.0 | 10 ≤ x ≤ 14 |
| `touchdown.bounces` | Array mit 3 Einträgen | exakt 3 |
| `bounces` (PIREP top-level) | Array mit 3 Einträgen | exakt 3 |

### `bounces[]`-Struktur-Validation

```json
[
  {
    "index": 1,
    "ts_offset_ms": 0,
    "max_agl_ft": 12.0,
    "air_time_ms": 5000,
    "vs_at_touchdown_fpm": -350,
    "g_force": 2.1,
    "ias_kt": 95
  },
  {
    "index": 2,
    "ts_offset_ms": 5000,
    "max_agl_ft": 7.0,
    "air_time_ms": 5000,
    "vs_at_touchdown_fpm": -240,
    "g_force": 1.6,
    "ias_kt": 88
  },
  {
    "index": 3,
    "ts_offset_ms": 10000,
    "max_agl_ft": 3.0,
    "air_time_ms": 2000,
    "vs_at_touchdown_fpm": -200,
    "g_force": 1.4,
    "ias_kt": 82
  }
]
```

**Konsistenz-Check:**

- `bounces.length == forensic_bounce_count` (= 3)
- `count(b: b.max_agl_ft >= 15) <= scored_bounce_count` (= 2 → aber unsere alle <15, also Edge-Case: Achtung — `scored_bounce_count=2` aus dem Touchdown-Detector kommt nicht ausschließlich von max_agl_ft, sondern auch vs_fpm-basiert. Daher kann `scored=2` auch bei max_agl=12/7/3 stimmen, wenn vs_fpm-Schwellen alle drei Touchdowns scored, aber der dritte unter 5 ft fällt. Validation hier ist: `scored_bounce_count` ≤ `forensic_bounce_count` immer.)
- `max(bounces[].max_agl_ft) == bounce_max_agl_ft` (= 12)

**Validation-Skript:**

```bash
$ jq -e '
  .touchdown.bounces|length==3 and
  (.touchdown.bounces|map(.max_agl_ft)|max) == .touchdown.bounce_max_agl_ft and
  (.touchdown.bounces|length) == .touchdown.forensic_bounce_count and
  .touchdown.scored_bounce_count <= .touchdown.forensic_bounce_count
' results/G2/run-$(date +%F)/pirep.json
```

## Erwartete Bounce-Werte

```json
{
  "forensic_bounce_count": 3,
  "scored_bounce_count": 2,
  "bounce_max_agl_ft": 12.0,
  "bounces": [ { "index": 1, ... }, { "index": 2, ... }, { "index": 3, ... } ]
}
```

## Erwartetes GlitchTip-Verhalten

**Erwartete Events:** **1** (genau ein `hard_landing_or_bounce`-Event).

| Event-Type | Tags (Pflicht) | Tags (verboten) |
|---|---|---|
| `hard_landing_or_bounce` | `va_code=gsg`, `pilot_id_hash=...`, `callsign=ASA-VFR-...`, `aircraft_icao=BE36`, `simulator=xplane`, `route=EDQA-EDQA`, `vs_fpm=-350`, `bounce_count=2`, `forensic_bounce_count=3` | `pilot_email`, `phpvms_token`, `pilot_id` (un-gehasht), `pirep_notes_text` |

**Validation:**

```bash
$ curl -s -H "Authorization: Bearer $GLITCHTIP_API_TOKEN" \
    "https://tip.kant.ovh/api/0/projects/asa-acars-qs/issues/?query=callsign:ASA-VFR-Pattern" \
  | jq '[.[] | select(.title|contains("hard_landing"))] | length'
1
```

## Erwartetes Discord-RPC-Verhalten

| Sim-Zeit | Details | State |
|---|---|---|
| 00:00 | `Bonanza · EDQA Pattern` | `PREFLIGHT · BE36` |
| 6:45 | `Bonanza · EDQA Pattern` | `TAKEOFF · BE36` |
| 12:30 | `Bonanza · EDQA Pattern` | `FINAL · BE36` |
| 13:00 | `Bonanza · EDQA Pattern` | `LANDING · BE36` |
| 13:30 | `Bonanza · EDQA Pattern` | `LANDED · BE36 · 3 bounces` (oder ähnliches Detail) |
| 16:00 (PIREP filed) | (gecleart) | — |

**Anmerkung:** Bei 3-Bounce-Landung soll der LANDED-State im RPC den Bounce-Count zeigen (kleine Konversation-Hilfe für Discord-Mitleser).

## Privacy-Check

Wie G1, plus zusätzlich für das eine GlitchTip-Event:

- Im Event-Body: `vs_fpm`, `bounce_count`, `forensic_bounce_count` ok
- `bounces[]`-Array darf in Event-Extras enthalten sein (= Forensik), ABER ohne Pilot-Identifikatoren
- Der `pilot_id_hash` ist SHA-256-gehasht — Pflicht-Prüfung: muss 64 Hex-Zeichen sein, nicht numerisch

```bash
$ jq '.[0].tags.pilot_id_hash' glitchtip-events.json
"5e884898da28047151d0e56f8dc6292773603d0d6aabbdd62a11ef721d1542d8"
$ echo -n "5e88..." | wc -c   # = 64
```

## Pass/Fail-Kriterien

- [ ] Phase-Sequenz wie erwartet (KEIN CRUISE, kein DESCENT)
- [ ] `bounces[]`-Array hat exakt 3 Einträge
- [ ] `forensic_bounce_count == 3`, `scored_bounce_count == 2`, `bounce_max_agl_ft ≈ 12`
- [ ] Konsistenz-Check via jq passt
- [ ] Genau 1 GlitchTip-Event `hard_landing_or_bounce`
- [ ] Privacy-Validation pass (kein PII im Event-Body)
- [ ] Discord-RPC zeigt Bounce-Count im LANDED-State
- [ ] PIREP-JSON enthält `bounces` als top-level Array (= duplikat von `touchdown.bounces`)
- [ ] Tester-Signatur

## Estimated QS-Time

- Sim-Zeit: ~25 min (kurzer Pattern-Flug + 3 Bounces sind heikel zu landen)
- Auswertung: ~30 min (3-Bounce-Konsistenz-Check ist die meiste Arbeit)
- **Total: ~55 min ≈ 1 h**

---

# G3 — RTO bei 38 kt (Aborted Takeoff)

**Test-Pilot:** Thomas K oder Adrian (heikel, braucht entspannten Setup)
**Sim:** MSFS 2024
**Aircraft:** Fenix A320
**Route:** LIRF (Roma Fiumicino) RWY 16L — Lineup, Power, RTO bei 38 kt
**Hauptzweck:** Validierung der NEUEN Phase `REJECTED_TAKE_OFF`. PIREP-Note + `rto_event`-Struktur. Sentry-Tag `rto=true`.

## Reproduktions-Anweisung

### Sim-Setup

```
1. MSFS 2024, Fenix A320, Cold & Dark am Gate LIRF Apron
2. Wetter: Wind „160° / 10 kt", FEW 040, OAT 22°C, QNH 1013
3. Time: 10:00 Local
4. Failures: NONE im Sim (RTO ist Pilot-induziert, kein Engine-Failure-Trigger)
```

### ASA-ACARS-Setup

```
1. v0.9.0-rc.N, Bid „GSG2741 LIRF→EDDF Test"
2. Features: alle Defaults + error_reporting + discord_rpc EIN
3. phases.rejected_take_off_enabled = true (verifizieren, Default ist true)
4. MQTT + Wireshark
```

### Pilot-Aktionen mit Timing

| Sim-Zeit (mm:ss) | Aktion |
|---|---|
| 00:00 | Flight Start, Cold & Dark |
| 02:00 | Boarding, APU on |
| 08:00 | Engine Start, Taxi to RWY 16L |
| 12:00 | Lineup RWY 16L |
| 12:30 | TOGA Power set |
| 12:40 | 30 kt — beschleunigen |
| 12:45 | **38 kt — Master Caution-Reflex: Thrust Levers auf IDLE + Reverse, max Braking** |
| 12:50 | Aircraft kommt zum Stehen ca. 200 m vor RWY-Ende |
| 13:30 | Taxi vom RWY runter (KEIN zweiter Takeoff-Versuch — Test endet hier) |
| 16:00 | Taxi to Gate, Park, „Cancel Flight" oder „File PIREP" |

**Wichtig für Phase-StateMachine:**

- Trigger `TAKEOFF_ROLL` muss bei `gs_kt > 30 && on_ground=true && phase==TAXI_OUT` greifen
- Trigger `REJECTED_TAKE_OFF` muss bei `phase==TAKEOFF_ROLL && gs_kt deceleration ≥ X fpm && back on TAXI_OUT speeds` greifen
- Keine `TAKEOFF`-Phase darf gefeuert werden (= wir kommen nie über die V1-Schwelle, geschweige denn airborne)

## Fixture / Replay

**Pfad:** `tests/fixtures/golden-flights/G3-lirf-rto-38kt.jsonl.gz`

**Status:** muss aufgezeichnet werden. Thomas oder Adrian beim ersten v0.9.0-rc.1.

## Erwartete Phase-Sequence

```
PREFLIGHT  (0:00)  → BOARDING       (2:00)  → PUSHBACK     (8:00)
→ TAXI_OUT (9:30)  → TAKEOFF_ROLL   (12:30) → REJECTED_TAKE_OFF (12:45)
→ TAXI_OUT (12:50) → BLOCKS_ON      (16:00) → DEBOARDING   (16:30)
```

**Negativ-Erwartung:** KEIN `TAKEOFF`, KEIN `CLIMB`, KEIN `LANDING`, KEIN `ARRIVED` (= nie airborne gewesen).

**Schwierige Frage:** Geht die Phase nach `REJECTED_TAKE_OFF` zurück zu `TAXI_OUT` oder direkt zu `BLOCKS_ON`? Per Expanded-Phases-Spec: zurück zu `TAXI_OUT`, weil der Pilot ja vom RWY runter taxen muss bevor er parkt. Danach normal `BLOCKS_ON → DEBOARDING`.

## Erwartete PIREP-Felder

| Feld | Erwarteter Wert | Toleranz |
|---|---|---|
| `callsign` | `"GSG2741"` | exakt |
| `dep` | `"LIRF"` | exakt |
| `arr` | `"EDDF"` | exakt (bleibt der Bid-Wert, auch wenn Flug nie stattfand) |
| `flight_time_min` | ~0 (nie airborne) | 0 ≤ x ≤ 3 |
| `block_time_min` | ~16 | 12 ≤ x ≤ 20 |
| `landed` | `false` ODER `null` | KEIN Touchdown |
| `touchdown` | nicht vorhanden | exakt |
| `bounces` | nicht vorhanden | exakt |
| `rto_event` | Objekt vorhanden | Pflicht |
| `rto_event.airport` | `"LIRF"` | exakt |
| `rto_event.runway` | `"16L"` | exakt |
| `rto_event.peak_speed_kt` | `38` | 36 ≤ x ≤ 42 |
| `rto_event.ts_start` | (Timestamp Power-Set) | innerhalb 5 s der Aktion |
| `rto_event.ts_aborted` | (Timestamp 38 kt) | dito |
| `rto_event.ts_stopped` | (Timestamp Stillstand) | dito |
| `rto_event.deceleration_distance_m` | ~250 | 150 ≤ x ≤ 400 |
| `pirep_notes` | enthält Substring `"RTO at LIRF 16L · aborted at 38 kt"` | exakt diese Format-Vorgabe |

**Validation-Skript:**

```bash
$ jq -e '
  .rto_event != null and
  .rto_event.airport=="LIRF" and
  .rto_event.runway=="16L" and
  (.rto_event.peak_speed_kt >= 36 and .rto_event.peak_speed_kt <= 42) and
  (.pirep_notes|contains("RTO at LIRF 16L")) and
  (.touchdown == null or .touchdown.ts == null)
' results/G3/run-$(date +%F)/pirep.json
```

## Erwartete Bounce-Werte

Nicht relevant (nie airborne):

```json
{
  "touchdown": null,
  "bounces": null
}
```

## Erwartetes GlitchTip-Verhalten

**Erwartete Events:** **1** (genau ein `rejected_takeoff`-Event als Forensik).

| Tag-Schlüssel | Erwarteter Wert |
|---|---|
| `event_type` | `"rejected_takeoff"` |
| `va_code` | `"gsg"` |
| `pilot_id_hash` | SHA-256-Hash, 64 Hex |
| `callsign` | `"GSG2741"` |
| `aircraft_icao` | `"A320"` |
| `simulator` | `"msfs"` |
| `route` | `"LIRF-EDDF"` |
| `airport` | `"LIRF"` |
| `runway` | `"16L"` |
| `peak_speed_kt` | `38` |
| `phase_at_event` | `"REJECTED_TAKE_OFF"` |
| `client_version` | `"0.9.0-rc.N"` |

**Validation:** wie G2 mit angepasstem Query.

## Erwartetes Discord-RPC-Verhalten

| Sim-Zeit | Details | State |
|---|---|---|
| 12:30 | `GSG2741 · LIRF → EDDF` | `TAKEOFF · A320` |
| 12:46 | `GSG2741 · LIRF → EDDF` | `REJECTED TAKEOFF · A320 · 38 kt` |
| 16:30 (PIREP filed) | (gecleart) | — |

**Pflicht:** Die `REJECTED TAKEOFF`-Anzeige im RPC ist sofort sichtbar (≤ 1 s) — Pilots im Discord-Channel sehen sofort dass ein Mitspieler eine RTO hatte (Gesprächs-Anfang).

## Privacy-Check

Wie G1/G2, mit zusätzlicher Pflicht:

- `peak_speed_kt` darf gesendet werden (= Sachfakt, keine PII)
- Die geographische Position des RTO (`lat`/`lon` des Stop-Punktes) darf NICHT als Event-Tag enthalten sein (= das ist Pilot-Bewegung — auch wenn auf einem öffentlichen Flughafen, lieber konservativ). Stattdessen nur `airport=LIRF` + `runway=16L`.

## Pass/Fail-Kriterien

- [ ] Phase-Sequenz enthält `REJECTED_TAKE_OFF`, KEIN `TAKEOFF` und KEIN `CLIMB`
- [ ] PIREP-`rto_event` korrekt befüllt
- [ ] `pirep_notes` enthält den vorgegebenen Substring
- [ ] Genau 1 GlitchTip-Event vom Type `rejected_takeoff`
- [ ] Discord-RPC zeigte `REJECTED TAKEOFF` ≤ 1 s nach Trigger
- [ ] Privacy: keine `lat`/`lon` in GlitchTip-Tags
- [ ] Tester-Signatur

## Estimated QS-Time

- Sim-Zeit: ~15 min
- Auswertung: ~25 min (Phase-Sequenz-Validation, neues Feld `rto_event` ist heikel)
- **Total: ~40 min ≈ 0.7 h**

---

# G4 — Doppelter GO_AROUND bei 800 ft, dann Landung

**Test-Pilot:** Adrian
**Sim:** MSFS 2024
**Aircraft:** Fenix A320
**Route:** EDDF (Frankfurt) RWY 25L — Approach, 1. GO_AROUND, Pattern, 2. GO_AROUND, dann Landung
**Hauptzweck:** Validierung NEUE Phase `GO_AROUND`. Mehrfach-Auftreten (`go_around_events[]` mit 2 Einträgen). Phase-State-Maschine bei wiederholtem Pull-Up.

## Reproduktions-Anweisung

### Sim-Setup

```
1. MSFS 2024, Fenix A320, am EDDF Gate (von vorherigem CRZ-Test übernehmen oder neu Cold)
   Vereinfachung: direkt am Final Approach starten, 12 nm aus, FL080.
2. Wetter: Wind „240° / 8 kt", BKN 800, OAT 5°C, QNH 1019
   (BKN 800 ist Trigger für realistisches GO_AROUND wegen Wolken-Untergrenze)
3. Time: 16:00 Local
4. Failures: NONE
```

### ASA-ACARS-Setup

```
1. v0.9.0-rc.N, Bid „GSG1108 EDDS→EDDF" (= übergreifender Flug, wir starten mid-Approach)
2. Features: alle Defaults + error_reporting + discord_rpc EIN
3. phases.go_around_enabled = true (Default true)
4. MQTT + Wireshark
```

### Pilot-Aktionen mit Timing

| Sim-Zeit (mm:ss) | Aktion |
|---|---|
| 00:00 | Flight Start, A320 12 nm aus EDDF, FL080 |
| 03:00 | Localizer alive, Glideslope alive (Phase: APPROACH) |
| 05:00 | 2000 ft AGL, full Configuration (Flaps Full, Gear Down) — Phase: FINAL |
| 06:30 | 1000 ft AGL — Phase: LANDING |
| 07:00 | **800 ft AGL — TOGA, Pitch up, Gear up, Flaps 1** (= 1. GO_AROUND) |
| 07:30 | Climb to 3000 ft, Radar-Vector, Pattern |
| 13:00 | Re-Approach, wieder Localizer, FINAL bei 2000 ft |
| 15:00 | 1000 ft AGL, LANDING |
| 15:30 | **800 ft AGL wieder — TOGA + Pitch up + Gear up** (= 2. GO_AROUND) |
| 16:00 | Pattern wieder, Approach #3 |
| 22:00 | LANDING #3 — diesmal sauber landen bei 200 fpm |
| 22:30 | Vacate, Taxi to Gate |
| 25:00 | Park, Engines off |
| 26:00 | File PIREP |

**Wichtig für Phase-StateMachine:**

- Phase-Sequence bei jedem GO_AROUND: `LANDING → GO_AROUND` (nicht zurück zu `FINAL`)
- Nach `GO_AROUND`: zurück zu `CLIMB` (nicht zu `APPROACH` — das ist Pattern-Climb, kein neuer Anflug)
- Beim erneuten Anflug: `APPROACH → FINAL → LANDING` wie üblich

## Fixture / Replay

**Pfad:** `tests/fixtures/golden-flights/G4-eddf-double-go-around.jsonl.gz`

**Status:** muss aufgezeichnet werden.

## Erwartete Phase-Sequence

```
APPROACH    (3:00)   → FINAL    (5:00)   → LANDING   (6:30)
→ GO_AROUND (7:00)   → CLIMB    (7:30)
→ APPROACH  (13:00)  → FINAL    (13:30)  → LANDING   (15:00)
→ GO_AROUND (15:30)  → CLIMB    (16:00)
→ APPROACH  (18:00)  → FINAL    (19:30)  → LANDING   (21:30)
→ ARRIVED   (22:00)  → TAXI_IN  (22:30)  → BLOCKS_ON (25:00) → DEBOARDING (26:00)
```

**Validation:** `GO_AROUND` muss 2× im Phase-Stream auftauchen, dazwischen `CLIMB → APPROACH → FINAL → LANDING`.

## Erwartete PIREP-Felder

| Feld | Erwarteter Wert | Toleranz |
|---|---|---|
| `callsign` | `"GSG1108"` | exakt |
| `dep` | `"EDDS"` | exakt |
| `arr` | `"EDDF"` | exakt |
| `flight_time_min` | ~25 | 22 ≤ x ≤ 30 |
| `touchdown.airport` | `"EDDF"` | exakt |
| `touchdown.runway` | `"25L"` | exakt |
| `touchdown.vs_fpm` | -200 | -300 ≤ x ≤ -150 |
| `touchdown.bounce` | `false` | exakt |
| `go_around_events` | Array mit 2 Einträgen | exakt 2 |
| `pirep_notes` | enthält `"2× GO_AROUND at EDDF · 25L"` | exakt diese Format-Vorgabe |

### `go_around_events[]`-Struktur

```json
[
  {
    "index": 1,
    "ts": 1747500000000,
    "airport": "EDDF",
    "runway": "25L",
    "agl_at_decision_ft": 800,
    "reason": "pilot_initiated",
    "ts_resumed_climb": 1747500030000
  },
  {
    "index": 2,
    "ts": 1747500930000,
    "airport": "EDDF",
    "runway": "25L",
    "agl_at_decision_ft": 800,
    "reason": "pilot_initiated",
    "ts_resumed_climb": 1747500960000
  }
]
```

**Validation:**

```bash
$ jq -e '
  (.go_around_events|length)==2 and
  .go_around_events[0].airport=="EDDF" and
  .go_around_events[1].agl_at_decision_ft >= 700 and
  .go_around_events[1].agl_at_decision_ft <= 900 and
  .pirep_notes | contains("2× GO_AROUND at EDDF")
' results/G4/run-$(date +%F)/pirep.json
```

## Erwartete Bounce-Werte

Nach der finalen Landung (= 3. Approach):

```json
{
  "forensic_bounce_count": 0,
  "scored_bounce_count": 0,
  "bounce_max_agl_ft": null,
  "bounces": null
}
```

## Erwartetes GlitchTip-Verhalten

**Erwartete Events:** **2** (ein `go_around`-Event pro GO_AROUND).

Tags pro Event:

| Tag | Wert (Event 1 / Event 2) |
|---|---|
| `event_type` | `"go_around"` |
| `airport` | `"EDDF"` |
| `runway` | `"25L"` |
| `agl_at_decision_ft` | `800` / `800` |
| `attempt_number` | `1` / `2` |
| `aircraft_icao` | `"A320"` |
| `simulator` | `"msfs"` |
| `pilot_id_hash` | SHA-256 |

## Erwartetes Discord-RPC-Verhalten

| Sim-Zeit | Details | State |
|---|---|---|
| 6:30 | `GSG1108 · EDDS → EDDF` | `LANDING · A320` |
| 7:00 | `GSG1108 · EDDS → EDDF` | `GO_AROUND · A320 · 800 ft` |
| 7:30 | `GSG1108 · EDDS → EDDF` | `CLIMB · A320` |
| ... (Pattern) ... | | |
| 15:30 | `GSG1108 · EDDS → EDDF` | `GO_AROUND · A320 · 800 ft (2nd)` |
| 22:00 | `GSG1108 · EDDS → EDDF` | `LANDED · A320 · -200 fpm` |

**Pflicht:** Der „(2nd)"-Hinweis im State zeigt sofort dass es ein wiederholter GO_AROUND ist.

## Privacy-Check

- `agl_at_decision_ft` ist erlaubt (Sachfakt)
- `lat`/`lon` der Decision-Höhe NICHT als Tag (= Pilot-Spur)
- Nur `airport`+`runway` als geo-Tags

## Pass/Fail-Kriterien

- [ ] Phase-Sequenz enthält `GO_AROUND` exakt 2×
- [ ] Zwischen den GO_AROUNDs `CLIMB → APPROACH → FINAL → LANDING`
- [ ] `go_around_events[]` hat 2 Einträge mit korrekten Index/Timestamps
- [ ] `pirep_notes` enthält den vorgegebenen Substring
- [ ] Exakt 2 GlitchTip-Events vom Type `go_around` (mit `attempt_number=1` und `=2`)
- [ ] Discord-RPC zeigte beide GO_AROUNDs korrekt (mit „(2nd)"-Hinweis beim zweiten)
- [ ] Finaler Touchdown ist sauber (`bounce=false`, `score≥80`)
- [ ] Privacy: keine `lat`/`lon` in Tags
- [ ] Tester-Signatur

## Estimated QS-Time

- Sim-Zeit: ~40 min (3 Approaches sind zeitintensiv)
- Auswertung: ~30 min
- **Total: ~70 min ≈ 1.2 h**

---

# G5 — Pause/Slew/Sim-Disconnect mitten im Flug

**Test-Pilot:** Thomas K
**Sim:** MSFS 2024
**Aircraft:** beliebig (Default-Cessna ok — Test prüft ASA-ACARS-Resilience, nicht Aircraft-Modell)
**Route:** beliebig — kurzer Hop EDDB→EDDF
**Hauptzweck:** Validierung der Sim-Disconnect-Auto-Resume-Logik (siehe `docs/spec/sim-disconnect-auto-resume.md`). Phase-StateMachine darf nicht in falschen State springen. GlitchTip bekommt `simconnect_disconnect`-Event. Discord-RPC zeigt „Paused"-Hinweis.

## Reproduktions-Anweisung

### Sim-Setup

```
1. MSFS 2024, Default-Cessna 172 G1000, am EDDB Apron
2. Wetter: Standard CAVOK
3. Time: 14:00 Local
```

### ASA-ACARS-Setup

```
1. v0.9.0-rc.N, Bid „ASA-TEST EDDB→EDDF" (oder ohne Bid, Free-Flight-Mode)
2. Features: alle Defaults + error_reporting + discord_rpc EIN
3. MQTT + Wireshark
```

### Pilot-Aktionen mit Timing

| Sim-Zeit (mm:ss) | Aktion | Erwartung |
|---|---|---|
| 00:00 | Flight Start, Cold | PREFLIGHT |
| 02:00 | Engine Start, Taxi, Takeoff | TAKEOFF |
| 05:00 | Climb to 3000 ft | CLIMB |
| 08:00 | Level off bei 3000 ft, En Route | CRUISE |
| 12:00 | **MSFS Pause-Taste (Esc → Pause Menu)** | Phase friert ein |
| 12:30 | 30 Sekunden warten, dann Pause aufheben | Phase resumed identisch |
| 14:00 | **Slew-Mode aktivieren (`Y`-Taste), 50 nm nach Norden teleportieren** | Activity-Log warnt |
| 14:30 | Slew-Mode deaktivieren | Phase normal weiter |
| 16:00 | **MSFS-Fenster schließen (Alt+F4 oder via Taskleiste)** | SimConnect-Disconnect-Event |
| 16:30 | 60 s warten, MSFS neu starten, Free Flight am gleichen Punkt loaden | SimConnect-Reconnect erkannt |
| 18:00 | Weiter fliegen normal, Approach + Landing EDDF | Phase normal |
| 22:00 | File PIREP | |

## Fixture / Replay

**Pfad:** `tests/fixtures/golden-flights/G5-pause-slew-disconnect.jsonl.gz`

**Status:** muss aufgezeichnet werden. Achtung: Pause + Slew + Disconnect sind schwer im Replay zu reproduzieren — Replay-Modus für diesen Test ist nur teilweise nutzbar.

## Erwartete Phase-Sequence

```
PREFLIGHT (0:00) → BOARDING (0:30) → TAXI_OUT (2:30) → TAKEOFF (4:30) → CLIMB (5:00)
→ CRUISE   (8:00)
→ CRUISE   (12:00) [PAUSE — Phase BLEIBT CRUISE, Position-Stream pausiert]
→ CRUISE   (12:30) [RESUME — Stream resumed]
→ CRUISE   (14:00) [SLEW — Activity-Log warnt: „slew_detected"]
→ CRUISE   (16:00) [DISCONNECT — Stream stoppt, kein neuer Phase]
→ CRUISE   (17:30) [RECONNECT — Stream resumed]
→ DESCENT  (19:00) → APPROACH (20:00) → FINAL (20:30) → LANDING (21:00)
→ ARRIVED  (21:15) → TAXI_IN (21:30) → BLOCKS_ON (21:45) → DEBOARDING (22:00)
```

**Wichtig:** Während Pause/Disconnect darf keine falsche Phase auftauchen (z.B. nicht plötzlich `LANDING` weil `vs_fpm=0`).

## Erwartete PIREP-Felder

| Feld | Erwarteter Wert | Toleranz |
|---|---|---|
| `flight_time_min` | ~22 | 18 ≤ x ≤ 28 |
| `dep` | `"EDDB"` | exakt |
| `arr` | `"EDDF"` | exakt |
| `touchdown.airport` | `"EDDF"` | exakt |
| `flags.had_sim_pause` | `true` | exakt |
| `flags.had_sim_disconnect` | `true` | exakt |
| `flags.had_slew_detected` | `true` | exakt |
| `pirep_notes` | enthält „Sim disconnect at 16:00, resumed at 17:30" | Format-Vorgabe |

## Erwartete Bounce-Werte

Standard-Landung erwartet:

```json
{"forensic_bounce_count": 0, "scored_bounce_count": 0, "bounces": null}
```

## Erwartetes GlitchTip-Verhalten

**Erwartete Events:** **2 (mindestens)**

| Event | Tags |
|---|---|
| `simconnect_disconnect` | `va_code`, `pilot_id_hash`, `callsign`, `aircraft_icao=C172`, `ts_disconnect`, `last_known_phase=CRUISE`, `last_known_lat/lon` (nur grober Bereich, ggf. geclustert auf Airport) |
| `simconnect_reconnect` | dito + `ts_reconnect`, `gap_duration_ms` |

**Optional:** `slew_detected`-Event, wenn Spec das vorschreibt.

## Erwartetes Discord-RPC-Verhalten

| Sim-Zeit | Details | State |
|---|---|---|
| 8:00 | `Test · EDDB → EDDF` | `CRUISE · C172` |
| 12:00 | `Test · EDDB → EDDF` | `PAUSED · C172` |
| 12:30 | `Test · EDDB → EDDF` | `CRUISE · C172` |
| 14:00 | `Test · EDDB → EDDF` | `CRUISE · C172 · ⚠ slew` (oder unverändert) |
| 16:00 | `Test · EDDB → EDDF` | `DISCONNECTED · C172` |
| 17:30 | `Test · EDDB → EDDF` | `CRUISE · C172` |

**Pflicht:** Bei Disconnect muss RPC binnen 10 s den „DISCONNECTED"-Hint zeigen. Bei Reconnect spätestens 5 s nach Stream-Resume zurück zu normalem Phase-State.

## Privacy-Check

- `last_known_lat/lon` in `simconnect_disconnect`-Event: Erlaubt NUR wenn auf den nächsten Airport gerundet (z.B. `nearest_airport=EDDB`). Wenn raw lat/lon mit 6 Nachkommastellen: Fail.
- Sonst Standard-Negativliste.

## Pass/Fail-Kriterien

- [ ] Phase friert bei Pause ein (keine ungewünschten Transitions)
- [ ] Bei Disconnect kein Crash, kein Phase-Reset
- [ ] Bei Reconnect saubere Fortsetzung
- [ ] PIREP-Flags `had_sim_pause`, `had_sim_disconnect`, `had_slew_detected` korrekt
- [ ] Mind. 2 GlitchTip-Events (`simconnect_disconnect` + `_reconnect`)
- [ ] Discord-RPC zeigt PAUSED + DISCONNECTED korrekt
- [ ] Privacy: `last_known_lat/lon` ist gerundet oder durch `nearest_airport` ersetzt
- [ ] Tester-Signatur

## Estimated QS-Time

- Sim-Zeit: ~25 min (inkl. MSFS-Neustart)
- Auswertung: ~20 min
- **Total: ~45 min ≈ 0.75 h**

---

# G6 — Schlechte FPS / Telemetrie-Lücken (Heartbeat-Test)

**Test-Pilot:** Adrian (Replay-Modus)
**Sim:** n/a (Replay-only)
**Hauptzweck:** Validierung Delta-Sampling-Heartbeat bei artifiziell injizierten Telemetrie-Gaps von 5–10 Sekunden. Force-Publish bei Phase-Transition muss trotzdem greifen. Bounce-Detection muss trotz weniger Samples funktionieren (`forensics_version=2`).

## Reproduktions-Anweisung

### Setup

Dieser Test läuft im Replay-Modus mit einer **modifizierten Fixture**, die künstlich Lücken hat.

```bash
# 1. Standard-Fixture nehmen (z.B. G1)
$ cp tests/fixtures/golden-flights/G1-eddb-eddm-a320.jsonl.gz /tmp/G6-base.jsonl.gz
$ gunzip /tmp/G6-base.jsonl.gz

# 2. Mit dem Gap-Injector-Tool Lücken einfügen
$ asa-acars-cli inject-gaps /tmp/G6-base.jsonl \
    --gap-prob 0.3 \
    --gap-duration-ms 5000-10000 \
    --seed 42 \
    --output tests/fixtures/golden-flights/G6-fps-gaps.jsonl

# 3. Komprimieren + ablegen
$ gzip tests/fixtures/golden-flights/G6-fps-gaps.jsonl

# 4. Replay starten
$ asa-acars-cli replay tests/fixtures/golden-flights/G6-fps-gaps.jsonl.gz \
    --output results/G6/replay-$(date +%F)/
```

`--gap-prob 0.3` = 30% der Samples werden gedroppt, künstlich Gaps eingefügt.

### ASA-ACARS-Setup

```
1. v0.9.0-rc.N im Replay-Modus
2. Features: alle Defaults + error_reporting EIN (Discord nicht relevant im Replay)
3. position_sampling.delta_enabled = true (Pflicht für diesen Test)
4. MQTT-Capture
```

## Fixture / Replay

**Pfad:** `tests/fixtures/golden-flights/G6-fps-gaps.jsonl.gz`

**Status:** abgeleitet aus G1-Fixture via `inject-gaps`-Tool. Tool muss als Teil von v0.9.0 mitgeliefert werden — Implementation-Aufwand ~2 h, ist im Spec-Aufwand-Budget.

## Erwartete Phase-Sequence

Identisch zu G1 — alle Phase-Transitions müssen trotz Gaps detektiert werden.

```
PREFLIGHT → BOARDING → PUSHBACK → TAXI_OUT → TAKEOFF_ROLL → TAKEOFF
→ CLIMB → CRUISE → DESCENT → APPROACH → FINAL → LANDING → ARRIVED
→ TAXI_IN → BLOCKS_ON → DEBOARDING
```

**Pflicht:** Force-Publish bei jeder Phase-Transition feuert auch wenn Sim-Snap fehlt — die StateMachine arbeitet auf dem letzten verfügbaren Sample.

## Erwartete PIREP-Felder

Identisch zu G1. Aber ZUSÄTZLICH:

| Feld | Erwarteter Wert | Toleranz |
|---|---|---|
| `telemetry_quality.sample_count` | ~70% der G1-Referenz | 65% ≤ x ≤ 75% |
| `telemetry_quality.gap_count` | > 50 | min. 30 |
| `telemetry_quality.max_gap_ms` | ~10000 | max ≤ 12000 |
| `telemetry_quality.heartbeat_count` | mindestens 1 pro 60 s Sim-Zeit | n_heartbeats ≥ flight_time_min |

## Erwartete Bounce-Werte

```json
{"forensic_bounce_count": 0, "scored_bounce_count": 0, "bounces": null}
```

(G1 hat keinen Bounce — Gaps ändern das nicht.)

## Erwartetes GlitchTip-Verhalten

**Erwartete Events:** **0 bis 1**

Ein `telemetry_gap_quality_warning`-Event darf gefeuert werden wenn `max_gap_ms > 8000` UND `gap_count > 100`. Wenn nicht: 0 Events.

Bei G6-Standard-Setup mit gap-prob=0.3 erwarten wir **1** Warning.

## Erwartetes Discord-RPC-Verhalten

Nicht relevant im Replay-Modus (Discord wird nicht initialisiert).

Bei Live-Sim-Variante: RPC-Updates kommen mit Gaps verzögert — soll trotzdem alle 60 s mind. einmal aktualisiert werden (Heartbeat).

## Privacy-Check

Standard-Negativliste.

## Pass/Fail-Kriterien

- [ ] Alle G1-Phase-Transitions vorhanden im MQTT-Capture trotz Gaps
- [ ] `telemetry_quality.sample_count` ist 65–75% von G1-Referenz
- [ ] Heartbeat-Frequenz min. 1 pro 60 s (Delta-Sampling-Spec)
- [ ] Bounce-Werte identisch zu G1 (= keine, weil G1-Base bouncefrei)
- [ ] 0 oder 1 GlitchTip-Event (`telemetry_gap_quality_warning`)
- [ ] Tester-Signatur

## Estimated QS-Time

- Sim-Zeit: 0 (Replay)
- Auswertung: ~30 min (Vergleich mit G1-Referenz ist die meiste Arbeit)
- **Total: ~30 min ≈ 0.5 h**

---

# G7 — Alter v0.8.4-PIREP wird in v0.9.0 Webapp geöffnet (Backward-Compat)

**Test-Pilot:** Thomas K
**Sim:** n/a (Webapp-Test)
**Hauptzweck:** Validierung Backward-Compat-Strategie aus Telemetry-Contract Sektion 5. Ein PIREP ohne neue Felder (= aufgenommen mit ASA-ACARS v0.8.4) darf in der v0.9.0-Webapp keinen JS-Error werfen, keine fehlenden UI-Pills, keine Crashes.

## Reproduktions-Anweisung

### Setup

```bash
# 1. Echten v0.8.4-PIREP aus dem Recorder-Archiv finden
$ asa-acars-recorder query --version "0.8.4" --limit 1
{"pirep_id": "abc123", "callsign": "GSG2156", "aircraft": "B738", ...}

# 2. PIREP-JSON exportieren
$ asa-acars-recorder dump --pirep abc123 > /tmp/legacy-pirep.json

# 3. Verifizieren: keine neuen v0.9.0-Felder
$ jq '{bounces, rto_event, go_around_events}' /tmp/legacy-pirep.json
{"bounces": null, "rto_event": null, "go_around_events": null}

# 4. v0.9.0-Webapp starten (lokal oder via VPS-Staging)
$ cd webapp && npm run preview
# Öffnet http://localhost:4173

# 5. Browser öffnen: /pirep/abc123
```

### Auswertung

Mit Browser-DevTools (F12, Tab „Console"):

```
1. Page laden /pirep/abc123
2. Console auf JS-Errors prüfen — Soll: KEINE Errors, max. 1 Warning ok
3. UI-Elemente prüfen:
   - LandingPanel: zeigt Score, vs_fpm — KEINE bounces[]-Sektion
   - PhasePanel: zeigt Standard-Phasen — KEINE Pills für RTO/GO_AROUND/DEBOARDING
   - PirepNotes: zeigt Original-Notes ohne v0.9.0-Note-Inserts
4. Network-Tab: alle API-Calls erfolgreich (= 200), keine 500er, keine fehlenden Endpoints
5. Page-Source-View: keine `undefined`-Renderings (= „undefined" als String im DOM)
```

## Fixture / Replay

**Pfad:** `tests/fixtures/golden-flights/G7-legacy-v084-pirep.json`

**Status:** muss aus Recorder-Archiv exportiert + committed werden. Größe ~50 KB JSON.

```bash
$ ls tests/fixtures/golden-flights/G7-legacy-v084-pirep.json
-rw-r--r-- 1 user staff 47892 May 18 12:34 G7-legacy-v084-pirep.json
```

**Bevorzugter PIREP:** ein bouncefreier 60-Min-Flug aus v0.8.4 (= Mainstream-Case, repräsentativ).

## Erwartete Phase-Sequence

n/a (kein Live-Flug, nur Webapp-Anzeige eines abgeschlossenen PIREP).

## Erwartete PIREP-Felder

Genau wie der Original-PIREP — keine Modifikation. v0.9.0-Webapp darf NIE den PIREP modifizieren beim Anzeigen.

## Erwartete Bounce-Werte

`null` / fehlend — was der Original-PIREP eben hat.

## Erwartetes GlitchTip-Verhalten

**Erwartete Events:** **0**

Wenn Webapp einen JS-Error wirft (= unbehandelter `Cannot read property 'length' of undefined` o.ä.), würde das via Sentry-Browser-SDK auch in GlitchTip landen. Wir wollen **0** Webapp-Errors.

```bash
$ curl -s -H "Authorization: Bearer $GLITCHTIP_API_TOKEN" \
    "https://tip.kant.ovh/api/0/projects/asa-acars-qs/issues/?query=age:5m+project:webapp" \
  | jq 'length'
0
```

## Erwartetes Discord-RPC-Verhalten

n/a (Webapp hat kein Discord-RPC).

## Privacy-Check

n/a (kein neuer Datenverkehr — nur PIREP-Anzeige).

## Pass/Fail-Kriterien

- [ ] PIREP-Page lädt ohne JS-Errors in Browser-Console
- [ ] LandingPanel zeigt ohne `bounces[]`-Sektion, kein Crash
- [ ] PhasePanel rendert alle Standard-Phasen, neue Pills (RTO/GO_AROUND/DEBOARDING) gar nicht sichtbar (= sind nicht im PIREP, soll auch nichts erscheinen)
- [ ] PirepNotes zeigt Original-Inhalt
- [ ] Keine `undefined`-Strings im DOM
- [ ] 0 neue GlitchTip-Events von der Webapp
- [ ] Network-Tab: alle Requests grün
- [ ] Tester-Signatur

## Estimated QS-Time

- Sim-Zeit: 0
- Auswertung: ~15 min (Webapp-Check + Console-Inspektion)
- **Total: ~15 min**

---

# G8 — GlitchTip offline (SDK-Buffer-Test)

**Test-Pilot:** Thomas K
**Sim:** MSFS 2024 (kurzer Pattern-Flug)
**Aircraft:** beliebig
**Hauptzweck:** Validierung der GlitchTip-SDK-Resilience bei Server-Ausfall. Pilot-Client darf nicht crashen, Events landen in lokalem Buffer, beim Reconnect wird gefluscht.

## Reproduktions-Anweisung

### Setup

```bash
# 1. GlitchTip-Test-Instanz absichtlich offline nehmen
#    (= NGINX vor GlitchTip blockt)
$ ssh root@tip.kant.ovh "systemctl stop nginx"

# 2. Pilot-Client v0.9.0-rc.N starten
# 3. Bid auswählen, Sim starten
# 4. Einen Flug machen mit GUARANTEED Sentry-Event
#    = z.B. Hard-Landing absichtlich (Bonanza Pattern mit Bounce)
```

### Setup im Pilot-Client

```
1. v0.9.0-rc.N, Bid „ASA-VFR Pattern"
2. error_reporting.enabled = true (mit korrekter DSN auf den offline-Server)
3. discord_rpc.enabled = false (nicht relevant)
4. MQTT-Capture
```

### Pilot-Aktionen

| Sim-Zeit (mm:ss) | Aktion |
|---|---|
| 00:00 | Flight Start |
| 03:00 | Pattern, Final, **Hard Landing** (-450 fpm, garantierte Hard-Bounce-Sentry-Auslösung) |
| 04:00 | Roll-out, File PIREP |
| 06:00 | Pilot-Client weiter offen lassen (für Reconnect-Test) |
| 10:00 | **GlitchTip wieder online nehmen:** `ssh root@tip.kant.ovh "systemctl start nginx"` |
| 11:00 | 60 s warten, Pilot-Client soll automatisch Buffer flushen |
| 12:00 | GlitchTip-Issue-Liste prüfen — der Hard-Bounce-Event muss jetzt angekommen sein |

## Fixture / Replay

**Pfad:** `tests/fixtures/golden-flights/G8-glitchtip-offline.jsonl.gz` (optional — manueller Test)

**Status:** Replay schwer reproduzierbar (Server-Status ist extern). Live-Test bevorzugt.

## Erwartete Phase-Sequence

Trivial (Pattern-Flug). Nicht im Fokus dieses Tests.

## Erwartete PIREP-Felder

Trivial. Hard-Landing-Werte:

| Feld | Erwarteter Wert |
|---|---|
| `touchdown.vs_fpm` | -450 |
| `touchdown.score` | < 50 (= „Poor"-Range) |

## Erwartete Bounce-Werte

Je nach exakter Aktion 0–2 Bounces. Nicht im Fokus.

## Erwartetes GlitchTip-Verhalten

**Während GlitchTip offline:** 0 sichtbare Events. Pilot-Client puffert intern (= Sentry-SDK-Standard-Verhalten, „transport.send_envelope returns error → enqueue").

**Nach Reconnect:** Hard-Landing-Event taucht auf. Spätestens 60 s nach Reconnect.

**Pflicht:** KEIN Crash im Pilot-Client während Offline-Phase. KEIN UI-Freeze.

**Validation während Offline:**

```bash
# Pilot-Client-Log
$ grep -i "sentry\|glitchtip" ~/.asa-acars/logs/client.log | tail -20
[2026-05-18 14:03:15] WARN sentry: transport send failed: HTTP 502
[2026-05-18 14:03:15] DEBUG sentry: enqueued to local buffer, queue size: 1
[2026-05-18 14:04:15] WARN sentry: transport send failed: HTTP 502
[2026-05-18 14:04:15] DEBUG sentry: enqueued to local buffer, queue size: 1
...
```

**Validation nach Reconnect:**

```bash
$ curl -s -H "Authorization: Bearer $GLITCHTIP_API_TOKEN" \
    "https://tip.kant.ovh/api/0/projects/asa-acars-qs/issues/?query=age:15m" \
  | jq '.[] | select(.title|contains("hard_landing"))'
{...event...}
```

## Erwartetes Discord-RPC-Verhalten

n/a (Discord aus).

## Privacy-Check

n/a in der Offline-Phase. Nach Reconnect Standard-Privacy-Check des gefluschten Events.

## Pass/Fail-Kriterien

- [ ] Pilot-Client crasht NICHT während GlitchTip-Offline-Phase
- [ ] UI bleibt responsive
- [ ] PIREP wird normal gefiled
- [ ] Client-Log zeigt „enqueued to local buffer"
- [ ] Nach Reconnect: Event taucht in GlitchTip auf (≤ 60 s nach Reconnect)
- [ ] Tester-Signatur

## Estimated QS-Time

- Sim-Zeit: ~10 min
- Auswertung: ~20 min (Buffer-Log-Inspektion + Wait-for-Reconnect)
- **Total: ~30 min ≈ 0.5 h**

---

# G9 — Discord nicht installiert (Fallback-Test)

**Test-Pilot:** Michel (hat Discord absichtlich nicht auf Test-Maschine)
**Sim:** MSFS 2024
**Aircraft:** beliebig
**Hauptzweck:** Validierung Graceful-Degradation. Wenn Discord nicht installiert ist (= IPC-Pipe nicht erreichbar), darf DiscordPresenceManager nicht crashen, Settings-UI zeigt klaren Hinweis, alles andere läuft normal.

## Reproduktions-Anweisung

### Setup

```
1. Test-Maschine ohne Discord-Installation (Michels Setup)
   ODER: Discord-Prozess killen vor Test
   $ taskkill /F /IM Discord.exe   # Windows
2. Verifizieren: $ tasklist | findstr Discord  → leer
3. v0.9.0-rc.N starten
4. Settings → Integrations → Discord Rich Presence Toggle aktivieren
5. Settings-Dialog soll zeigen: „Discord-Client nicht gefunden — bitte starten oder installieren"
6. Trotzdem normal Flug machen (kurzer Hop)
```

### Pilot-Aktionen

| Sim-Zeit (mm:ss) | Aktion |
|---|---|
| 00:00 | Flight Start mit aktiviertem Discord-Toggle obwohl Discord aus |
| 02:00 | Takeoff |
| 08:00 | Landing |
| 10:00 | File PIREP |

**Variation:** Während Flug Discord-Client starten → Manager soll Discord erkennen und retroaktiv State setzen (= Auto-Reconnect zum IPC).

## Fixture / Replay

**Pfad:** `tests/fixtures/golden-flights/G9-no-discord.jsonl.gz` (optional)

**Status:** Replay nicht reproduzierbar (externer Prozess-Status). Live-Test mit Michel.

## Erwartete Phase-Sequence

Trivial.

## Erwartete PIREP-Felder

Trivial. KEIN spezielles Feld.

## Erwartete Bounce-Werte

Trivial.

## Erwartetes GlitchTip-Verhalten

**Erwartete Events:** **0**

Auch wenn Discord nicht erreichbar ist, darf das NICHT als GlitchTip-Error landen — das ist erwartetes Fallback-Verhalten, kein Bug.

Activity-Log-Eintrag erwartet (INFO-Level, NICHT WARN/ERROR):

```
[INFO] discord_rpc: Discord-Client nicht erreichbar — RPC deaktiviert. Toggle bleibt aktiv, automatischer Reconnect bei nächster Discord-Verbindung.
```

## Erwartetes Discord-RPC-Verhalten

**State während Flug:** KEINER (= Discord ist ja aus).

**Bei Discord-Start während Flug (Variation):** Binnen 10 s nach Discord-Start erscheint der korrekte State (z.B. `LANDING · C172`).

## Privacy-Check

n/a.

## Pass/Fail-Kriterien

- [ ] Pilot-Client crasht NICHT bei aktiviertem Discord-Toggle + nicht-installiertem Discord
- [ ] Settings-UI zeigt klare Meldung „Discord-Client nicht gefunden"
- [ ] Activity-Log hat INFO-Eintrag (NICHT ERROR/WARN)
- [ ] 0 GlitchTip-Events
- [ ] Bei Discord-Start während Flug: Auto-Reconnect funktioniert
- [ ] Tester-Signatur

## Estimated QS-Time

- Sim-Zeit: ~15 min
- Auswertung: ~15 min
- **Total: ~30 min ≈ 0.5 h**

---

# G10 — Alle neuen Features deaktiviert (Opt-Out-Vollständigkeit)

**Test-Pilot:** Thomas K
**Sim:** MSFS 2024
**Aircraft:** beliebig (Default-Cessna ok)
**Hauptzweck:** Sentinel-Test für vollen Rollback-Pfad. Wenn ein Pilot alle 7 v0.9.0-Feature-Flags abschaltet, soll Verhalten 1:1 identisch mit v0.8.4 sein.

## Reproduktions-Anweisung

### Setup

```
1. v0.9.0-rc.N, frische config.json
2. In Settings ALLE Toggles AUS:
   - position_sampling.delta_enabled = false   (= zurück zu Fixed-Rate-Sampling)
   - phases.rejected_take_off_enabled = false
   - phases.go_around_enabled = false
   - phases.deboarding_enabled = false
   - forensics.per_bounce_enabled = false
   - error_reporting.enabled = false
   - integrations.discord_rpc.enabled = false
3. MQTT-Capture starten
4. Bid auswählen, Sim starten
```

### Pilot-Aktionen

Ein normaler 30-Min-Flug von einem kleinen Airport zum nächsten (z.B. EDDB → EDDH). Inkludiert eine sanfte Landung. KEINE RTO, KEIN GO_AROUND.

## Fixture / Replay

**Pfad:** `tests/fixtures/golden-flights/G10-all-opt-out.jsonl.gz`

**Status:** muss aufgezeichnet werden.

## Erwartete Phase-Sequence

NUR die v0.8.x-Phasen:

```
PREFLIGHT → BOARDING → PUSHBACK → TAXI_OUT → TAKEOFF_ROLL → TAKEOFF
→ CLIMB → CRUISE → DESCENT → APPROACH → FINAL → LANDING → ARRIVED
→ TAXI_IN → BLOCKS_ON
```

**Negativ-Erwartung:** KEIN `DEBOARDING` (= Flag aus). KEIN `REJECTED_TAKE_OFF`, KEIN `GO_AROUND`.

## Erwartete PIREP-Felder

| Feld | Erwarteter Wert |
|---|---|
| `bounces` | nicht vorhanden (Flag aus) |
| `rto_event` | nicht vorhanden |
| `go_around_events` | nicht vorhanden |
| `touchdown.bounces` | `null` ODER nicht vorhanden |
| `touchdown.forensic_bounce_count` | weiter vorhanden (= bestehendes v0.8.x-Feld) |
| `touchdown.scored_bounce_count` | weiter vorhanden |
| `forensics_version` | `1` (= legacy, weil Per-Bounce-Flag aus) ODER `2` mit nur Counter, kein Detail-Array — abhängig von Implementierung. Spec-Owner muss vor Test klären. |

## Erwartete Bounce-Werte

```json
{
  "forensic_bounce_count": 0,
  "scored_bounce_count": 0,
  "bounces": null
}
```

(Standard-Landung ohne Bounce.)

## Erwartetes Position-Stream-Verhalten

**Fixed-Rate-Sampling wie v0.8.4** — alle 5–8 s ein Position-Event. KEINE Delta-Triggering, KEINE Heartbeats anders als v0.8.4. Position-Stream-Volumen sollte 1:1 mit v0.8.4-Referenz übereinstimmen.

## Erwartetes GlitchTip-Verhalten

**Erwartete Events:** **0** (genauer: SDK ist nicht mal initialisiert, kein einziger HTTPS-Request an GlitchTip).

**Wireshark-Validation:** 0 Connections zu `tip.kant.ovh` während des gesamten Test-Zeitraums.

```bash
# Wireshark-Capture nach Test
$ tshark -r results/G10/run-*/capture.pcapng -Y 'host tip.kant.ovh' | wc -l
0
```

## Erwartetes Discord-RPC-Verhalten

**0 Discord-RPC-Calls.** Manager wird nicht initialisiert. Discord (falls laufend) zeigt KEINEN ASA-ACARS-Status.

## Privacy-Check

**Stärkster Privacy-Check des gesamten Test-Sets:**

- 0 Bytes an GlitchTip
- 0 Bytes an Discord-RPC-IPC
- Nur MQTT-Traffic zum ASA-ACARS-VPS (= identisch v0.8.4)

```bash
# Pcap-Auswertung: nur erlaubte Endpoints
$ tshark -r results/G10/run-*/capture.pcapng -T fields -e ip.dst | sort -u
# Erwartet: nur live.kant.ovh, ggf. DNS-Server. KEINE tip.kant.ovh, KEINE discord.com.
```

## Pass/Fail-Kriterien

- [ ] Phase-Sequenz enthält KEIN `DEBOARDING`, `REJECTED_TAKE_OFF` oder `GO_AROUND`
- [ ] PIREP hat KEINE `bounces`, `rto_event`, `go_around_events` Felder
- [ ] Position-Stream-Volumen ±5% identisch mit v0.8.4-Referenz-Fixture
- [ ] 0 HTTPS-Requests an `tip.kant.ovh` (Wireshark)
- [ ] 0 IPC-Calls an Discord
- [ ] Discord-Client zeigt KEINEN ASA-ACARS-Status
- [ ] PIREP-JSON validiert auch von v0.8.4-Webapp (= Backward-Compat in beide Richtungen)
- [ ] Tester-Signatur

## Estimated QS-Time

- Sim-Zeit: ~30 min
- Auswertung: ~30 min (Wireshark-Auswertung + v0.8.4-Webapp-Cross-Check)
- **Total: ~60 min ≈ 1 h**

---

# Total QS-Zeit-Budget

## Pro Test (Recap)

| Test | Live-Sim | Auswertung | Total |
|---|---|---|---|
| G1 | 95 min | 25 min | 120 min |
| G2 | 25 min | 30 min | 55 min |
| G3 | 15 min | 25 min | 40 min |
| G4 | 40 min | 30 min | 70 min |
| G5 | 25 min | 20 min | 45 min |
| G6 | 0 (Replay) | 30 min | 30 min |
| G7 | 0 (Webapp) | 15 min | 15 min |
| G8 | 10 min | 20 min | 30 min |
| G9 | 15 min | 15 min | 30 min |
| G10 | 30 min | 30 min | 60 min |
| **Summe** | **255 min** | **240 min** | **495 min ≈ 8.3 h** |

## Verteilung auf Tester

| Tester | Tests | Zeit-Anteil |
|---|---|---|
| Thomas K | G3, G5, G7, G8, G10 (+ Auswertungen aller anderen) | ~4–5 h |
| Adrian | G1, G4, G6 (Replay) | ~3.5 h |
| Svenny | G2 | ~1 h |
| Michel | G9 | ~0.5 h |
| **Total Personenstunden** | | **~9–10 h** |

## Sprint-Planung

- **Einzel-Tester (= nur Thomas):** ~8.3 h = **1 voller Test-Tag**
- **Parallel auf 4 Tester verteilt:** **~3 h Wallclock** (= ein Nachmittag)
- **Replay-Modus für CI (G1, G2, G4, G6, G10):** ~2 h Wallclock + Sim-Sessions reduziert
- **Bei Re-Test nach Bugfix:** mind. der gefailte Test + alle abhängigen Specs (= meist 2–3 Tests, ~1.5 h)

## Release-Gate-Regel

**Pflicht für v0.9.0-Release:**

1. **MINDESTENS einmal Live-Sim-Lauf für jeden Test** — Replay alleine reicht NICHT für das Release-Voting
2. **ALLE 10 Tests müssen pass sein** — Single-Fail = Release-Block
3. **Datenschutz-Gate DS1–DS8 aus telemetry-contract.md Sektion 9 muss zusätzlich pass sein** (= ~4 h extra)
4. **Bei Fail: Root-Cause + Fix + Re-Test des gefailten Szenarios PLUS aller Szenarien die den gleichen Code-Pfad nutzen**

**Bei Release-Tag-Push:** Im Release-Body bilingualer Block muss enthalten:
- „🇩🇪 E2E-Golden-Flights G1–G10 vollständig durchlaufen am [Datum] durch [Tester]"
- „🇬🇧 E2E Golden Flights G1–G10 fully passed on [date] by [testers]"

## Tracker

- **Anchor:** QS-Round-3-Befund 2026-05-18 — „G1–G10-Tabelle ist Testliste, kein QS-Gate"
- **Blockiert bis:** Spec gemerged + Fixture-Aufzeichnungs-Workflow im Pilot-Client implementiert (`asa-acars-cli --record-fixture` + `replay` + `inject-gaps`)
- **Folge-Specs:**
  - `asa-acars-cli-replay-and-fixtures.md` — exakte CLI-Spec für die o.g. Werkzeuge
  - `v0.9.0-release-checklist.md` — Konsolidierung aus Telemetry-Contract Sektion 8/9/10 + diesem Spec + Datenschutz-Gate
  - `beta-pilot-program.md` — formelle Onboarding-Anweisung für Adrian/Svenny/Michel inkl. Test-Maschinen-Setup

## Out of Scope

- **Visual-Regression-Tests der Webapp** (= separate Spec, mit Playwright/Chromatic)
- **Performance-Benchmarks** (FPS-Impact von ASA-ACARS auf Sim — separate Spec)
- **Multi-Pilot-Race-Conditions** auf dem VPS (= eigenes Spec, braucht 3+ parallele Sim-Sessions)
- **Automatisierte CI-Pipeline** für G-Tests (= Folgespec, MVP ist manueller Test)
- **Long-Haul-Flüge > 6 h** (= G1 deckt 90 min, längere Flüge sind v1.0-Thema)
- **Mobile-Device-Tests** (= keine Mobile-App)
- **i18n-Validation** in Discord-RPC-Strings (= eigener i18n-Test, nicht in G-Reihe)
